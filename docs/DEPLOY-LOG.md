# Dağıtım Günlüğü (Deploy Log)

Prod'a (VPS) yapılan her dağıtımın **append-only** kaydı. En yeni en üstte.
Her satır: tarih · sürüm/commit · ne dağıtıldı · sonuç. `scripts/deploy.sh` bu dosyaya
otomatik satır ekler; elle dağıtımda da buraya yaz. Bu, "güncelleme geçmişi"nin kalıcı
kaydıdır (sohbet hafızasından bağımsız).

Hedef: `root@167.233.108.12:/opt/lisans-yonetim-paneli` · URL'ler:
`api.167-233-108-12.sslip.io` / `admin.167-233-108-12.sslip.io`

| Tarih | Commit | Servis(ler) | Not | Sonuç |
|---|---|---|---|---|
| 2026-08-18 | f8e51e3 | api + admin | **MAK dağıtım politikası (ürün ayarı) + yanlış girilen anahtarın KALICI silinmesi (migration 0046).** İki operatör şikâyeti, ikisi de kodda kök nedeniyle doğrulandı; plan modunda tasarlanıp onaylandı. **[1 — dağıtım tersine sabitti]** *"3 adet MAK aldı, stokta 3 farklı anahtar varsa 3 farklı anahtar gönderebilir mi?"* Motor bunun TERSİNİ yapıyordu (`ORDER BY (kalan >= istenen) DESC` → sipariş başına EN AZ anahtar) ve ürün bazında değiştirilemiyordu. Artık `products.multi_use_distribution`: **En az anahtar** (varsayılan, davranış DEĞİŞMEDİ) | **Ayrı anahtar** (3 birim + 3 uygun anahtar = 3 atama × 1 hak; ayrı anahtar bitince kalan talep AYNEN eski doldurmaya düşer → 1 anahtar + 6 birim = tek anahtardan 6 hak). Sıralama FEFO/FIFO KALDI (`use_count ASC` reddedildi: FEFO'yu düşürür, siparişler arası dağıtır, `use_count` indekslenince kapasite düşümünün HOT-update'ini öldürürdü) → mevcut `license_items_alloc_idx` ile birebir örtüşür, **yeni indeks gerekmedi**. Satır başına `MAX_SPREAD_KEYS=100` + görünür uyarı. Müşteri metni *"bu **siparişte**"* → *"bu **anahtarda** N etkinleştirme hakkınız var"* (panel + WP aynı cümle; etiket atama satırı başına basılıyor, yeni politikada eski cümle "toplam 1 hak mı var?" diye okunurdu). **[2 — silinen anahtar tekrar girilemiyordu]** "Sil" kaydı SİLMİYOR (`voided`, bilinçli) ama mükerrer engeli tablo genelinde ve statüye bakmıyor; üretimde `license_items` satırını silen **hiçbir yol yoktu** → operatörün çıkışı YOKTU. Owner-only `DELETE /v1/admin/license-items/:id/purge` (sebep zorunlu, denetim izi kalır), **iki adımlı kapı** (yalnız ZATEN `voided` kalem) + **altı guard**: teslim edilmiş · karantinadaki · **GEÇMİŞ ataması olan (revoked dahil)** · değişim soyağacında geçen · tedarikçiye bildirilmiş · **parti geri çekmesiyle ölmüş**. Son guard parti DURUMUNA değil kalemi NEYİN öldürdüğüne bakar (aksi halde tam da çözülen vaka — elle void + sonra recall — bloklanırdı). *Reddedilen alternatif:* unique index'i kısmi yapmak (geri çekilmiş kusurlu anahtar denetimsiz satılabilir hâle gelirdi). **[3]** `duplicateDetail`: mükerrer artık NEDENİNİ söylüyor (*"1 tanesi GEÇERSİZ KILINMIŞ bir kayıtla çakıştı — kalıcı silerseniz tekrar girebilirsiniz"*), kuru çalıştırma ile gerçek giriş TEK yardımcıdan besleniyor; "Sil" onayı kayıp yolu ekranda anlatıyor. | typecheck 4/4 + **altı kapı** · birim 68+184+170 · **VPS izole PG17+Redis7: entegrasyon 454/454** (447 → +7) **+ yarış 3/3** · `db:generate` **"No schema changes"** · build 3/3 · **iki kontrol denemesi:** form alanını gövdeden çıkarmak ve DIŞLAMA listesini devre dışı bırakmak testleri KIRMIZI yaptı (ilk denemem hiçbir şeyi ayırt etmemişti — spread'de çağıran zaten `want=1` geçiyor; bu ayrım kodda dürüstçe not edildi) · **dev'de uçtan uca ölçüldü:** ürün `one-per-key` olarak KALICI, ve purge senaryosu birebir koştu — import ✓ → void ✓ → aynı anahtar **duplicates:1 + `existingByStatus:{voided:1}`** → purge ✓ → aynı anahtar **imported:1**; defterde `void`(qty=500) + `purge`(qty=0) satırları ayakta · dev **36 rota 200** · prod `deploy.sh api admin` (sağlık kapısı + rollback) → `/v1/health` **200 v1.1.0** (db+redis), migration izleme **47**, `products.multi_use_distribution` canlı (NOT NULL, default `fewest-keys`), api **0 ERROR**, purge ucu canlıda: rol `admin` → **403**, sebep 2 karakter → **400**, olmayan kalem → **404**. |
| 2026-08-17 | 1daa9ca | api + admin | **Kalan iki eksik + kendi kapımın kaçağı (migration YOK).** Kullanıcı: *"geri kalan tüm eksikleri tamamla, projeyi daha iyi hale getir, mimariyi/projeyi daha düzenli derli toplu hale getir."* Kodda duran üç `TODO` tarandı; ikisi "yapılmadı" değil **çalışan arıza**ydı. **[Sessiz kırpma — `TODO(api)`]** `GET /v1/admin/deployments` hedef süzgeci KABUL ETMİYORDU. Panel dağıtımı, eklenti yayını ve DR işleri AYNI kuyrukta durur; sabit 50'lik pencere gecelik yedek satırlarıyla **~50 günde tamamen dolar** → `/releases`, geçmişte GERÇEKTEN yapılmış yayınlar dururken "Henüz yayın işi yok" derdi. Kod bunu biliyordu ve yalnız "pencere doldu" uyarısı veriyordu (doğru teşhis, eksik çözüm). Artık `?target=` (virgülle çoklu, whitelist dışı **400**) + `?limit=` (1..200) SUNUCUDA süzer; geçersiz hedef **fail-closed** (boş liste). Parametresiz çağrı eski davranışı korur. **`/deployments` ekranı BİLEREK süzmez:** o liste `hasActiveDeployment` guard'ını besliyor ve "tek aktif iş" güvencesi hedeften bağımsız — süzülseydi çalışan yedek görünmez olur, dağıtım düğmesi açık kalır, operatör 409 yerdi (H1 sınıfı: bir kümeyi daraltmak onu okuyan HER yolu etkiler); pencere 50→150 büyütüldü. **[`TODO(ops)`]** Haftalık TAM mutabakat HİÇ zamanlanmamıştı: 15dk'lık sweep yalnız son 30 günü tarar, penceresinden çıkmış bir çifte satış BİR DAHA HİÇ raporlanmıyordu → `reconcile-full` zamanlayıcısı (Pazar 04:15, `RECONCILE_FULL_CRON`) + `?full=true`. Bunun için `sole-scheduler` çoğullaştırıldı (`upsertJobSchedulers`): tekil sürüm aynı kuyruğa ikinci zamanlayıcıyı **sessizce imkânsız** kılıyordu (her çağrı diğerini yetim sayıp siliyor, geriye tek `warn` kalıyordu). **[Kendi kapım]** Bir önceki turda eklediğim `check-docs` uç denetimi yalnız İLK segmente bakıyordu; çıplak bir `@Controller('admin')` olduğu için `/v1/admin/<herhangi>` uydurması SESSİZCE geçiyordu — panel uçlarının neredeyse hepsi orada, yani denetim fiilen boştu. Kontrol denemesiyle ölçüldü, metot yolları da toplanıyor, tekrar denendi → **KIRMIZI** (anılan uç 8→13). **[Yan]** `ZodBody` girdi tipi `unknown`a genişletildi (query string metin taşır; eski imza dönüştüren şemayı pipe'a hiç sokamıyordu). **[Kapı yakaladı]** `RECONCILE_FULL_CRON` compose'dan geçmiyordu → `check-env-passthrough` kırmızı verdi. | typecheck 4/4 + **altı kapı** · birim 68+184+**170** · **VPS izole PG17+Redis7: entegrasyon 436/436** (429 → +7) **+ yarış 3/3** · build 3/3 · **dev'de arıza birebir kuruldu ve ölçüldü:** 90 günlük bir yayın işi 60 yedek satırının altına gömüldü → süzgeçsiz uç onu **görmüyor (0)**, `?target=plugin` **görüyor (1)**, `/releases` ekranı işi listeliyor · dev **36 rota 200** · prod `deploy.sh api admin` (sağlık kapısı + rollback) → `/v1/health` **200 v1.1.0** (db+redis), prod Redis'te **2 reconcile zamanlayıcısı** (`reconcile-sweep` + `reconcile-full`, yetim uyarısı YOK), api **0 ERROR**, `?target=` 200 / geçersiz hedef 400 canlıda doğrulandı. |
| 2026-08-17 | 00647b3 | api + admin | **İnceleme turu: iki sessiz sayı hatası + kilitli `LIMIT` deseninin iki kardeşi + belge düzeni (migration YOK).** Kullanıcı: *"gerekli dağıtımları yap ve incelemelere devam et, sorunları gider, projeyi derli toplu düzenli bir hale getir."* **[Sessiz 10× kapasite]** Stok girişindeki `parseMaxUses` noktayı KOŞULSUZ atıyordu → sayı biçimli bir Excel hücresinden gelen `500.0` sessizce **5000** oluyordu; bu, özelliğin ÖNLEMEK için yazıldığı aşırı satışın ta kendisi (panel 5.000 hak sanar, anahtar 500’de biter) ve hiçbir katmanda hata üretmiyor. Fonksiyonun HİÇ testi yoktu; hata test yazılırken çıktı. Ayraç artık ancak ardından TAM 3 rakam geliyorsa binliktir. **[Sessiz 1000× maliyet]** Aynı ekranın para alanında yalnız nokta varken değer koşulsuz ondalık sayılıyordu → tr-TR yazımıyla `1.234` giren operatör 1234 ₺ sanıp **1,23 ₺** kaydediyordu (birim maliyet her lisansa snapshot’lanır, maliyet raporu + tedarikçi karnesi onu okur). **[Kilitli LIMIT — atama motorundaki aşırı teslimat hatasının kardeşleri]** MAK kapasite tüketimi CTE kullanıyordu ama `MATERIALIZED` DEMİYORDU ve docstring "MAK yolu zaten doğru deseni kullanıyor" diye YANLIŞ bilgi veriyordu (açıklamanın kendisi denetimi yanlış yönlendirdi) → düzeltildi + fail-closed kalkan. Dağıtım kuyruğunun `claimNext`’i de `UPDATE … WHERE id = (SELECT … LIMIT 1 FOR UPDATE SKIP LOCKED)` yazıyordu (tek çağrıda birden çok isteği `running` yapabilir, `.returning()` yalnız ilkini döndürür → diğerleri sessizce öksüz kalır ve "tek aktif iş" kilidi zombi süpürmesine kadar açılmaz) → seç/güncelle ayrı ifadeye alındı (tek transaction). **[Semver tek kaynağa]** "Hangi sürüm en yeni" kuralının İKİ kopyası vardı — API (sitelerin fiilen indirdiği paket) ve admin `/releases` (rozet + yayın kapısı); davranışları aynıydı, sorun yarınki sapmaydı → `@lisans/shared/domain/semver`. **[Tema yedeği]** `restore.sh` "tek komutta eski temaya dön" diyordu ama yedekteki `globals.css` bugünkü yüzey token’larını taşımıyor (ölçüldü: `--success-vivid` yedekte 0, güncelde 6) → çalıştırılsa sessizce renksiz arayüz bırakırdı; betik artık fail-closed. **[Araç]** Entegrasyon+yarış paketi için tekrarlanabilir giriş noktası `pnpm test:iso` (izole PG17+Redis7, `--frozen-lockfile`) — elle kurulum üç kez sahte/eksik doğrulama üretmişti; sonuncusu bu turda ÖLÇÜLDÜ: checkout’ta **vitest 2.1.9** dururken lockfile 3.2.6 istiyordu, paket sessizce yanlış araç zinciriyle koşuyordu. **[Belge düzeni]** `CLAUDE.md` **310 KB → 18 KB**; tur-tur günlük BİREBİR `docs/GECMIS.md`’ye taşındı (2857 satır satır-satır doğrulandı, kayıp yok), yerine damıtılmış durum özeti + 20 maddelik "tekrarlayan tuzaklar" + doğrulama tablosu geldi; geliştirme komutları paragrafı birebir iki kez yazılmıştı, tekilleştirildi. CHANGELOG’a bu tur + YAZILMAMIŞ önceki tur eklendi. | ✅ typecheck 4/4 + **beş kapı** (use-server 26/89 · nest-wiring 42/133+13 · env 51 · workflows 2/4/47 · tx-pool **38**) · birim **68+184+165** · build 3/3 · şema sapması YOK (`db:generate` "No schema changes") · **VPS izole PG17+Redis7: entegrasyon 429/429 + yarış 3/3** (yeni `claimNext` invaryant testi dahil) · PHP-lint temiz + eklenti davranış **108/108** · `deploy.sh api admin` (sağlık kapısı + rollback) → `/v1/health` **200 v1.1.0** (db+redis), admin `/pending` 200, api **0 ERROR**. |
| 2026-08-17 | ab56a71 | admin + eklenti v1.1.6→v1.1.7 | **MAK sayıları artık kendini anlatıyor (kullanıcı geri bildirimi) + iade sonrası bayat panel-durumu.** Kullanıcı: *"bu siparişte 5 etkinleştirme · anahtar geneli 5/5 — daha açıklayıcı olmalı; başka yerde de yeterince belirtilmiyorsa düzelt"*. Sayılar DOĞRUYDU ama okuyan kişi (a) "aynı anahtar başka siparişlerde de kullanılıyor" bilgisini bilmek, (b) "satılabilir hak kaldı mı" sorusunu kendi çıkarmak zorundaydı. Sipariş detayında satır artık **"Bu siparişe N etkinleştirme"** + **"Anahtarın toplamı: X/Y · N hak kaldı"** (tükenmişse "tükendi"), ikisinde de açık ipucu; lisans listesinin başında **tek seferlik açıklama** (yalnız MAK satırında). Aynı belirsizliğin diğer yerleri: ürün özeti **"MAK×5" → "MAK (varsayılan 5 kullanım)"** (o sayı artık bağlayıcı tavan değil VARSAYILAN — gerçek üründe 5/3/1001 kapasiteli anahtarlar bir arada bulunabiliyor ve "×5" "her anahtar 5 taşır" diye okunuyordu) · ürün listesi STOK kolonu MAK satırında **"hak" birimi** (aynı kolon tek kullanımlıkta ANAHTAR, MAK'ta KULLANIM HAKKI sayıyor; çıplak "1002" "1002 anahtar" diye okunuyordu) · kategori kartı "N stok" ipucu · mağaza sipariş ekranı (eklenti v1.1.7) panelle BİREBİR aynı dile hizalandı + kalan hak eklendi. **Migration YOK.** | ✅ typecheck 4/4 + 5 kapı · admin birim 159/159 · admin production build · dev **36 rota 200, hata sınırına düşen yok** (`smoke-routes.sh`) · dev gerçek MAK siparişinde ölçüldü ("Bu siparişe 5 etkinleştirme · Anahtarın toplamı: 5/5 · tükendi" + açıklama satırı) ve ürün listesinde "MAK (varsayılan 5 kullanım) · 1002 hak" · prod `deploy.sh admin` (rollback'li) → `/v1/health` 200 v1.1.0, admin `/pending` 200, admin logunda 0 hata · **eklenti v1.1.7 yayınlandı** (201; public info 1.1.7) |
| 2026-08-17 | e649b9c | api + admin + eklenti v1.1.6 | **MAK kapasitesi ANAHTAR BAŞINA + dev sıfırlama + tüm sipariş türlerinin uçtan uca testi.** Kullanıcı: *"MAK stoğu eklerken kapasiteyi ayarlayamıyorum; ürün düzenleme yerine anahtara göre vermek daha doğru"*. Şema ZATEN satır bazındaydı (`license_items.max_uses`); import onu ürün ayarından KOPYALIYORDU → operatör ya her lot için ürünü değiştirmek (ve o sırada giren her şeyi bozmak) ya da yanlış kapasiteyle girmek zorundaydı; ikincisi SESSİZ AŞIRI SATIŞ (panel 500 hak sanar, anahtar 50'de biter). `ImportItem.maxUses` (1..100.000, verilmezse ürün varsayılanı); tek kullanımlıkta gönderilirse TÜM istek 400 (sessizce yok sayılmaz); geçersiz değer YALNIZ o satırı reddeder; kilitli yeniden-doğrulama artık yalnız VARSAYILANA DAYANAN satırları geçersiz kılar. **`productCapacityChange` daraltıldı:** veri bozan tek geçiş `multi → single`; varsayılanı DÜŞÜRMEK serbest (satırlar kendi kapasitesini taşır, atama/stok/rapor yolları satırdan okur) — eskiden 409 verip 50'lik lota geçen operatörü kilitliyordu. Arayüz: "Her anahtarın kullanım hakkı" alanı (ürün varsayılanıyla dolu) + "kapasiteleri farklı" seçeneğiyle İKİ SÜTUNLU yapıştırma (sessiz çıkarım YOK: seçenek kapalıyken ikinci sütun asla yorumlanmaz); sayaç/onay modali toplamı satır kapasitelerinden hesaplar; ürün formu etiketi "Anahtar kapasitesi (varsayılan)". · **DEV SIFIRLAMA (kullanıcı isteği):** belgelenmiş yordam (yedek → API DUR → WP force-delete → panel TRUNCATE → Redis FLUSHALL → API BAŞLAT); `dev-clean-3.sql` şemayla hizalandı (`supplier_claims`/`supplier_claim_items` eklendi — `suppliers` FK'si yüzünden CASCADE'siz TRUNCATE zaten hata verirdi = güvenlik ağı çalışıyor; `product_guides`/`product_categories` BİLEREK korundu). PROD'A DOKUNULMADI. · **TÜM SİPARİŞ TÜRLERİ GERÇEK WooCommerce'te (müşteri admin@dev.local, 11 sipariş):** tek kullanımlık tam · MAK tek-anahtar (kapasite 500 → TEK atama 3 birim) · MAK KARIŞIK kapasite (5+3 anahtar, 7 birim → 5+2 taşma) · süreli hesap (valid_until +365g) · kod/hediye çeki · çok kalemli karışık · kısmi teslimat (1/5) · stok yok (pending) · ya-hep-ya-hiç (stok 2 / talep 5 → 0 teslim) · eşlenmemiş ürün (unmapped, yanlış teslim YOK) · stoksuz/ön sipariş (pending). Ardından tam iade (anahtar KARANTİNA, satır canceled) · kısmi iade (MAK 7→5; §2 kapasite havuza DÖNMEDİ) · "Sorun Bildir" (destek talebi 201) · otomatik tamamlama (stok girişi 3 bekleyen satırı doldurdu). · **TESTİN BULDUĞU KUSUR [eklenti v1.1.6]:** tam iade sonrası mağaza sipariş listesindeki panel-durum kolonu/filtresi BAYAT kalıyordu (panel `revoked`, liste "Teslim edildi"). Aynı kavramın iki meta anahtarı var ve `_wpteslimat_panel_status` yalnız geri-kanal webhook'uyla yazılıyordu; panel iade için webhook ÜRETMEZ. Ortak `set_panel_status()` (iki meta + bayat sayaç temizliği) push/uzlaştırma/iade yollarının üçüne birden bağlandı. **Migration YOK.** | ✅ typecheck 4/4 + **5 kapı** (use-server 26/89 · nest-wiring 42/133+13 · env 51 · workflows 2/4/47 · tx-pool 37) · birim shared 68 + api **184** + admin **159** · admin production build (/stock/import 19→25,4 kB) · **VPS izole PG+Redis: entegrasyon 428/428** (5 yeni kapasite testi) · **kontrol denemesi:** satır kapasitesi geri alınınca 3 test KIRMIZI · PHP-lint temiz + eklenti davranış **108/108** · dev sıfırlama doğrulandı (panel 0 sipariş / WP 0 sipariş; site+ürün+kategori+rehber+eşleme+katalog korundu; **6 sweep zamanlayıcısı yeniden kuruldu**) · **iade düzeltmesinin ÖNCE/SONRA kanıtı aynı ortamda:** #76 (düzeltme öncesi) `panel_status=fulfilled` bayat, #77 (sonrası) `panel_status=revoked` · prod `deploy.sh api admin` (rollback'li) → `/v1/health` **200 v1.1.0**, admin `/pending` **200**, api **0 ERROR** · **eklenti v1.1.6 yayınlandı** (201, 138.544 bayt; public info 1.1.6 + download 200 birebir) |
| 2026-08-17 | 9199d32 | api + admin + eklenti v1.1.5 | **MAK teslimat görünürlüğü + tek anahtar tercihi + gerçek mail önizlemesi (kullanıcı bildirimi).** Bildirilen: 6 adetlik MAK siparişinde panel iki anahtar yanında "10/10" ve "6/10" gösteriyor → operatör toplayıp "16 kullanım hakkı teslim edilmiş" sonucuna varıyor. **ÖLÇÜLDÜ (dev DB): veri DOĞRU, ekran yanlış anlatıyor** — o siparişin payı 5+1=6; sayaçlar ANAHTARIN TÜM siparişlerdeki toplamı (#58→2, #59→3, #69→5 / #59→1, #67→3, #68→1, #69→1) ve bu hiçbir yerde YAZMIYORDU. Aynı düzeltme WP eklentisinde v1.0.7 ile yapılmıştı, **panelin kendi sipariş detayı geride kalmıştı**. → satırda önce **"bu siparişte N etkinleştirme"** (vurgulu), yanında etiketli **"anahtar geneli X/Y"** (title: "bu siparişin payı değildir") + özet şeridine "toplam N etkinleştirme hakkı". Kip artık ürünün `usageMode` alanından belirlenir (`maxUses>1` yalnız eski API imajı yedeği). · **[DAĞITIM ŞEKLİ — asıl istek] `consumeMultiUseCapacity` sıralamasına "talebi TEK BAŞINA karşılayan anahtar" birincil anahtarı** (FEFO+FIFO ikincil olarak AYNEN durur): tipik sipariş artık TEK anahtar + TEK atama alır ("aynı anahtarı N kez etkinleştir"); hiçbiri yetmezse eski doldur-taşır davranışı birebir korunur; küçük talepler yarım kalmış anahtarları önce kapatır (parça kapasite çürümez). Gerekçe yalnız derli görünüm değil: MAK anahtarı PAYLAŞIMLIDIR ve panel yalnız defter tutar → müşterinin eline geçen her FAZLADAN anahtar fazladan aşırı-etkinleştirme yüzeyidir. **Dev gerçek HMAC siparişiyle kanıtlandı:** ilk anahtarda 3 boş, talep 5 → eski davranış 3+2 (iki anahtar), yeni davranış tek atama units=5 (5 boşu olan anahtardan), diğer iki anahtara dokunulmadı. · **[MÜŞTERİ METNİ] Mail "(5 adet)" diyordu** → "5 anahtar" gibi okunuyordu; **birim sayısı 1 olan MAK anahtarında ise HİÇBİR ŞEY yazmıyordu** (müşteri paylaşımlı anahtarın tamamının kendisine ait olduğunu sanıyordu — gerçek sipariş #69 ikinci satırı tam olarak buydu). Karar artık birim sayısına değil ÜRÜN MODUNA bağlı: çok kullanımlıysa HER ZAMAN yazılır → "(bu siparişte N etkinleştirme hakkı)" (hesap ürününde "kullanım hakkı" — hesap açılır, etkinleştirilmez). Aynı cümle mağaza sayfası + indirilen .txt + mağaza sipariş ekranında da görünür (eklenti v1.1.5, tek kaynak `units_note`). `getDeliveries` + `siteAdminView` EKLEMELİ `usageMode` döndürür; alan gelmezse eklenti eski kuralına düşer (dağıtım sapması toleransı). · **[MAİL ÖNİZLEMESİ] gerçeğe benzetildi:** ham kaynak Mailpit üzerinden okundu — tek parça `text/plain; charset=utf-8`, HTML parça YOK → önizleme artık e-posta TIPKIBASIMI: zarf başlığı (Kimden/Kime/Konu/Tarih), orantılı yazı tipi (istemciler düz metni monospace göstermez), **beyaz kâğıt (koyu temada da)** — panelin teması "müşteriye ne gidiyor" yargısını değiştirmemeli —, HTML enjeksiyonu olmadan linkify (React düğümleri; şablonu operatör yazar). Uydurma gönderim tarihi BASILMAZ ("Önizleme anı"). Gönderici başlığı artık builder tek kaynağından okunur. **Migration YOK.** | ✅ typecheck 4/4 + **5 kapı** (use-server 26/89 · nest-wiring 42/133+13 · env 51 · workflows 2/4/47 · tx-pool 37) · birim shared 68 + api **184** + admin **159** · admin production build · **VPS izole PG+Redis: entegrasyon 423/423 + yarış 3/3** · **kontrol denemesi:** düzeltme geri alınınca yeni 3 MAK dağıtım testi KIRMIZI · PHP-lint temiz + eklenti davranış **108/108** · **dev canlı doğrulama:** sipariş detayı "bu siparişte 5/1 etkinleştirme · anahtar geneli 10/15 · 6/10", mail önizlemesi tarayıcıda ÖLÇÜLDÜ (kâğıt rgb(255,255,255) koyu temada, gövde 14px/1.6 pre-wrap, kontrast **7.3:1**, 375px yatay kayma **0**, gerçek bağlantı render edildi), gerçek HMAC siparişiyle tek-anahtar tercihi kanıtlandı · prod deploy.sh api admin (rollback'li) → `/v1/health` **200 v1.1.0**, admin `/pending` **200**, api **0 ERROR** · **eklenti v1.1.5 yayınlandı** (201, 137.848 bayt; public plugin/info 1.1.5, download/1.1.5 200 birebir boyut) |
| 2026-08-17 | cb85d8e | api + admin + eklenti v1.1.4 | **[KRİTİK/üretimde canlıydı] Atama motorunda AŞIRI TESLİMAT — `LIMIT` kaçağı.** `assignAvailableSingleUse` `UPDATE license_items WHERE id IN (SELECT … LIMIT n FOR UPDATE SKIP LOCKED)` yazıyordu. READ COMMITTED'da UPDATE hedef satırı eşzamanlı değiştirilmiş bulursa satırın YENİ sürümünde WHERE'i yeniden değerlendirir (EvalPlanQual) ve `IN (…)` alt sorgusu YENİDEN KOŞAR; her koşuda "ilk n" baştan seçilir, `SKIP LOCKED` o an kilitli olanları atlar → kümelerin BİRLEŞİMİ güncellenir, LIMIT fiilen kalkar. **ÖLÇÜLDÜ** (izole PG, tek süreç, ~3 koşumda 1): `qty=6` istenirken **20 satır** (ürünün TÜM stoğu) döndü, hepsi tek sipariş satırına atandı, motorun kendi olayı `+20 atandı (20/6)` yazdı → müşteriye bedava lisans + envanterden sessiz yanma. Aralıklı olduğu için aylarca "test gürültüsü" sanılmıştı; kontrol deneyiyle (kodum tamamen çıkarılıp aynı ortamda koşuldu) **dağıtılmış kodda** olduğu kanıtlandı. DÜZELTME: seçim `WITH picked AS MATERIALIZED` içinde BİR KEZ (MAK yolu bu deseni zaten kullanıyordu — tek-kullanımlık yol geride kalmıştı) + **fail-closed kalkan** (istenenden fazla dönerse transaction patlar). Regresyon testi `allocate-limit.test.ts`. · **Kullanıcı istekleri:** teslimat maili ÖNİZLEMESİ (gerçek gönderim yoluyla TEK kaynak — builder taşındı, kopyalanmadı; rol-farkında maskeleme; `reveal` denetim kaydı; `email_log` gövde saklamadığı için ARŞİV DEĞİL, anlık üretim ve bu ekranda yazılı) · **MAK kapasite düzeltmesi** (`use_count` ASLA düşmez, `max_uses = use_count + girilen kalan`; `depleted → available`; owner-only + sebep zorunlu + ürün `FOR SHARE`; commit sonrası tamamlama motoru tetiklenir — açılan kapasite bekleyen siparişleri HEMEN doldurur) · envanterden müşteri-lisansı değiştirme KALDIRILDI, hesap kimlik güncelleme sipariş detayına TAŞINDI. · **33 denetim bulgusu (4 paralel işçi):** maskeli fiş uyarısı hiç render edilmiyordu (bayrak zarfın üst düzeyindeydi, istemci `claim.masked` okuyordu) · `/review` "Reddet" idempotent no-op'ta bile "lisans gitmedi" diyordu · owner kendi satırındaki "2FA Kapat" ile kendi GİRİŞ kilidini yakabiliyordu (aynı kova) · destek yanıtı müşteriye ulaşmadığında "kaydedildi" deniyordu · Ctrl+K arama hatası "Sonuç yok."a dönüşüyordu · dashboard KPI'ları elde varken "—" gösteriyordu · şablon önceliği editörde **TERS** yazılıydı (gerçek: ürün+site > ürün > site > genel) · parti detayı aktif partide olmayan menü öğesini vaat ediyordu · site sihirbazı webhook'un "boş bırakırsanız" doldurulacağını söylüyordu (gerçekte HER ZAMAN eziliyor) · `canceledUnits` sunuma çıkmıyordu (aynı kutuda "2/3 bekliyor" + "Teslim edildi") · tedarikçi kusur karnesi hesaplanıp ATILIYORDU · maliyet raporunda ömrü dolmuş sermaye görünmüyordu · KVKK 7 sayaçtan 2'sini gösteriyordu · **WP:** pasif eşleme "Eşli" görünüyordu, varyasyonlu üründe kutu değiştirdiğinizi sandırıp AYRI kayıt yazıyordu (o varyasyonun siparişleri eski ürünü teslim etmeye devam ediyordu), MAK'ta "Değiştir" garantili 400 veriyordu, ham hata enum'u ("Not Found") kullanıcıya basılıyordu, katalog kırpması sessizdi · **`products/limits.ts`:** `products.controller` ⇄ `stock.service` DÖNGÜSEL import'u kırıldı (CommonJS döngüsünde sabit `undefined` → zod `.max(undefined)` sınırı SESSİZCE uygulamıyor; bugün yalnız modül yükleme sırası sayesinde patlamıyordu). **Migration YOK.** | ✅ typecheck 4/4 + **5 kapı** (use-server 26/89 · nest-wiring 42/133+13 · env 51 · workflows 2/4/47 · tx-pool 37) · birim shared 68 + api 179 + admin 159 · build 3/3 · **VPS izole PG+Redis: entegrasyon 421/421 × 3 ARDIŞIK + yarış 3/3** (düzeltme öncesi aynı ortamda ~%50 kırmızıydı; alt kümede 8/8 ek doğrulama) · PHP-lint 12/12 + eklenti davranış **108/108** · prod `deploy.sh api admin` (rollback'li, sağlık kapısı geçti) → `/v1/health` **200 v1.1.0**, admin `/pending` **200**, api **0 ERROR** · dev yığını güncel (`/health` OK) · **eklenti v1.1.4 yayınlandı** (201, 135.714 bayt; public `updates/plugin/info` 1.1.4, `download/1.1.4` 200 birebir boyut) |
| 2026-08-16 | 50b0f96 | api + admin + eklenti v1.1.3 | **Kalan eksikler turu — 5 lensli denetim (belge↔gerçek · müşteri deneyimi · docstring iddiaları · admin metinleri · ölü-uç/kapalı-döngü taraması) + 5 ayrık-dosya işçi.** **[YÜKSEK/güvenlik] AAD ad-alanı oracle'ı HÂLÂ AÇIKTI — 3. turdaki KENDİ düzeltmem eksikmiş:** `connectTokenAad` eklenmiş ama `onboarding.claim`'de **KOŞULSUZ** legacy geri düşüş bırakılmıştı → saldırgan `sites.hmac_secret_enc` blob'unu kendi eklediği bir connect-token satırına kopyalar (`site_id` düz uuid, FK YOK), yeni AAD patlar, `catch` eski AAD'yi dener — o da blob'un GERÇEKTEN şifrelendiği AAD olduğu için **ÇÖZÜLÜR** ve kimlik istemeyen `POST /v1/connect/claim` sitenin **DÜZ METİN** `hmac_secret`'ini döndürür (sonrasında düz metin lisans döndüren `GET /orders/:id/deliveries` dahil her site-facing uç imzalanabilir — MASTER_KEY hiç ele geçirilmeden). `crypto.service` docstring'im "sınıf kapanır" diyordu; kapanmamıştı. "Kodlar 15 dk yaşar" gerekçesi de saldırganı BAĞLAMIYORDU (`expires_at`'i o yazıyor → dal asla ölmüyordu). Geri düşüş KALDIRILDI (meşru kodların ömrü 15 dk, token-AAD'ye geçiş saatler önce dağıtıldı → geriye dönük etki YOK); o dal artık 400 + `logger.error` ile GÖRÜNÜR saldırı sinyali. **[YÜKSEK/müşteri] Destek yazışması TAMAMEN KAPALI DÖNGÜYDÜ:** API'de iki yönlü yazışma kurulu (`GET/POST /v1/replacements/:id/messages`, site-scoped, iç notlar süzülü, hız sınırlı) ama eklenti bu uçları **HİÇ** çağırmıyor ve `create()` yanıtındaki `{id}`'yi de okumuyordu → operatör "Ek bilgi iste" diyor, müşteriye "bilgi lazım" maili gidiyor, müşterinin **CEVAP VERECEK HİÇBİR YOLU YOK**; tek çıkış yeni talep açmak (24s/5 bütçesini yer, eski talep sonsuza dek `info_requested` kalır, `unansweredByAdmin` hiç `true` olamaz). Eklentiye talep referansı (`_wpteslimat_replacement_ids` sipariş meta) + yazışma bloğu + cevap formu eklendi. **GÜVENLİK:** panel uçları SİTE bazında yetkilendiriyor, MÜŞTERİ bazında değil → eklentide sipariş meta'sına üyelik kontrolü (`owns_request()`; yabancı id 403 ve panele HİÇ gitmez). Sistematik tarandı: 14 site-facing uçtan kapalı döngü olan TEK uç buydu. **[YÜKSEK/operatör] Sandbox metni YALAN söylüyordu:** "siparişler gerçek teslimat üretmez" — `sandbox` `apps/api/src` içinde YALNIZ mail yolunda okunuyor (`orders/` dizininde SIFIR eşleşme) → gerçek stok tüketilir, gerçek lisans atanır, müşteri çözülmüş anahtarı görür; yalnız mail operatöre döner. Aynı formun 15 satır altında DOĞRUSU yazılıydı. Metin gerçeğe uyduruldu + uyarı kutusu. **[ORTA] Aynı ürün için iki ekran FARKLI tükenme tahmini veriyordu:** `products.detailVelocity` elle yazılmış statü listesi kullanıyor ve `ol.canceled` filtresi TAŞIMIYORDU (`reports.velocity` + `reorder` taşıyordu) → tam iadeden sonra hayatta kalan `expired` atama ürün detayında satış sayılıyordu; `STANDING_STATUSES` + `canceled=false` ile hizalandı. **[~40 METİN↔KOD SAPMASI]** şablon önceliği TERS yazılıydı (gerçek: ürün+site > ürün > site > genel — site'ye özel şablon yazan operatör sessizce eziliyordu) · `bundleQty` açıklaması stok yakacak biçimde yanlıştı (adetle ÇARPILIYOR: 3 × 2 adet = 6 lisans) · `/audit` "yönetici girişleri" filtresi hiç yazılmayan bir enum değerine bağlıydı (daima boş; girişler `security_events`'te) · "gönderen e-posta" alanı ÖLÜ (`from` her zaman `MAIL_FROM`) · ön sipariş "yayın tarihinde teslim edilir" diyordu ama o tarihte hiçbir süpürme tetiklenmiyor · `/releases` sürüm kapısı YOKTU (düşük sürüm 201 döner, panel "siteler güncelleyebilir" der, hiçbir site ASLA almaz — sitelere sunulan max semver) + "En yeni" rozeti `created_at`'ten türüyordu · `/deployments` "30 dk sonra otomatik kapanır" derken temizlik yalnız `request()` içinde ve banner çıkınca formlar kapalı (kilit kendi kendine AÇILAMAZ) · rehberde "denetim kaydı silinmez" (auto-reveal 90 günde budanıyor) ve "her görüntüleme audit'e düşer" (yalnız owner düz metin) · Ctrl+K sessizce ≥3 RAKAM istiyor · hesap ürününde anahtar araması çalışmaz · sert kotanın ödenmiş siparişi 429 ile REDDETTİĞİ hiçbir kullanıcı metninde yoktu · `GELISTIRME.md` taze klonda çalışmıyordu (portları açan `docker-compose.override.yml` `.gitignore`'da ve örneği YOKTU; `3005` hiçbir compose'da geçmiyordu) → **`docker-compose.override.yml.example`** + `wp-dev.sh` sağlık beklemesi artık `exit 1` · `RUNBOOK-RELEASE`'te elle rollback komutu (`git checkout <sha>`) geri almayı İPTAL ediyordu · `RUNBOOK-DR`'de iki dış-kopya alarmı eksikti. **[KOD]** `allocatableCountForLine` ön-sipariş kapısını saymıyordu → değişimde SONSUZ "tekrar deneyin" döngüsü · admin kimlik uzunluğu OLUŞTURMA yolunda sınırsızdı (kalıcı giriş yapamayan hesap üretilebiliyordu; seed yolu controller Zod'unu hiç geçmiyor) · `ACCOUNT_FAIL_KEY` inline kopyaları (ad alanı değişirse TOTP ile parola AYRI kovaya düşer = taze deneme bütçesi) · `notExpiredCond` elle kopyası · geri çekme sonuç bandı askıdakileri saymıyordu (yeşil "başarılı" derken iş listesi doluydu) · "hiç bağlanmadı" rozeti `last_seen_at`'e bakmıyordu · `revoke_failed` + `notice_failed` olay tipleri sözlükte yoktu (operatör çıplak snake_case görüyordu — üstelik ilki sistemin EN ALARM VERİCİ olayı). **[BAĞIMLILIK]** vitest 2→3 + `vite`/`esbuild` override → `pnpm audit` **TÜM AĞAÇ** için temiz (6 açık kapandı); ertelenmişti ("400+ testi riske atar") ama paket artık aynı DB'de tekrar koşabildiği için risk ÖLÇÜLDÜ. **[TEST]** sabit payload'lar etiketlendi (MASTER_KEY sabitlenirse `payload_hash` çakışıp sessiz atlama riski) + "sessiz atlama yasağı" kontrolü + dosya sırası **ALFABETİK** sabitlendi (eskiden dosya BOYUTUNA bağlıydı → bir satır eklemek sırayı sessizce değiştiriyordu; koşarak DOĞRULANDI). **[OPS, kod değil]** `ADMIN_TOKEN` **rotasyonu yapıldı** (değer hiçbir yere yazdırılmadan): yeni token 200, **eski token 401**, `/health` 200, admin render ediyor, deploy+backup runner'ları 201 almaya devam ediyor (75 sn ölçüldü: yeni 401 yok — log'daki eskiler geçmişten). **Migration YOK** (0000-0045 sabit) | ✅ typecheck 4/4 + **5 kapı** (use-server 26/88 · nest-wiring 42/130+13 · env 51 · workflows 2/4/47 · tx-pool 36) · birim shared **64** + api **174** + admin **154** · build 3/3 · **VPS taze test DB: entegrasyon 410/410 + yarış 3/3** · PHP-lint **13/13** · `pnpm audit` **temiz (tüm ağaç)** · **sıralayıcı kanıtı:** dosyalar alfabetik koştu (`account-…`, `admin-revoke-…`, `admin-totp`, `admin-users.auth`, `anonymize`, `audit-list`) · prod `deploy.sh api admin` → `/v1/health` **200 v1.1.0**, admin `/pending` 200 (hata sınırına düşmüyor), api **0 ERROR** · dev yığını güncel (`/health` 200) · **eklenti v1.1.3 yayınlandı** (201, **130.557 bayt**; public `plugin/info` 1.1.3, `download/1.1.3` **200** ve boyut BİREBİR) |
| 2026-08-16 | f3c73a2 | api + admin | **Kararlılık turu — 4-lensli denetim (havuz/sızıntı · test idempotanlığı · çökme/hata sınırları · eşzamanlılık) + 4 ayrık-dosya işçi.** **[YÜKSEK] Havuz kilitlenmesi, bu sınıfın 3. tekrarı:** `createOrder` tx'i içinden `loadOrderResult` KÖK havuzu kullanıyordu (postgres.js'te tx bir bağlantı rezerve eder; gövdeden kök havuza sorgu İKİNCİ bağlantı ister → havuz dolunca dairesel bekleme, `/v1/health` dahil her şey cevapsız — bu kod tabanında k6 ile İKİ kez ÖLÇÜLDÜ). Aynı dosyanın diğer ikiz yolu bunu bilinçle *"(tx dışı)"* diye işaretlemiş; sonradan eklenen F2 dalı kuralı ihlal etmişti. → `exec` parametresi + `tx` geçişi. **YENİ KAPI `scripts/check-tx-pool.js`** (TS AST, `pnpm typecheck` + CI): tx gövdesinde `this.db` VE "executor sunan metodu tx'siz çağırma" desenlerini yakalar. **İlk sürümü kontrol denemesinde YAKALAMADI** (yalnız gövdeyi tarıyor, `exec = this.db` varsayılan parametresini görmüyordu) → kural düzeltildi, kırmızı olduğu GÖRÜLDÜKTEN sonra bağlandı. **[YÜKSEK] Geri çekilmiş partiye satılabilir anahtar eklenebiliyordu:** `resolveBatchForImport` parti satırını KİLİTSİZ okuyordu ve docstring *"TOCTOU ücretsiz kapanır"* diyordu — READ COMMITTED altında aynı tx'te olmak YETMEZ: eşzamanlı `recallBatch` (FOR UPDATE alır, beklemez) süpürmesini import'un commit EDİLMEMİŞ satırlarını göremeden tamamlıyor, import commit edince geri çekilmiş partinin altında TAZE `available` anahtarlar kalıyordu (§2). → `FOR SHARE OF batches`. **[YÜKSEK] Ürün sözleşmesi değişimi MAK kapasitesini sessizce imha ediyordu:** `values` dizisi tx'ten ÖNCE, kilitsiz okunan ürüne bağlıydı; `products.update` guard'ı o sırada `license_items` sayımını 0 görüp `multi→single` yapabiliyordu → anahtar başına 499 aktivasyon hiçbir hata üretmeden yok oluyordu (aynı pencere `kind`/`payloadSchema` üzerinden `payload_hash`i saptırıp dedupe'u da kaçırıyordu). → tx içinde `FOR SHARE` ile yeniden doğrulama + 409. **[ORTA] `replaceAssignmentLocked`** durum guard'ı kilitsiz okuyordu → eşzamanlı "Askıya al" sonrası `revokeAssignment` yalnız `revoked`'da `already` döndüğü için ASKIDAKİ atama taze anahtarla değiştirilebiliyordu → `FOR UPDATE OF assignments` (kardeş `replacements.approveTx` ile parite). **[ORTA] `rejectHeld` ABBA:** kilit sırası sözleşmenin tersineydi (`orders`→`order_lines`→`assignments`; sözleşme advisory→assignments→order_lines→orders) ve `revokeAssignment` advisory almadığı için iki yol serileşmiyordu → 40P01/opak 500. Sıra sözleşmeye çekildi (`orders FOR UPDATE` kaldırıldı — advisory zaten serileştiriyor; `revokeOrderForSite`'ta uygulanan çözümün aynısı). **[ORTA] `deploy.sh` admin sağlık probu HİÇBİR bileşeni render etmiyordu:** varsayılan kök `/`, middleware render'DAN ÖNCE 307 veriyor, prob `-L` taşımıyor ve [200,500) kabul ediyordu → panel tamamen kırık dağıtılsa bile "başarılı" + otomatik rollback HİÇ tetiklenmiyordu. Prob `/pending`'e alındı, `-L` eklendi, gövdede `error.tsx` imzası aranıyor (`smoke-routes.sh` deseni) + başarısızlık SEBEBİ rollback mesajına basılıyor. **[ORTA] Redis boot'ta erişilemezse API HİÇ ayağa kalkmıyordu** (`upsertJobScheduler` try/catch dışında, 8 servis `onModuleInit`'te çağırıyor, `app.listen()` içinde → port hiç açılmaz) — oysa runtime bilinçli olarak Redis'siz çalışacak şekilde tasarlanmıştı (nonce fail-closed-fast, rate-limit fail-open, health `degraded`); Redis restart'ına denk gelen bir dağıtım İYİ bir sürümü rollback ettirirdi. → boot devam eder, hata loglanır (upsert başarısızsa yetim temizliği KOŞMAZ). **[ORTA]** `main.ts`'e `unhandledRejection`/`uncaughtException` kancaları (@nestjs/bullmq `@OnWorkerEvent` handler'ını `.catch()` sarmadan bağlıyor → async bir handler'ın reddi Node 22'de süreci öldürür; bugün latent ama tek noktaya bağlı) + Redis `.on('error')` (ioredis `silentEmit` ham `console.error`'a yazıyordu → pino/Sentry görmüyordu, Redis kimlik/TLS arızası pratikte GÖRÜNMEZDİ). `requestTimeout` yorumu düzeltildi — ÖLÇÜLDÜ: handler süresini sınırlamıyor (`1000` + 3 sn handler → `200, 3027 ms`). **[ORTA] `findForAuth`'ta korumasız `decrypt`** → MASTER_KEY sapmasında mağazanın HER isteği 401 yerine 500; eklenti 401'i "yapılandırma hatası" sayıp durur, 500'ü geçici sanıp saatlerce yeniden dener → `null` + `logger.error`. **[ORTA] Admin→API 254 çağrının hiçbirinde zaman aşımı yoktu** (undici varsayılanı 300 sn; API "asılı" ise her sekme dakikalarca boş bekler ve `/deployments` de aynı katmandan geçtiği için müdahale edilemez — ters kanıt aynı repoda: `lib/auth.ts` 1,5 sn kullanıyor) → `fetchWithTimeout` (okuma 8 sn / yazma 20 sn) → `ApiError(504)`. **[YAPISAL] `PgExceptionFilter`** (`23505→409` · `23503→404/409` · `22003/22001→400` · `22P02→400` · `57014→503+Retry-After` · `40P01/40001→409`; `SentryExceptionFilter`'dan KALITIM — iki catch-all'da Nest sırası ters çevirdiği için filtre sessizce hiç koşmayabilirdi). **En sinsi kapanan açık:** `validityDays` üst sınırsızdı → `5e8` int4'e sığıp KAYIT BAŞARILI oluyor, sonra o ürüne gelen HER sipariş `RangeError: Invalid time value` ile 500 veriyordu (arıza yapılandırmadan GÜNLER sonra, sipariş push'unda). Sayısal alanlara gerekçeli üst sınırlar, SKU çakışması 409, PO/şablon FK→404, 8 uçta `ParseUUIDPipe`, `purchaseOrders.update()` tx+`FOR UPDATE`. **[TEST] Entegrasyon paketi aynı DB'de tekrar koşamıyordu** (ÖLÇÜLDÜ: 1→3→8 hata; dosya tek başına 9/9; taze DB'de temiz) — kalıntı kod regresyonu gibi görünüyordu. Temizlik kapsamını genişletmek yerine (bu oyun DÖRT kez oynandı, her seferinde sonraki tablo unutuldu) koşu BAŞINDA `globalSetup` ile tam sıfırlama + **üretim DB'sini koruyan emniyet kilidi** (test-olmayan ad → fail-closed; `lisanspanel`/`lisansdev` REDDEDİLİR). **[SESSİZ YUTMA ×12]** iade sırasında kaçak atama geri alınamazsa (müşteride CANLI lisans kalır, panel "Geri alındı" der) log + sipariş zaman çizelgesi · reveal audit yazılamazsa düz metin gösterilip ize geçmediği loglanır · bonus teslimat maili kuyruğa girmezse müşteri lisansı almamış olabilir · `supply-ops` altyapı arızasını "stok yok" saymaz (ayrı `failed` sayacı + admin bandı + destructive ton) · kota/hold güvenlik olayları · `email_log` `queued`da kalması (mükerrer lisans maili) · AAD legacy geri düşüşü · arama kripto hatası. **[TIE-BREAK]** aynı tx'te yazılan satırların `created_at` damgaları BİREBİR eşit → LIMIT'li her `ORDER BY`a benzersiz son anahtar: teslimat FIFO penceresi (EN CİDDİSİ), pending-lines tanı↔çöz, soyağacı, revoke sebebi, arama, `email_log`, `/ops` dead-letter, katalog. **[SAKLAMA] `plugin_releases` sınırsız büyüyordu:** 18 yayının zip gövdesi **1947 kB**, gecelik yedek dosyası **1,1 MB** → yedeğin NEREDEYSE TAMAMI tarihî paketler (dış kopya hâlâ kurulu değilken). Son N sürümün gövdesi saklanır, eskiler ARŞİVLENİR (satır/sürüm geçmişi KALIR; **en yüksek semver HER ZAMAN korunur** — müşteri siteleri tam o paketi indirir, sırasız yayında tarih tek başına yanıltırdı); arşivlenmiş sürüm indirmede **410** (404 "bu yayın hiç olmadı" dedirtiyordu). Varsayılan 20 = bugün NO-OP. **[KENDİ REGRESYONUM]** saklama özet satırının üç adımında `${...}` interpolasyonları kaybolmuştu (`dec4c91`) → operatöre BOŞ değer basıyordu; düzeltildi + raporun HER sayısının satırda geçtiğini doğrulayan test. **Migration YOK** (0000-0045 sabit) | ✅ typecheck 4/4 + **BEŞ** kapı (use-server 26/88 · nest-wiring 42/130+13 · env **51** · workflows 2/4/**47** · **tx-pool 36 tx gövdesi**) · birim shared 64 + api **165** + admin **150** · build 3/3 · **VPS taze test DB: entegrasyon 410/410 × ÜÇ ARDIŞIK KOŞU (AYNI DB) + yarış 3/3** (idempotanlık düzeltmesinin kanıtı — eskiden burada 1→3→8 bozulma vardı) · PHP-lint 13/13 · prod `deploy.sh api admin` **yeni sağlık probuyla** geçti → `/v1/health` **200 v1.1.0** (db+redis), admin `/pending` 200 ve hata sınırına DÜŞMÜYOR, api **0 ERROR**, iki konteyner healthy · dev yığını güncel, **37 rota 200** · **canlı 4xx doğrulaması:** mükerrer SKU → **409** (anlamlı mesaj), `validityDays=5e8` → **400** (girişte), bozuk uuid → **400** — üçü de eskiden 500'dü |
| 2026-08-16 | 94cf3f7 | api + admin + eklenti v1.1.2 | **3. tur — derin denetlenmemiş dört alan (kripto/HMAC · stok+tedarik · şema · WP sipariş senkronu).** **[ORTA/güvenlik] AAD ad alanı çakışması → PUBLIC uç çözme oracle'ı:** `site_secret:<siteId>` etiketi ÜÇ şifreli kolonda paylaşılıyordu; AAD kaydın id'sini bağlıyor ama KOLONU bağlamıyordu → DB'ye YAZMA erişimi olan ama MASTER_KEY'i OLMAYAN biri, sitenin şifreli HMAC sırrını kendi eklediği bir connect-token satırına kopyalayıp kimliksiz `POST /v1/connect/claim` ile paneli o blob'u çözüp **DÜZ METİN döndürmeye** ikna edebiliyordu (sonrasında reveal dahil her site-facing ucu imzalar). Kodun kendi güvencesi ("ciphertext satır-taşıma imkânsız") bu sınıfı kapsamıyordu → `connectTokenAad(tokenId)` + eski satırlar için tek seferlik geri düşüş (kod ömrü 15 dk) + **taşımanın çözülemediği birim testi**. Ayrıca hiç claim edilmemiş kodlar retention penceresi boyunca O AN GEÇERLİ creds'i tutuyordu (NULL'lanır) ve auth-fail mesaj ayrımı api_key geçerlilik oracle'ıydı (tek mesaj; ayrım `logger.debug`'da). **[YÜKSEK] Stok girişi ~5.957 satırda opak 500:** sistemin EN BÜYÜK toplu INSERT'i chunk'sızdı (11 bind param/satır × int16 65534) ama DTO 10.000 satıra İZİN VERİYOR ve gövde sınırına sığıyor; üstelik onay modalini besleyen KURU ÇALIŞTIRMA ayrı/ucuz yol kullandığı için TEMİZ geçip "8.000 kayıt girilecek" diyor, gerçek gönderim `MAX_PARAMETERS_EXCEEDED` ile düşüyordu (veri güvenli — tam rollback — ama giriş HİÇ yapılamıyor, sebebi panelden anlaşılmıyor). Aynı tuzak kod tabanında üç yerde çoktan çözülmüştü; en büyüğü atlanmıştı. Kardeşi (geri çekmede zayi kaydı, >10.922 kalem) de chunk'landı. **[ORTA] Parti sayacı ≠ geri çekmenin VOID ETTİĞİ küme** (sayaç `available`, eylem `available+depleted`) → kapasitesi tükenmiş MAK anahtarları taşıyan partide ekran "Stokta 0" derken onay modali "0 anahtar geçersiz kılınacak" diyor, recall o anahtarları void edip Kusurlu Stok havuzuna düşürüyordu; operatör GERİ ALINAMAZ kararını yanlış sayıyla veriyordu ("satılmış 6 birim" hatasının aynı sınıfı). **[ORTA/§2 WP] Sipariş KALEMİNİN SİLİNMESİ panele hiç ulaşmıyordu** → satır `fulfilled`, atamalar `active` kalıyor, müşteri artık satın almadığı lisansları kullanmaya devam ediyor, stok kalıcı tüketilmiş sayılıyordu; adet 3→1 hâli ZATEN doğruydu (`revokeExcess`), eksik olan 3→0'dı → **opt-in `fullSync`** (bayrak YOKSA eski güvenli davranış; "gelmeyen satır = silinmiş" varsayımı KISMİ push'ta canlı anahtarları topluca yakardı) + iade semantiği (MAK kapasitesi havuza DÖNMEZ) + görünür `order_edited` olayı + iki yönlü regresyon testi. **[ORTA WP] Tükenmiş retry zincirinden sonra aynı işin TÜM başarısızlıkları KALICI SESSİZ** (haftalar sonraki ikinci kısmi iade iletilemezse hiçbir iz kalmıyordu) → yeni iş tetiğinde sayaç+hata izi sıfırlanır; tüm kalemleri silinen siparişte sessiz `return` yerine görünür not. **[DÜŞÜK ×6]** `correct`/`recall` + `licenseItemIds` sessiz yutma (ilk id hariç hepsi) → 400 · `recall`/`bulk-replace` `ParseUUIDPipe` · stok girişi `payload` üst sınırı (kardeş yol 4.000 uyguluyordu) · `resolveBundleQty` tie-break (kardeş üç sorguda vardı) · vd. **ŞEMA DENETİMİ (ilk kez) büyük ölçüde TEMİZ:** migration `when` sırası monoton, 31 tablo + ~62 indeks migration'larda, 0045 snapshot güncel, tüm zaman kolonları `timestamptz`, dokuz kısmi indeksin yüklemi sorgularla birebir. **Migration YOK** (0000-0045 sabit) | ✅ typecheck 4/4 + dört kapı (use-server 26/88 · nest-wiring 42/**130**+13 kuyruk · env **50** · workflows 2/4/46) · birim shared 64 + api **153** + admin 147 · build 3/3 · **TAZE VPS izole test DB: entegrasyon 406/406 + yarış 3/3** · PHP-lint 13/13 + eklenti davranış 108/108 · prod `deploy.sh` sağlık kapısı geçti → `/v1/health` **200 v1.1.0**, api **0 ERROR**, dev yığını güncel · eklenti **v1.1.2** yayınlandı (201, 120.350 bayt; public `plugin/info` 1.1.2, `download/1.1.2` **200** ve boyut birebir). **NOT (harness):** entegrasyon paketi AYNI DB'de tekrar koşmaya karşı idempotent DEĞİL — üst üste koşumlarda hata 1→3→8'e çıktı, dosya tek başına 9/9, TAZE DB'de 406/406; koşumlar arası taze DB gerekiyor (ölçülerek ayırt edildi) |
| 2026-08-15 | 53a5584 | api + admin + eklenti v1.1.1 | **6-lensli proje denetimi — en ağır bulgu: CI 19 gündür hiç koşmuyormuş.** `.github/workflows/ci.yml` GEÇERLİ YAML DEĞİLDİ (`- name: 'use server' export denetimi` — tek tırnakla BAŞLAYAN skalerden sonra düz metin gelemez, `bad indentation of a mapping entry (38:28)`); GitHub Actions dosyayı reddeder, yani **iş akışının TAMAMI** (use-server, Nest DI, env passthrough, typecheck, birim testleri, WP php-lint, migration drift, `bash -n`) 2026-07-28 / `ee59e14`'ten beri hiç çalışmıyordu — üstelik kırılma, tam da "sessiz kırılmayı önleyecek" kapıyı CI'a ekleyen commit'te girmişti. `js-yaml` ile ölçülerek kanıtlandı. Yeni kapı `scripts/check-workflows.js` **YEREL `pnpm typecheck`** zincirinde (dosya ayrıştırılamıyorsa kendi adımı kendini denetleyemez); kontrol denemesiyle kırmızı görüldü. **[YÜKSEK]** Ürün formundaki `guideId` sunucu aksiyonunda HİÇ okunmuyordu → kurulum rehberi panelden ETKİNLEŞTİRİLEMİYORDU (operatör seçer, yeşil sonuç alır, `products.guide_id` NULL kalır, rehber müşteri sayfasında/mailde/`.txt`'de hiç görünmez); kök sebep test edilebilirlikti (`'use server'` dosyasından yardımcı export edilemez → testi olamıyordu) → `lib/product-form-body.ts` + 12 test, düzeltme geri alınınca 5'i kırmızı. **[YÜKSEK/güvenlik]** `POST /v1/admin/sites` OwnerGuard taşımıyordu ve taze `apiKey`+`hmacSecret` döndürüyor → owner-olmayan admin kendine site açıp gerçek lisansı DÜZ METİN okuyabiliyordu; guard eklendi + hiçbir sayfada render edilmeyen ama hâlâ çağrılabilir olan `createSiteAction` kaldırıldı. **[ORTA/§2]** `revokeExcess` MAK kapasitesini havuza geri veriyordu, `syncRefunds` vermiyordu (aynı fiziksel olay, zıt semantik; iki test çelişkiyi ayrı ayrı kilitliyordu) → mağaza re-push'u NET adet taşıdığı için bir iade bu yoldan uzlaşıp HARCANMIŞ aktivasyonları havuza döndürebiliyordu (sessiz aşırı-satış); geri alınacak atama seçimi de deterministik yapıldı (önce `suspended`, sonra en yeni, sonra `id`). **[perf]** atamada birim başına ayrı INSERT → tek çok-satırlı INSERT; `releaseAllocations` → tek `UPDATE … FROM (VALUES …)`; düşük-stok süpürmesi kısmi indeksi kullanamıyordu; fiş detayı → LATERAL; ürün detayı parti/PO tavanı + görünür kırpma uyarısı. **[gözlem]** teslimat maili arızasının alarmı YOKTU (`failedEmails`); dış kopya alarmında şiddet yükselmesi kendi dedupe'una takılıyordu. **[kapılar]** env passthrough `getOrThrow`+sabit değişken (44→49), nest-wiring `@InjectQueue` (+13), deploy/backup runner sessiz claim hatası, exec-bit kapısı — dördü de kontrol denemesiyle kırmızı görüldü. **[UI/WP]** owner-only aksiyonlar gate'lendi, şablon araması Türkçe İ'de sessiz boş dönüyordu, Ctrl+K `unmapped` ham İngilizceydi, ölü `revealAction` (çağrılırsa sahte "lisans görüntülendi" audit'i yazıyordu), 9 rotaya `loading/error`; eklenti v1.1.1'de katalog kırpma birimi uyuşmazlığı (tek emoji'li ürün adı TÜM snapshot'ı 400'letiyordu). **Migration EKLENMEDİ** (0000-0045 sabit) | ✅ typecheck 4/4 + **dört** kapı (use-server 26/88 · nest-wiring 42+69+**13 kuyruk** · env **49** · **workflows 2/4/46**) · birim shared 57 + api 152 + admin 147 (+12) · build 3/3 · `pnpm audit --prod` temiz · şema sapması yok · PHP-lint 13/13 + eklenti davranış 108/108 · **VPS izole test DB: entegrasyon 402/402 + yarış 3/3** (çifte satış 0 — toplu INSERT ve küme-tabanlı geri alma SONRASI) · prod `deploy.sh api admin` sağlık kapısı geçti → `/v1/health` **200 v1.1.0**, migration tracking **46**, api **0 ERROR** · eklenti v1.1.1 yayınlandı (HTTP 201, 119.490 bayt; public `plugin/info` 1.1.1 sunuyor, `download/1.1.1` **200** ve boyut birebir) |
| 2026-08-15 | 226aed0 | api + admin | **Dış kopya (offsite) yokluğu artık ALARM üretiyor** (migration YOK). Boşluk kendi alarm tasarımımızdaydı: yedek TAZELİĞİ ve TATBİKAT için alarm vardı, dış kopya için YOKTU → yedekler düzenli alınır, tatbikat geçer, iki alarm da susar ama her dump YALNIZ yedeklemenin sebebi olan makinede durur; sunucu kaybedilirse veri de MASTER_KEY de gider ve "yedeğimiz var" sanısı gerçek kurtarma imkânı OLMADAN sürer (durum `/deployments` ekranında yalnız rozetti — rozet yalnız BAKANA yarar). **CANLI ÖLÇÜM:** prod'da `BACKUP_OFFSITE_CMD` tanımsız, dump yalnız sunucuda, iki mevcut alarm da haklı olarak sessiz. İki durum AYRI şiddet: `skipped` (kanca kurulu değil) → **warning** + 7 gün dedupe (sürekli durum; alarm yorgunluğu yaratmamalı), `failed` (kanca kurulu ama çalışmıyor) → **critical** + 24 saat dedupe çünkü operatör dış kopyanın ALINDIĞINI sanıyor ve yanlış güven hiç güvenmemekten tehlikelidir. Yalnız BAŞARILI son yedeğe bakılır (yedek zaten alınamıyorsa asıl sorun `backupStale`; aynı arıza iki başlıkla bildirilmez). Yeni tip etiket sözlüğüne de eklendi — eklenmeseydi operatöre ham `backup_offsite` görünürdü (geçen turda düzeltilen kusurun aynısı). UX: rehber metni ipucu "Sağda görünür" diyordu ama önizleme dar ekranda ALTTA durur; sınırın gerekçesi de sayacın altında ikinci paragraf olarak kalıyordu (`Field` yardım metnini çocuklardan SONRA basar) → ipucuna taşındı | ✅ entegrasyon **401/401** (+1: warning / critical / yanlış-pozitif üretmez 3 şıklı test; **kontrol denemesiyle kırmızı olduğu doğrulandı**) · typecheck 4/4 + 3 kapı temiz · `/v1/health` 200 v1.1.0 · **alarm canlıda tetiklendi ve düştü** (`created:1`, warning, doğru mesaj) |
| 2026-08-15 | 64f68dc | api | **Rehber gövdesi teslimat sorgusunda tekrarlanmıyor + rehber yolu testleri** (migration YOK). İlk sürüm gövdeyi atama satırlarının LEFT JOIN'ine koyuyordu → sonuç SATIR BAŞINA tekrarlanıyordu: 50 anahtarlı siparişte 4.000 karakterlik metin 50 kez (~200 KB) PG'den uygulamaya taşınıyordu, üstelik bu uç yalnız sayfa render'ında değil mağazanın CANLI YOKLAMASINDA da (8-60 sn'de bir) çağrılıyor. Gövde artık aynı `Promise.all` içindeki BAĞIMSIZ `selectDistinct`'ten tekil gelir (ek round-trip YOK); `selectDistinct` sıra garanti etmediği için başlığa göre sıralanır. Rehber yolu tamamen testsizdi → 5 davranış kilitlendi (rehberli ürün · rehbersiz ürün · aynı rehbere bağlı iki kalem TEK KEZ · iptal satırın rehberi girmez · rehber silinince ürün ayakta kalır) | ✅ entegrasyon **400/400** (+5) + yarış 3/3 · **kontrol denemesi:** iptal-satır filtresi kaldırılınca tam o test kırmızı · `/v1/health` 200 v1.1.0 |
| 2026-08-15 | 3a88390 | api + admin + eklenti **v1.1.0** | **Kurulum/etkinleştirme rehberleri + teslimat arayüzü** (migration **0045**). Anahtarı teslim etmek yetmiyordu: "Office 365'e nasıl giriş yaparım", "Windows anahtarını nereye girerim" destek yükünün büyük kısmıydı. `product_guides` + `products.guide_id` (ON DELETE SET NULL); metin ürüne GÖMÜLMEDİ çünkü aynı anlatı onlarca SKU'da ortaktır ve bir adım değişince tek yerden düzelmelidir. **ÜÇ YÜZEY, TEK RENDER** (`packages/shared/domain/guide.ts`): mağaza sayfası (HTML) · e-posta (düz metin) · indirilen `.txt`; eklenti PHP'de İKİNCİ ayrıştırıcı taşımaz, paneldeki HTML'i `wp_kses` allow-list'iyle yalnız süzer. Biçimleme markdown'ın küçük+GÜVENLİ alt kümesi (ham girdi önce kaçırılır → müşteri tarayıcısında script çalışmaz). **Karakter sınırı 4.000, e-posta istemcisinden GERİYE hesaplandı:** Gmail 102 KB'ı aşan maili kırpar ve kırpılan yer genelde SONDUR = rehberin yeri; maile en fazla 3 rehber / 12.000 karakter, sığmayan SESSİZCE düşmez. Şablonda `{{guides}}` yoksa blok mailin SONUNA eklenir — mevcut şablonların hiçbirinde token yok, yalnız token'a güvenilseydi rehber sessizce kaybolurdu (**kontrol denemesiyle doğrulandı**). **Render sırasında bulunan kusur:** adımlar arasına boş satır koymak her adımı ayrı liste yapıyor ve HTML numarayı 1'den YENİDEN başlatıyordu (müşteri "1. 1. 1." görürdü) — üstelik aynı metnin DÜZ METİN sürümü doğruydu; `start` özniteliğiyle düzeltildi + kses listesine izin eklendi. Panel `/guides`: canlı önizleme (mağaza ↔ e-posta sekmeli, müşterinin göreceği HTML'in TA KENDİSİ), karakter sayacı, hazır taslaklar (Office 365 / Office 2021-2019 / Windows 10-11 / genel hesap); hiçbir ürüne bağlı olmayan rehber müşteriye ULAŞMAZ → "Ürüne bağlı değil" uyarısı. Eklenti teslimat görünümü KART yapısına geçti (uzun anahtar kırpılmıyor, renkler tema-nötr). Yol boyunca: şablon editörü "desteklenen değişkenler" listesinin KENDİ kopyasını tutuyordu ve ayrışmıştı (`valid_until` yoktu → geçerli token'a "desteklenmiyor" uyarısı) → `@lisans/shared`'a taşındı | ✅ typecheck 4/4 + 3 kapı · birim 57+135+152 · VPS izole test DB **entegrasyon 395/395 + yarış 3/3** · PHP-lint 13/13 + eklenti davranış testleri 108/108 · `/v1/health` 200 **v1.1.0**, migration tracking 46, api 0 ERROR · **eklenti v1.1.0 yayınlandı** (HTTP 201, 115.794 bayt; public update ucu https ile 1.1.0 servis ediyor, zip 200) · **dev gerçek E2E:** 3 ürünlü sipariş #67 → mağaza sayfasında üç kart + her birinde kendi rehberi + `<ol start="2">` numaralandırma; teslimat maili iki rehberi TEKİLLEŞTİRİLMİŞ taşıyor; 31 rota 200 |
| 2026-08-15 | 5e593f6 | api + admin | **Denetim: zaman çizelgesi sırası + görünmeyen etiketler** (migration **0044**). Doğrulama temeli ÖNCE ölçüldü (typecheck 4/4, birim 148+135+35, entegrasyon **394/394** + yarış 3/3, şema sapması yok, `pnpm audit --prod` temiz, WP php-lint 13/13 + 108 davranış doğrulaması, 34 rota 200); çekirdek para yolu / 14 owner-only ucun RBAC kapsamı / düz-metin maskeleme **temiz çıktı**. **[ORTA] Sipariş zaman çizelgesi yanlış sırada gösterebiliyordu:** bir siparişin olayları TEK transaction'da yazılır ve `now()` transaction başını döndürdüğü için damgalar BİREBİR aynıdır → yalnız `created_at` ile sıralandığında "Geri alındı" satırı "Sipariş tamamlandı"nın ÜSTÜNE çıkabiliyor ve sıra her yenilemede değişebiliyordu (denetim izi ekranında nedensel sırayı yanlış anlatıyor). **ÖLÇÜLDÜ:** dev verisinde aynı damgayı paylaşan **7.200 olay grubu** (`fulfilled + line_completed + revoked` üçlüleri dahil). `fulfillment_events.seq` (0044; `license_items.seq`/0030 deseni + aynı rewrite uyarısı — uygulanırken prod 5, dev 14.418 satırdı, ölçüldü) + sıralama `created_at, seq`. **[ORTA] Kritik mail alarmı ham kod olarak görünüyordu:** `mail_config` (üretimde mail hedefi yakalayıcıya bakıyor → teslimat mailleri gerçek müşteriye ULAŞMIYOR) etiket sözlüğünde yoktu ve alarm prod'da **CANLI** (ölçüldüğünde 26 kayıt, en yenisi aynı gün); aynı sınıftan ikinci boşluk yeni `account_credentials_rotated` olayıydı. İki sözlük de API'nin ürettiği değerlerin TAMAMINA karşı yeniden denetlendi (kalanlar tam). **[DÜŞÜK]** ürün detayı stok hareketleri listesi kararsızdı (toplu düşme kalem başına satır → aynı damga → `LIMIT 50` penceresi keyfi) → `created_at DESC, id DESC`; **[DÜŞÜK]** duman testi `sites/new` + `templates/new`'i hiç taramıyordu (ikisi de sunucu action'lı, `next build` temiz geçerken çalışma anında kırılabilir). Yeni test `order-timeline-order.test.ts` **mutasyonla kanıtlandı** — ilk sürümü fix geri alındığında da geçiyordu (tie-break olmadan da Postgres küçük tabloda satırları fiziksel sırada döndürür), `seq` fiziksel sıranın tersine yazılarak ayırt edici hâle getirildi. Dağıtım sonrası: migration tracking **44→45**, `seq` bigint NOT NULL + mevcut 5 satır dolu, yeni etiketler admin imajında doğrulandı | ✅ `/v1/health` 200 v1.1.0 (db+redis), api **0 ERROR**, rollback tetiklenmedi; entegrasyon **395/395** + yarış 3/3, 36 rota duman testi temiz |
| 2026-08-15 | 578f003 | api + admin | **Denetimde ertelenen tüm maddeler kapatıldı** (migration **0043**, 3 indeks; eklenti **v1.0.7** yayınlandı). **[YENİ UÇ] Teslim edilmiş HESABIN kimlik bilgileri yerinde güncellenebiliyor** (`POST /v1/admin/license-items/:id/rotate-credentials`, **owner-only**): sağlayıcı parolayı döndürdüğünde panelde doğru araç YOKTU — kalem düzenleme teslim edilmişi (haklı olarak) 409'lar, "Değiştir" ise BAŞKA bir hesap verir ve eski hesap müşterinin elinde ÇALIŞMAYA DEVAM eder (bilgileri kopyalamıştır) + müşterinin o hesaptaki verisi kaybolur; yani anahtar ürününde doğru olan çözüm hesap ürününde YANLIŞ. Yeni akış AYNI kalem + AYNI atama + yeni bilgiler: sebep zorunlu, maskeli değer reddedilir (`looksMasked`), sipariş zaman çizelgesine görünür olay + audit, müşteriye bildirim KARARI operatörde (yanıt etkilenen `orderIds`'i döner, UI "teslimat mailini yeniden gönderin" der). **[MAK çıkmaz sokağı]** üç değişim yolu da MAK'ı reddediyordu ve **red DOĞRU** (geri alınan kapasite aynı paylaşımlı anahtara döner → yeni atama yine o kusurlu anahtarı seçer); eksik olan operatöre yapabileceğini söylemekti — sipariş detayı + destek ekranı düğmeyi GATE'SİZ sunuyor, tıklayınca 400 veriyordu (envanter tablosu doğru gate'liyordu, proje kuralı "tıklanıp hata veren düğme hiç sunulmayandan kötüdür" ihlal ediliyordu). İki ekranda da sebebiyle kapalı + gerçek reçete (mağazadan "+1 Bonus", Kusurlu Stok akışıyla tedarikçiye bildir) + rehberde 4 adımlı yordam; "İptal" onayındaki yanıltıcı *"kusurlu key"* örneği KALDIRILDI (o uç iade semantiğidir, müşterinin hakkı yanar). `MULTI_REPLACE_BLOCKED` tek kaynak `lib/labels`'e taşındı (üç ekran aynı metni okur). **[Tükenmiş kapasite görünmezdi]** `depleted` kalemler hiçbir stok ekranında listelenemiyordu: ürün detayında kova yok, envanter facet'inde seçenek yok, `detailStock().expired` alanı ÖLÜ (hiçbir kod yolu `license_items.status='expired'` yazmıyor → daima 0). Üçü de düzeltildi; MAK'ta kovaların TOPLANAMAYACAĞI (kısmen satılmış anahtar hem stokta hem müşterilerde sayılır) ekranda ve kodda not edildi. **[Maliyet raporu penceresiz tam tablo taramasıydı]** → `?from&to`, varsayılan son 12 ay, uygulanan dönem ekranda YAZILI (sessiz kırpma yok, tek tıkla "tüm zamanlar"); stok değerlemesi bilerek pencerelenmedi (dönem akışı değil ANLIK pozisyon — daraltmak "stok değerimiz düştü" yalanı üretirdi). **migration 0043:** `assignments_delivered_at_idx` (kısmi) · `purchase_orders_spent_at_idx` (İFADE indeksi — harcama teslimde gerçekleşir, `coalesce(received_at, created_at)`) · `stock_adjustments_created_idx` (kısmi, yüklem servisle BİREBİR). **[Hesap araması YALAN SÖYLÜYORDU]** `payload_suffix_hash` hesap ürününde kanonik JSON'un kuyruğunu hash'liyordu → operatörün aradığı parola/kullanıcı-adı sonu ASLA eşleşmiyor, üstelik kuyruğun 2 karakteri sabit olduğu için etkin entropi 5→3 düşüyordu; hesapta artık yazılmıyor + arama ipucu gerçeği söylüyor. **[WP v1.0.7]** sipariş ekranı özeti MAK'ta KALEM sayıyordu (3 aktivasyonluk tek anahtar "1 lisans") → operatör eksik teslimat sanıp bedava "+1 Bonus" verebiliyordu; birim de yazılıyor + `useCount/maxUses` çipi "Anahtar geneli" diye etiketlendi (o sayaç anahtarın TÜM siparişlerdeki toplamı). **[OPS — sessiz arıza]** `scripts/backup-runner.sh` git'te **100644 (çalıştırılabilir DEĞİL)** idi ve runbook onu doğrudan crontab'a koymayı söylüyor → operatör runbook'u HARFİYEN uygulasa bile yedek cron'u "Permission denied" ile sessizce hiç koşmayacaktı (üstelik arızayı raporlayacak olan da o runner'dı). Exec bit düzeltildi (`smoke-routes.sh` ile birlikte), VPS'te dakikalık + gecelik + aylık-tatbikat cron'ları kuruldu, ilk yedek alınıp panelde `success` göründüğü doğrulandı. | ✅ typecheck 4/4 (src+test) · check-use-server 25/87 · shared 35/35 · api birim **148/148** · admin birim **135/135** · build 3/3 · VPS izole test DB **entegrasyon 394/394** (383 → **+11**: 5 kimlik-rotasyonu + 6 maliyet-penceresi) **+ yarış 3/3** · PHP-lint 12/12 · dev: 0043'ün 3 indeksi canlı, **30 rota 200 (hata sınırına düşen yok)**, boot hatası 0 · **dev canlı E2E:** rotasyon ucu 201 → aynı kalem/aynı atama, yeni parola müşterinin `deliveries` yanıtında GÖRÜNÜYOR; maskeli değer **400** ile reddedildi · prod `deploy.sh api admin` (rollback'li) → `/v1/health` **200 v1.1.0**, 0043 indeksleri CANLI, api hata logu yalnız bilinen mail-relay alarmı, disk %62 · **eklenti v1.0.7 panele yayınlandı** (HTTP 201, 112.364 bayt) · yedek cron kuruldu + ilk dump (1,1 MB) panelde `success`. |
| 2026-08-15 | ad28f93 | api + admin | **Panel + eklenti tam denetimi + gerçek mağaza senaryosu** (migration **0042**, 7 indeks + 2 düşürme; eklenti **v1.0.6** yayınlandı). Beş bağımsız lens (WP eklentisi · güvenlik-2026 · performans · ürün tipi doğruluğu · test kapsamı) + 6 düzeltme işçisi + gerçek WooCommerce E2E (varyasyonlu ürün · MAK · Office 365 hesabı · karışık sipariş · kısmi teslimat/iade · otomatik tamamlama). **[YÜKSEK — YÜK TESTİNDE ORTAYA ÇIKTI, kod okunarak GÖRÜLEMEZDİ] Bağlantı havuzu kilitlenmesi:** `createOrder` transaction'ı İÇİNDEN `products.resolveMapping`/`getById` KÖK havuzu kullanıyordu; postgres.js'te `transaction()` bir bağlantı rezerve eder, kök havuzdan sorgu İKİNCİ bağlantı ister → `max:10` havuzda **10 eşzamanlı sipariş** tüm bağlantıları "idle in transaction" kilitliyor, hiçbiri serbest kalmıyor ve ancak `idle_in_transaction_session_timeout` (60sn) hepsini öldürünce çözülüyordu; o pencerede `/v1/health` bile `db:false` → **sipariş trafiği TÜM paneli deviriyordu.** ÖLÇÜLDÜ: k6 100 VU → **0 tamamlanan iterasyon** + PG logunda 60sn'de bir havuzun tamamı FATAL. Düzeltme sonrası aynı test: **7.171 istek / 354 istek-sn / 0 hata / 14.334 check geçti / 50 stoktan tam 50 sipariş (çifte satış 0) / idle-in-transaction ölümü 0.** Aynı sınıf `sites.rekey`→`getById`'de de vardı. **[YÜKSEK] connect-code yetki yükselmesi:** uç `OwnerGuard` taşımıyordu ama `rotate-secret` ile AYNI gücü veriyordu (`sites.rekey` → creds guard'sız public `/v1/connect/claim`ten teslim edilir) → owner-olmayan admin herhangi bir sitenin kimliğini alıp site-facing `reveal` imzalayarak düz metin lisans/parola okuyabiliyordu (A1/A3 kararı tamamen atlanıyordu). **[YÜKSEK] `parseAccountPayload` fallback'i `secret:false`** işaretliyordu → `kind: key→account` çevrilen üründe o ürünün TÜM anahtarları owner-olmayan admine / WP meta box'a / Ctrl+K aramasına DÜZ METİN çıkıyordu; fallback artık `secret:true` (fail-safe yön) + canlı kalem varken `kind`/`payloadSchema` değişimi 409 (alan silme→veri kaybı, secret düşürme→geriye dönük ifşa, alan ekleme→`payload_hash` sapması ve **mükerrer denetiminin kaçması**). **[YÜKSEK] Maskeli değer yazımı:** owner-olmayan admin hesap kaydını düzenlerse `••••••` GERÇEK parola olarak şifreleniyordu (sessiz lisans imhası, geri dönüş yok) → sunucuda `looksMasked()` kapısı. **[PERF] MAK atama birim başına ayrı UPDATE'ti** (qty=200 → 200 ardışık round-trip, tek tx'te kilitler tutulurken) → anahtar başına toplu (`LEAST(want, max_uses-use_count)`, `taken` CTE'de GÜNCELLEMEDEN ÖNCEKİ satırdan çünkü RETURNING NEW döner). ÖLÇÜLDÜ: süre artık **adetle ölçeklenmiyor** (50 adet 30ms, 200 adet 34ms). **migration 0042:** atama sorgusunun TAM karşılığı `license_items_alloc_idx` (sıralama anahtarları indekste olmadığı için aday satır başına heap erişimi; `expires_at IS NULL` çoğunlukta olduğundan presorted prefix tek dev gruba düşüp LIMIT devreye girmeden TÜM available satırlar sıralanıyordu) · `orders_open_status_idx` (**orders.status üzerinde HİÇ indeks yoktu** → /pending ana ekranı) · `license_items_dead_at_idx` (karantina `coalesce()` İFADE sıralaması) · `order_lines_pending_fifo_idx` · `replacement_requests_order_idx`+`_email_lower_idx` · `mappings_product_idx` (ikisi de indekssiz FK); yenilerinin ön ekini kapsayan 2 gereksiz indeks düşürüldü. **[WP v1.0.6 — E2E'de ÖLÇÜLDÜ]** Çok ürünlü siparişte müşteri sayfası DÜZ LİSTE basıyordu (ürün adı yok, MAK'ın kaç aktivasyon taşıdığı hiçbir yerde yok — 9 aktivasyon alan müşteri 2 çıplak anahtar görüyordu) oysa **aynı siparişin MAİLİ bunu DOĞRU yapıyordu** → sayfa + `.txt` artık `remoteLineId` ile gruplu (ürün adı WooCommerce'in KENDİ kaleminden) + kullanım hakkı etiketi; hesap alanları çözülemezse teslim edilmiş sipariş KALICI "Teslimat hazırlanıyor"da kilitlenmiyor; retry SONSUZ + sabit 300sn iken (401'de bile, günde ~288 not/iş) §4'teki 1dk/5dk/30dk + 3 deneme, 401/403'te HİÇ retry + tek kalıcı not. **Diğer:** mail `canceled` filtresi + `valid_until` değişkeni · `siteReveal` süre filtresi · `expiredHidden` sweep koşunca kaybolmuyor · hesap payload'ı `maskSecret`'e verilince parola kuyruğu sızıyordu → account'ta `payload:null` · `withinWarranty` lisans ömrünü saymıyordu (süresi dolmuş `keep` lisans "garanti içi" görünüp BEDAVA yenileniyordu, 12 ay tekrarlanabilir) · **stok import `.trim()`** (kalem düzenleme trimliyordu, import ETMİYORDU → sondaki boşluk `payload_hash`i saptırıp mükerrer denetimini kaçırıyor, aynı anahtar İKİNCİ kez stoğa girebiliyordu; dev'de kanıtlandı) · webhook `concurrency:5` · envanter `count(*) OVER ()` tavanlandı · retention +3 tablo · şablon test maili hız sınırı · dağıtım claim/finish + connect-code OwnerGuard · login/logout `Origin` yoksa RED · Dockerfile `\|\| pnpm install` yedeği kaldırıldı (lockfile sabitlemesini sessizce iptal ediyordu) · prod imajı budandı (**626→145 paket**) · scrypt N=2^17 (geriye dönük + rehash-on-login). **KENDİ REGRESYONUM (dev'de yakalandı, prod'a GİTMEDİ):** budanmış imaj `pino-pretty`yi kaybetti ve kod yalnız `NODE_ENV`'e bakıyordu → prod imajı NODE_ENV≠production ile koşan dev stack'inde API **BOOT ETMEDİ**; `require.resolve` denetimi + JSON'a düşüş. | ✅ typecheck 4/4 (src+test) · check-use-server 25/86 · shared 35/35 · api birim **142/142** · admin birim **135/135** · admin build ✓ · PHP-lint **12/12** · VPS izole test DB **entegrasyon 383/383** (357 → **+26**; MAK satış yolu ve karışık tipli sipariş HİÇ test edilmemişti — MAK yalnız iade/red yollarında testliydi) **+ yarış 3/3** · dev: 0042'nin 7 indeksi canlı + 2 gereksiz indeks düşmüş, api+admin healthy, boot hatası 0 · **dev canlı E2E:** varyasyonlu ürün (Retail/MAK) + Office 365 hesabı + karışık sipariş → 205ms fulfilled, MAK tek atama `units=3`, hesap `valid_until` +365g; kısmi iade (satır 3→2, `use_count` 3'te KALDI — §2 doğru); kısmi teslimat (9 MAK + 5 hesap → iki anahtara bölünmüş 7 birim, "9/14"); otomatik tamamlama; müşteri sayfası ürün adıyla gruplu + "Bu anahtar 3 kullanım/aktivasyon hakkı içerir"; trim düzeltmesi kanıtlandı (`duplicates:1`) · **k6 100 VU: 0 hata, 354 istek/sn, çifte satış 0** · prod `deploy.sh api admin` (rollback'li) → `/v1/health` **200 v1.1.0** (db+redis), 0042 indeksleri CANLI (`license_items_fefo_idx` düştü), api hata logu yalnız bilinen mail-relay alarmı, disk %61 · **eklenti v1.0.6 panele yayınlandı** (HTTP 201, 111.505 bayt). |
| 2026-08-14 | e09f992 | api + admin | **Proje geneli 6-lensli denetim — 40 doğrulanmış bulgu** (migration **0041**, additive index). Altı lens (auth/2FA · veri ifşası/RBAC · sipariş invaryantları · rapor/DB · arayüz · ops/betik/WP) çekişmeli-doğrulamalı ajanlarla tarandı, sonra beş paralel işçi + merkezî tümleştirme. **CANLIDA ÖLÇÜLEN İKİ KUSUR (statik analiz göremezdi):** (1) **Dört süpürme işi prod'da İKİ KEZ koşuyordu** — Redis'te `expiry`/`low-stock`/`reconcile`/`daily-digest` (+`security`) kuyruklarının ikişer aktif zamanlayıcısı vardı: biri kararlı kimlikli yenisi, diğeri eski `queue.add(repeat)` çağrısından kalan hash anahtarlı yetim. Redis dağıtımlar arasında kalıcı olduğu için kod düzeltildiğinde eski kayıtlar SİLİNMEMİŞTİ ("yetim schedule kalmaz" güvencesi yalnız İLERİDEKİ değişiklikler için doğruydu). **Kanıt:** `notifications` tablosunda `digest_alert` günde tam **2** satır, damgalar `08:00:00.121` ve `08:00:00.132` — iki zamanlayıcı aynı dakikada ateşliyor; Telegram açık olsaydı operatör her sabah aynı kritik alarmı iki kez alacaktı. Düzeltme `queue/sole-scheduler.upsertSoleJobScheduler`: upsert'ten sonra beklenen kimlik DIŞINDAKİ zamanlayıcıları siler (boot'ta koşar → kendi kendini onarır, ileride bir schedulerId yeniden adlandırılsa da eskisi temizlenir) + 3 entegrasyon testi. **Dağıtımda 5 yetimin silindiği loglandı ve ZSET'ler doğrulandı.** (2) **`ADMIN_TOKEN` her admin isteğinde DÜZ METİN loglanıyordu** — prod API logunda son 1 saatte **30 satır** (pino-http `req`i o isteğin her satırına bağlar; redact listesinde `x-admin-token` yoktu). Bu token `AdminGuard`ın tek kapısı ve `x-admin-role` gönderilmediğinde `OwnerGuard` da geçiriyor → log okuma yetkisi TAM panel kontrolüne yükseliyordu. **ÇEKİRDEK PARA YOLU:** `syncRefunds` `canceled_units` defterini uzlaştırmıyordu → aynı iptal iki kez sayılıyor, hedef `fulfilled`in ALTINA düşüyor ve **değişim yolu KALICI kilitleniyordu** (stok VARKEN "stok yok"); admin manuel iptali MAK kapasitesini havuza geri veriyordu (aktivasyon sağlayıcı tarafında HARCANMIŞ → sessiz aşırı-satış); `rejectHeld` kaçak atamaları ham UPDATE ile kapatıp tek-kullanımlık kalemi kalıcı `assigned` limbosunda bırakıyor ve reconcile'da KALICI kritik alarm üretiyordu; satır durumu üç yerde ham `qty` ile hesaplanıyordu (satır kalıcı `partial`); geri çekilen partideki MAK anahtarı kapasite iadesiyle havuza dönebiliyordu; toplu geçersiz kılma canlı atamalı kalemi void'liyordu (tekil yol 409 verirken). **RAPOR/DB:** "kaç birim eksik" yükleminin İKİ tanımı vardı (`qty−fulfilled` vs kanonik `qty−canceled_units`) → stok girişi onay modali, bekleyen kuyruğu ve "neden bekliyor" tanısı yanlış sayı gösteriyordu; `deliveredCogs` yalnız `active` sayarken aynı ekrandaki satış hızı `active+suspended+expired` sayıyordu (`STANDING_STATUSES` tek kaynağa taşındı); `reorder` satışsız-ürün sayacı `FILTER` içindeki `NOT EXISTS` yüzünden ürün başına SubPlan koşuyordu; `/purchase-orders` listesi SINIRSIZ + indekssiz sıralamaydı (tablo her stok girişinde büyüyor) → tavan+`truncated` + **0041**. **GÜVENLİK/GİZLİLİK:** owner-olmayan admin 2FA'yı HİÇ açamıyordu (aktör başlığı yazma yolunda gönderilmiyordu → 403; tek noktadan düzeltildi); kendi 2FA'sını kapatmada parola+kod artık rolden bağımsız isteniyor (panel formu ikisini de zorunlu topluyordu, API owner için yok sayıyordu); `/totp/disable` hız sınırsızdı (her deneme bir scrypt); `rotate-secret` `OwnerGuard` taşımıyordu (uç yeni `apiKey`+`hmacSecret`i çağırana döndürür); `totp_secret_enc` readonly-sql sır listesindeki tek eksikti; tedarikçi fişinde düz metin DÖNMEDEN reveal audit'i yazılıyordu; KVKK anonimleştirme `saved_views.query` kasasını atlıyordu (`URLSearchParams` `@`→`%40` kodladığı için mevcut desen kaçırıyordu). **OPS:** yedek yolu HİÇBİR alarm kanalına bağlı değildi (cron kurulmamışsa aylarca sessiz) → `BackupAlarmService` (`backup_stale` critical / `drill_stale` warning, dedupe'lu); 30dk zombi eşiği RTO≤2sa hedefiyle çelişiyordu (uzun tatbikat "failed" sayılıp kilidi açıyor, deploy tatbikat sürerken koşuyordu) → hedefe-göre eşik + `finish` CAS; rotasyon başarısız yedekten sonra da doğrulanmış dump siliyordu; `SITE_SILENCE_HOURS` compose'tan geçmiyordu (`.env`'e yazmak SESSİZCE etkisiz); `SecurityProcessor` tek alarmsız sweep'ti; CI'da kabuk doğrulaması yoktu — **eklenen `bash -n` ilk koşuda `wp-dev.sh`'ta dengesiz tırnak buldu, yani `pnpm wp:dev` fiilen çalışmıyordu.** **ARAYÜZ:** kayıtlı görünüm tablo içi süzgeçleri SESSİZCE kaybediyordu (uyarı yalnız adres tamamen boşken çıkıyordu) → `/stock`+`/customers` `syncUrl`, diğer ikisi görünür uyarı; rehberdeki iki YANLIŞ VAAT ("gecelik yedek otomatiktir" — gerçekte elle kurulan cron; "filtreler adrese yansır" — 5 ekranın 4'ünde yansımıyordu) düzeltildi; 2FA kurtarma yolu native doğrulamayla kilitliydi; `/admins/security` breadcrumb'ı `/security` ile çakışıyordu. | ✅ typecheck 4/4 (src+test) · check-use-server 25/86 · api birim **124/124** · admin birim **135/135** · admin build ✓ · `bash -n` 9/9 · PHP-lint **13/13** · VPS izole test DB **entegrasyon 357/357** (333 → +24; 10 yeni dosya: iade-defteri · admin-revoke semantiği · recall/void canlı kalem · standing-COGS · reorder-out · tedarikçi zinciri · sole-scheduler · anonymize · fiş audit · readonly-sql) **+ yarış 3/3** · dev: temiz boot (DI hatası YOK — `security.module` glue'su olmasa API hiç açılmazdı, `tsc` yakalamaz), **30 rota 200**, yedek alarmı gerçekten `backup_stale`+`drill_stale` üretti · **tarayıcıda ölçüldü:** `?site=` korunurken tablo araması `tq=`ye yazıldı, süzgeçli adres geri yüklendi · prod `deploy.sh api admin` (rollback'li) → `/v1/health` **200 v1.1.0**, migration tracking **42**, `purchase_orders_created_idx` canlı, **5 yetim zamanlayıcı silindi (loglandı)**, api **0 ERROR**, disk %59. |
| 2026-08-14 | d7b874c | api + admin | **Sekiz öneri maddesinin tamamı (B1–B8)** — altı paralel işçi + merkezî tümleştirme (migration **0040**, additive + idempotent). **[B3] Denetim izi ekranı `/audit`:** `audit_log` doluydu (her reveal/revoke/import/login bir satır) ama onu LİSTELEYEN hiçbir uç yoktu — "bu anahtarı kim gösterdi", "bu siparişi kim iptal etti" soruları ancak veritabanına elle bağlanarak yanıtlanıyordu. Salt-okunur (yazma ucu bilerek yok), `meta` içinde sır taşıyabilecek anahtarlar redakte edilir (bugün no-op — 30 yazar tarandı — gelecek için kalkan), toplam `LIMIT CAP+1` ile SINIRLI sayılır ve aşıldığında `totalCapped` ile DÜRÜSTÇE söylenir. Üç bileşik indeks (ikinci kolon DESC — yön ayna değildir, 0031 dersi). **[B4/B5] İki yeni rapor:** `/reports/sla` teslimat süresi (anında ↔ bekledi ayrımı EŞİK DEĞİL yapısaldır: sipariş ve atamaları tek tx'te yazıldığı için anında teslimde fark tam 0; `avg` yanına **p50/p95** çünkü birkaç uzun bekleme ortalamada kaybolur; held/iptal/bonus/değişim atamaları hariç ve **kaç tanesinin elendiği yanıtta yazar**; tamamlanmamış siparişler `stillOpen` ile ayrı — kohort dürüstlüğü) · `/reports/reorder` yeniden-sipariş önerisi (hız + o tedarikçinin GERÇEKLEŞEN tedarik süresi; yüklem `SuppliersService.scorecard` ile BİREBİR aynı; süre bilinmiyorsa **öneri üretilmez**, varsayılan uydurulmaz; MAK'ta birim↔adet çevrimi — karıştırılsa 500 kat hata). **[B2] Mağaza sessizlik alarmı:** `sites.last_seen_at` HmacGuard'da **imza doğrulandıktan SONRA** yazılır (doğrulanmamış istek canlılık üretebilseydi ölü bir mağaza "canlı" gösterilip alarm susturulabilirdi — sinyalin kapatılabilir olması hiç sinyal olmamasından beterdi), 60 sn throttle hem uygulamada hem SQL'de. **[B8] TOTP iki faktörlü giriş:** RFC 6238 elle yazıldı (**sıfır yeni bağımlılık**), sır AES-256-GCM envelope + AAD `admin_user:<id>`, tekrar-oynatma defteri Redis `SET NX` (fail-CLOSED), lockout parola denemeleriyle **AYNI kovada** (ayrı kova, parolayı bilene 6 hane için taze bütçe vermek olurdu), çerez YALNIZ ikinci adımdan sonra (araya 5 dk'lık, imza anahtarı AYRI TÜRETİLMİŞ `typ`'li beklet-token'ı girer → bir beklet-token'ı asla oturum olarak doğrulanamaz), owner sıfırlaması `token_version` +1. **[B7] Panelden yedek + tatbikat:** dağıtımla AYNI kuyruk (tek aktif iş garantisi ikisini birden kapsar) ama **ayrı runner + hedef filtresi** (`claimNext(targets)`; filtresiz iki runner birbirinin işini kapardı), `/deployments`'ta yaş/boyut/**dış kopya** görünürlüğü + bayatlık bantları; panel konteynerine Docker soketi verilmez (istek/çalıştırma ayrımı korundu). **[B1/B6] Kayıtlı görünümler + envanter dışa aktarma:** görünümler URL query'sini saklar → `DataTable`'a opt-in **`syncUrl`** (arama/facet/sıralama `tq`/`tf.<kolon>`/`tsort` ile adrese yazılır; sayfanın KENDİ parametreleri korunur, bilinmeyen `tf.*` filtre enjekte edemez) — `/orders`'ta menü zaten bağlıydı ama **BOŞ görünüm kaydediyordu**, artık gerçekten çalışıyor; geri yükleme tam gezinme (yumuşak gezinme aynı rotada tabloyu yeniden kurmaz → "geri yükledim sanıp yüklememek"). Dışa aktarma AYRI sunucu ucu (istemci sayfa döngüsü 50 ayrı reveal audit'i üretir ve sayfalar arası mükerrer/atlanmış satır doğururdu), düz metin **yalnız owner** + TEK reveal audit; owner-olmayan maskeli dosya alır ve reveal GERÇEKLEŞMEDİĞİ için audit de YAZILMAZ (canlıda doğrulandı). **ENTEGRASYON PAKETİNİN YAKALADIĞI 2 SORUN (typecheck+build TEMİZ geçiyordu):** (1) **`/audit` tarih süzgeci HER ZAMAN 500 veriyordu** — ham `sql` fragmanına `Date` NESNESİ konmuştu, postgres.js bind'de `ERR_INVALID_ARG_TYPE` atıyor; projenin mevcut deseni (ISO dize + açık `::timestamptz`, `admin-orders.service`) uygulandı. (2) TOTP testleri kurulum onayı ile girişte AYNI 30 sn'lik adımı kullanıyordu; defter bu ikisi arasında ORTAK (RFC 6238 §5.2, kasıtlı) → "geçerli kod reddedildi" gibi görünen 3 başarısızlık; yardımcı düzeltildi ve davranış `(d2)` ile AÇIKÇA kilitlendi. **migration 0040:** `admin_users.totp_secret_enc/totp_enabled` + `sites.last_seen_at` + 3 audit indeksi; üç işçi eşzamanlı migration üretse journal'da çakışırdı → şema değişiklikleri bilerek tek elde toplandı, `when` sırası ve drift ("No schema changes") doğrulandı. | ✅ typecheck 4/4 (src+test) · check-use-server 25 dosya/86 export · api birim **124/124** · admin birim **131/131** (+10 URL durumu) · build 3/3 · VPS izole test DB **entegrasyon 333/333** (328 → +5) **+ yarış 3/3** · dev: migration 0040 uygulandı (tracking 41), **30 rota 200, hata sınırına düşen YOK** (duman listesine `/audit`, `/reports/sla|reorder|costs`, `/admins/security` eklendi) · dev'de canlı uç doğrulaması: audit tarih süzgeci **200** (önce 500) + geçersiz `action` **400**, SLA 7 ölçüm (5 anında / 2 bekledi, p50 38,9 sn / p95 62,9 sn), reorder gerçek ürün (hız 0,33/gün → 6 gün kaldı), dışa aktarma **owner düz metin / owner-olmayan `••••••83GT` + `masked:true`**, yedek özeti "hiç yedek yok → bayat" (dürüst) · prod `deploy.sh api admin` (rollback'li) → `/v1/health` **200 v1.1.0** (db+redis), migration tracking **41**, `audit_log` 4 indeks CANLI, api hata logu **0 ERROR**, `/audit` 307 (auth açık), disk %58. |
| 2026-08-14 | 622a561 | api + admin | **6-lensli güncel tarama — 22 doğrulanmış bulgu** (migration **0039**, additive index). Kullanıcı isteğiyle proje güncel durumuyla yeniden tarandı (13 ajanlı çekişmeli-doğrulamalı workflow: rehber · arayüz boşlukları · operatör akışları · güvenlik · performans · regresyon) → 24 ham / **22 CONFIRMED**; beş paralel işçi + merkezî glue. **[YÜKSEK/güvenlik] Sunucu aksiyonlarında yol enjeksiyonu:** id doğrulanmadan API yoluna gömülüyordu ve server action'lar TS tiplerini ÇALIŞMA ANINDA zorlamaz → `..` ile owner-only uçlara (secret rotasyonu, admin CRUD, dağıtım) Next'in KENDİ ADMIN_TOKEN'ıyla düşülebiliyordu. İki katman: merkezî `lib/api-path.assertSafePath` (dot-segment/`%2e`/şema/kontrol karakteri/çift `?`/kodlanmış bölü reddi; ihlalde istek YAPILMAZ) + **15 aksiyon dosyasının TAMAMINDA** `isUuid` kapısı (eski `!id` kontrolü boş-olmayan zararlı değeri geçiriyordu). Kapının meşru trafiği kırmadığı ÖLÇÜLDÜ: admin'deki **130 yol literali** kapıdan geçirildi, **0 reddedilen**. **[YÜKSEK/akış] Sihirbaz "Site bağlandı" diyordu ama eklenti hiç bağlanmamış olabilirdi:** dört kontrolün dördü de YENİ sitede zaten doğruydu (kayıt var · durum şema-varsayılanı `active` · secret saniyeler önce üretildi · webhook "beklemede"). Beşinci kontrol `sites.plugin_version` üzerinden — o kolon YALNIZ HmacGuard imzayı doğruladıktan sonra yazılır, yani "mağaza panele hiç imzalı istek göndermedi"i kesin ayırt eder; başarı bandı artık yalnız TÜM kontroller geçince çıkıyor. **[YÜKSEK/akış] Pasifleştirilen eşleme ÇIKMAZ SOKAKTI:** okuma yolları eşlemeyi yalnız `active` iken sayıyor, mükerrer ön-kontrolü saymıyordu → ekran "Eşlenmemiş" diyor, "Eşle" **409 "zaten kayıtlı"** veriyor, siparişler /pending'de birikiyor ve çıkış yolu görünmüyordu. Katalog/eşlenmemiş listeleri artık ÜÇÜNCÜ durumu gösteriyor ("Eşleme pasif" + tek tık "Etkinleştir"); `resolveMapping` DEĞİŞMEDİ (pasif eşleme teslimat yapmaz — bilinçli) ve 409 korundu, yalnız mesaj doğru çıkışı söylüyor. **[ORTA/güvenlik]** tam lisans anahtarı erişim logunda DÜZ METİN görünüyordu (arama query string'de gider; redact yalnız gövdeyi kapsıyordu) → `observability/log-redact` hassas parametre DEĞERİNİ gizler, yol/parametre adı okunur kalır; ayrıştırma elle (bozuk yüzde-kodlamada `URL` fırlatır ve bir log serializer'ının isteği düşürmesi kabul edilemez). **Caddy erişim logu ayrı katman — açık ops maddesi.** **[ORTA/arayüz] İnceleme Kuyruğu 200 kayıtta SESSİZCE kırpılıyordu** ve `heldAt DESC` yüzünden EN ESKİ held siparişler düşüyordu (müşteri ödedi, lisans teslim edilmedi, ekran söylemiyordu) → `{items, truncated, limit}` + tie-break + uyarı bandı. **[ORTA]** admin parola sıfırlama sessizce başarısız olabiliyordu (fire-and-forget + yutulan hata + tabloda gözlemlenebilir yan etki YOK) → üç aksiyon da `{ok,error}`, çağıran await ediyor, sonuç toast'lanıyor. **[ORTA/perf]** `/quarantine` havuzu her açılışta 5.000 kayıt + binlerce AES-GCM çözme tetikliyordu (teslimatı da servis eden TEK event loop'ta) → API'ye `preview` parametresi (kapalıyken decrypt HİÇ koşmaz ve reveal audit'i de YAZILMAZ — gösterilen sır yokken "5000 anahtar görüntülendi" kaydı denetim izini yalanlardı) + havuz tavanı 1.000; önizleme KAPATILMADI çünkü havuz paneli anahtar önizlemesini gerçekten gösteriyor (koddan doğrulandı). **[ORTA/perf]** tedarikçi karnesindeki parti listesi LIMIT'sizdi (her stok girişi bir parti üretir) → 200 + `batchesTruncated` + AYRI `batchCount` (kırpılmış listeden sayaç türetmek yanlış toplam gösterirdi). **[DÜŞÜK ×9]** karantinada ölü LEFT JOIN · PO "Tamamlanma" elle kapatılan emirde rozetle çelişiyordu · toplu işlem sonucu otomatik sayfa düzeltmesiyle ekrandan siliniyordu · "Stok Gir" düğmesi ürün detayına gidiyordu (→ `/stock/import?product=`) · ham `mail_resent` · eşleme durumunda ham küçük harf + terim çelişkisi. **[REHBER — operatörün TEK yazılı kaynağı]** "Stok düzeltmeleri" bölümü VAR OLMAYAN bir formu ve alanı tarif ediyordu ve bozuk kalemi gerçekten düşmenin yolunu (Envanter sekmesi → çoklu seçim → Geçersiz kıl/Hasarlı) hiç yazmıyordu → bölüm baştan yazıldı; aynı bayat metin EKRANIN kendisinde de vardı, kart başlığı da düzeltildi. Eksik bölümler eklendi: **Kategoriler · Genel Bakış/canlı akış · İade ve iptal** (kod okunarak: tam iade satırları terminal yapar, kısmi iade yalnız o birimi geri alır, MAK kapasitesi havuza dönmez). Kayıtlı görünüm iddiası gerçeğe daraltıldı, stok girişi tavanları ve kataloğun mağazadan nasıl geldiği yazıldı; **29 bağlantının tamamı** gerçek rotalarla eşleşiyor. Canlı akış "sekme arkadayken duraklar" diyordu — artık durmuyor, 60 sn'ye seyreliyor. **migration 0039:** `batches(supplier_id)` + `(received_at DESC, id DESC)` — FK kısıtı indeks YARATMAZ; `when` damgası kontrol edildi (0039 > 0038; küçük damga bu projede migration'ı sessizce hiç uygulamıyor). | ✅ typecheck 4/4 (src+test) · check-use-server 23 dosya/78 export · api birim **104/104** · admin **105/105** · build 3/3 · VPS izole test DB **entegrasyon 284/284** (274 → +10) **+ yarış 3/3** · dev duman testi **26 rota 200, hata sınırına düşen YOK** · prod `deploy.sh api admin` (rollback'li) → `/v1/health` **200 v1.1.0** (db+redis), migration tracking **40**, `batches_supplier_idx` + `batches_received_idx` CANLI, api hata logu yalnız bilinen mail-relay uyarısı, 5 admin rotası 307 (auth), disk %56. **+45 yeni test** (yol kapısı 31 · log maskeleme 10 · liste kırpma 4 · bağlantı testi/pasif eşleme). |
| 2026-08-14 | f72977e | api + admin + **eklenti v1.0.5** | **Karar bekleyen iki madde uygulandı + eklenti yayınlandı** (migration YOK). **[1] `purchase_orders.received_at` artık "emrin KAPANDIĞI an":** önceden HER kısmi teslim almada üzerine yazılıyordu → alanın anlamı "SON sevkiyatın tarihi" oluyordu ve `status='partial'` iken de doluyordu. TEK tüketicisi tedarikçi karnesindeki `avgLeadDays = avg(received_at - ordered_at)`; yani hâlâ AÇIK emirler tedarik süresi ortalamasına girip KPI'yı olduğundan KISA gösteriyordu (ilk parça gelir gelmez "teslim edildi" sayılıyordu). Sevkiyat başına tarih KAYBOLMAZ — her teslim alma kendi `batches.received_at` kaydını açar ve maliyet raporu ayı zaten ORADAN bucketlar (kod okunarak doğrulandı: `received_at` PO seviyesinde başka hiçbir yerde kullanılmıyor → maliyet raporu ETKİLENMEZ). ESKİ VERİ için karne yüklemine `status='received'` eklendi (DB'de hâlâ açık ama tarihi dolu satırlar olabilir). Admin: "Teslim tarihi" → **"Tamamlanma"**, boşken "— (henüz tamamlanmadı)" (etiket eski kalsaydı kısmi emirde boş bir "Teslim tarihi" operatöre "kayıt kayboldu" dedirtirdi). **[2] `receive()` denetim izine GERÇEK admini yazıyor:** servis `audit_log.actor`'a sabit `'panel:admin'` yazıyordu — "teslim alan kim" sorusunun cevabı hiç kaydedilmiyordu (jsdoc aksini söylüyordu). Çoklu-admin (§8) canlı ve stok girişi/düzeltme uçları çoktan `@AdminActor` kullanıyordu; bu uç atlanmıştı. Controller decorator'ı geçirir, servis parametresi varsayılanlı (doğrudan çağrılan test/iç servis yolları kırılmaz). **[3] Eklenti v1.0.5 YAYINLANDI** (`publish-plugin.sh`, repo HEAD'inden paketlenir — sürüm artırımı geliştirici makinesinde commit'lenir, VPS yalnız yayınlar): müşteri dilinde changelog ile 8 denetim düzeltmesi (saat dilimi UTC → mağaza TZ · order key sızıntısı · sessiz resync/kilit kayıpları · ön yüz bloklayıcı istek · Woo devre dışıyken FATAL · i18n). | ✅ typecheck (src+test) temiz · build 3/3 · api birim 94/94 · +2 entegrasyon testi (kısmi teslimde `received_at` NULL / tam teslimde dolu + partilerin kendi tarihi; denetim izinde gerçek aktör) · prod `deploy.sh api admin` (rollback'li) → `/v1/health` **200 v1.1.0** (db+redis ok) · eklenti yayını **HTTP 201** (105.148 bayt, id 3e074e0c) ve public uç doğrulandı: `plugin/info` **v1.0.5** + changelog metni, `plugin/download/1.0.5` **200** · dev stack güncellendi · disk %55. |
| 2026-08-14 | cca2f0c | api | **Ertelenen üç kapsam boşluğu kapatıldı: süpürme işleri · tedarik yazma yolu · WP eklentisi** (migration YOK). Önceki turda "bilerek yapmadıklarım" diye raporlanan alanlar aynı disiplinle (koddan kanıt → düzelt → test) üç paralel işçiyle ele alındı. **[SÜPÜRME] İki SESSİZ İŞ ÖLÜMÜ:** `low-stock.processor` ve `daily-digest.processor` içinde `@OnWorkerEvent('failed')` YOKTU (kardeş üç süpürmenin hepsinde var). Düşük stok taraması stok alarmının TEK kaynağı — kalıcı bir arıza 30 dakikada bir sessizce tekrarlanır, panel "stok yeterli" görünür, operatör ancak siparişler pending'e düşünce fark ederdi. Günlük özet işi ise günde BİR koşar ve idempotent DEĞİL: patlarsa o günün özeti + TÜM sabit-eşik kritik alarmları kaybolur, yani "alarm gelmedi" ile "her şey yolunda" ayırt edilemez hâle gelir. `SweepAlarmService` `NotificationsModule`'den export edildi (MaintenanceModule'ü import etmek DÖNGÜ olurdu); prod boot logunda `DailyDigestModule dependencies initialized` ile doğrulandı. **[KVKK]** Retention, anonimleştirmenin yazdığı deterministik takma adı (`anon-<hash>@redacted.invalid`) 365 gün sonra yeniden maskeliyordu → satır zaten PII taşımıyor (kazanç sıfır) ama "aynı kişi = aynı maske" bağı geri dönüşsüz kayboluyordu (kayıp kalıcı). **[TEDARİK — gerçek 500] int4 taşması:** `sum(qty_received * unit_cost_cents)` — Postgres'te `int4 * int4` yine int4'tür ve TOPLAMAYA GİRMEDEN taşar (22003); sondaki `::bigint` cast'i çok geç kalıyordu. Doğrulama sınırları buna izin veriyor (`unitCostCents ≤ 2e9`, `qtyOrdered ≤ 1e6`) → 500 adet × 43.000 ₺ gibi GERÇEKÇİ bir alım tedarikçi karnesini ham 500'e düşürürdü. Cast çarpanlara taşındı; işçi kendi dosyasında düzeltip AYNI SINIFI maliyet raporunda da bildirdi → `reports/costs.service.ts` üç noktası da kapatıldı (`PRORATED_COST` zaten `numeric` cast yaptığı için güvenliydi; kalan yerler tarandı). `qty <= 0` guard'ı **NaN'ı geçiriyordu** (NaN karşılaştırması hep false) → `Number.isInteger`. **[WP EKLENTİSİ — 8 kusur]** Tarihler UTC gösteriliyordu (`date_i18n()` timestamp'i ZATEN yerelleştirilmiş sayar; panelden gelen `validUntil` UTC ISO → Türkiye'de 3 saat geriden; müşteri ekranı VE metabox ortak fonksiyonu kullanıyor) · `order_key` giriş yapmış müşteride hâlâ DOM'a/URL'e gömülüyordu (canlı yoklama script'i + ".txt indir" bağlantısı — tarayıcı geçmişi/erişim logu/paylaşılan link) · başarısız `resync` TAMAMEN sessizdi (not yok, retry yok; operatör adedi DÜŞÜRÜP panel erişilemezse fazladan teslim edilmiş birim geri alınmıyordu, §2) · kilit meşgulken iş sessizce düşüyordu (Action Scheduler yoksa; "AS'siz eşzamanlılık oluşmaz" varsayımı YANLIŞ — IPN + admin isteği aynı kilide gider) · `$syncing` bayrağı exception-safe değildi (Throwable'da açık kalıp sonraki resync'leri no-op yapıyor VE kuyruktaki ikizleri düşürüyordu) · admin bar sağlık rozeti mağaza ÖN YÜZÜNDE de panele bloklayıcı istek atıyordu · WooCommerce devre dışıyken public webhook/report-issue/my-account uçlarında tanımsız-fonksiyon FATAL'i · eşleme kutusunda ham İngilizce + platform-sabit metin ("Adet başına key"). **ÇÜRÜTÜLENLER (kontrol edildi, kusur YOK):** klon guard kapsamı TAM · webhook imza yolu iki tarafta tutarlı · `acquire_lock` gerçekten atomik · `collect_lines` net-adet mantığı iade edilen birimi yeniden teslim ettirmiyor · `sync_refund` bundle ölçeğini çift uygulamıyor · audit budama yüklemi gerçekten dar (elle ifşa `meta.auto` yazmadığı için seçilmez) · retention'da FK/23503 riski yok, döngü sonlanır, outbox yalnız `delivered` budanır · `receive()` TOCTOU kilidi doğru (FOR UPDATE hesaptan ÖNCE), `accepted===0` imkânsız. **TESTLER:** +32 entegrasyon (`retention.service` 8 · `daily-digest.low-stock` 13 · `purchase-orders.receive` 11 — eşzamanlı 7+7 → toplam tam 10, over-receive 400 + SIFIR yan etki, int4 taşma regresyonu) + **WP için sıfır-bağımlılıklı PHP koşucusu** (`apps/wp-plugin/tests/run.php`, PHPUnit YOK, WP fonksiyonlarını stub'lar, üretim kodu test için DEĞİŞTİRİLMEDİ — private statikler Reflection ile) → CI'a bağlandı. | ✅ VPS izole test DB: **entegrasyon 274/274** (240 → +34) · yarış 3/3 · api birim 94/94 · typecheck (src+test) temiz · nest build temiz · gerçek `php:8.2-cli-alpine`: **PHP-lint temiz + 108 davranış doğrulaması geçti (exit 0)** · prod `deploy.sh api` (rollback'li) → `/v1/health` **200 v1.1.0** (db+redis ok), boot'ta DI hatası YOK (`DailyDigestModule dependencies initialized`), tek hata satırı bilinen mail-relay uyarısı, disk %54 · dev stack de güncellendi. |
| 2026-08-14 | be0ce50 | api + admin | **5-lensli proje denetimi — 31 doğrulanmış bulgu + 3 SESSİZ regresyon** (migration YOK; 0038 dosyası dayanıklılık için sertleştirildi ama uygulanmış DB'ler etkilenmez). Beş lens (regresyon · güvenlik · perf/DB · UI-UX/a11y · test kapsamı) çekişmeli-doğrulamalı workflow ile tarandı, her bulgu ÇÜRÜTME denemesinden geçti; düzeltmeler 6 paralel işçiye ayrık dosya kümeleriyle dağıtıldı. **[YÜKSEK] keyFormat ReDoS:** ürünün anahtar-biçimi alanı sınırsız serbest metindi ve stok girişinde 10.000 payload'a karşı SENKRON `test()` ediliyordu; Node'da regex zaman aşımı YOK → tek bir katastrofik desen (`^(a+)+$`) sipariş teslimatı dahil TÜM API'yi üstel süre dondururdu. Üç katmanlı kapı: kayıt anında `.max(200)` + derleme + katastrofik kalıp sezgisi (create VE update), kullanım anında aynı kapı (denetimden ÖNCE kaydedilmiş desenler için), payload uzunluk tavanı 4.000. Yeni bağımlılık YOK (RE2 gerekçesi kodda); sezgi 25 vakayla sınandı. **Bağımlılıklar:** `pnpm audit --prod` 11 açık (9 high + 2 moderate) → **0** (drizzle-orm 0.38→0.45, nodemailer 8→9, geçişli override: postcss/nanoid/fast-uri×2/find-my-way/sharp). **TESTLERİN YAKALADIĞI 3 SESSİZ REGRESYON — ikisi bu turun KENDİ değişiklikleri:** (1) **drizzle 0.45 sürücü hatalarını `DrizzleQueryError` içine SARIYOR** ve orijinali `cause` zincirine koyuyor → `(e as {code}).code === '23505'` ve `String(e).includes('unique')` desenleri devre dışı kaldı; **409 dönmesi gereken 5 yol ham 500** dönüyordu (kategori ikiz adı ×2, admin e-posta/kullanıcı adı, site-ürün eşlemesi ×2). typecheck + build + 86 birim testi TEMİZ geçiyordu — yalnız entegrasyon yakaladı (**238/240**) → yeni `apps/api/src/db/pg-error.ts` (cause zincirini gezen tek kaynak, döngü-güvenli, metin yedekli) + 8 birim testi. (2) `clampPageSize` varsayılanı sabit listesinin İLK elemanıydı; listeye başa 10 eklenince "pageSize göndermeyen" HER çağrı sessizce 25 → 10 satıra düştü (**208/210**) → `DEFAULT_LICENSE_PAGE_SIZE = 25` açıkça yazıldı. (3) test dosyaları HİÇBİR tip denetiminden geçmiyordu (`tsconfig.json` derleme için `*.test.ts`'i dışlıyor + vitest tip kontrol etmiyor) → 5 dosyada bayat imza (3 argümanlık kurucuya 1 argüman, kaldırılmış alan, daralmış union); `apps/api/tsconfig.tests.json` (src + test) eklendi ve `typecheck` script'i ona bağlandı → CI'daki mevcut adım artık testleri de kapsıyor. **Kullanıcıyı doğrudan etkileyen düzeltmeler:** müşteriye giden teslimat maili sipariş detayında **"Tedarikçiye gönderildi"** yazıyordu (metin yanlış sözlüğe yazılmış; `labels.ts`'e çakışan-anahtar uyarısı eklendi) · ürün detayındaki "Düzenle" kategori listesini almıyordu → ürünü kategoriye taşımak o ekrandan İMKÂNSIZDI · stok girişinde BÜYÜK harfli İngilizce başlıklar (`EMAIL`/`LOGIN`/`PIN`/`ID`) başlık sayılmayıp **VERİ olarak içe aktarılıyordu** (tr-TR katlamasında ASCII `I` → `ı`; kullanıcı adı "EMAIL" olan sahte hesap) · `includesTr` matrisinin 5 hücresi eşleşmiyordu ("isik" → "IŞIK" bulunamıyordu) → üçüncü katlama geçişi (İ/I/ı/i → i), DB'deki kategori ikiz kuralıyla (0038) AYNI dil, eski davranışı kilitleyen testler bilerek güncellendi · toplu geçersiz kılma BAŞARISIZKEN yeşil ✓ kutusunda gösterilip seçim temizleniyordu → ton sonuçtan türetiliyor, hata dalında seçim korunuyor · çok sayfalı seçim sessizce kayboluyordu → sıfırlama bilinçli ve YAZILI · `/ops` sunucu aksiyonunda yol enjeksiyonu (server action'lar TS tipini çalışma anında ZORLAMAZ) → beyaz liste + UUID + `encodeURIComponent` · müşteri kaynaklı `reason` üst sınırsızdı · `?siteId=` doğrulanmıyordu (22P02 → 500) · CollapsiblePanel açılınca klavye odağı `<body>`'ye düşüyordu (/categories, /admins, /suppliers) · Kusurlu Stok araması `includesTr`'yi atlıyordu · dar ekran kart görünümünde boş-durum çizilmiyordu · kategori "sabitleme sırası" boşaltılınca sessizce eski değerde kalıyordu. **Performans:** envanter aramasında hash eşitliği (UNIQUE index) ILIKE'lar ve KORELE `EXISTS` ile aynı OR'daydı → tam anahtar aramasında bile `license_items` TAM taraması + eşleşmeyen her satır için 3-join alt-plan; aday id kümesi UNION ALL'a ayrıldı ve süzgeç yalnız `li` kolonlarına dayandığı için `page_slice`'tan gereksiz join'ler kalktı (**davranış birebir korundu**) · `/stock` giriş ekranı hiç render edilmeyen ürün agregasyonunu her açılışta çekiyordu · `/customers` site özeti önbeleksiz tam tablo taramasıydı (60 sn TTL + tek-uçuş) · `customers` aramasındaki "SARGABLE" yorumu YANLIŞTI (önek dalı hem index açmıyor hem `önek ⊆ contains` olduğu için katkısız) → ölü dal kaldırıldı, gerçek çözüm (`pg_trgm`+GIN) ölçek eşiğiyle belgelendi · `pending-lines` LIMIT off-by-one + tie-break. **Test kapsamı:** 5 yeni entegrasyon dosyası (kategoriler · envanter araması · lisans mutasyonları · müşteri site özeti · süre/holder süzgeçleri) · `apps/admin`'de test koşucusu YOKTU → vitest + **74 test** (`includesTr` I/İ/ı/i tam matrisi + stok girişi ayrıştırıcısı) · `fill-target` ve `pg-error` birim testleri · `quota.guard` testi hiçbir rotaya BAĞLI OLMAYAN guard'ı doğruluyordu (yanlış güvence) → dürüstleştirildi · `smoke-routes.sh` `/categories`'i taramıyordu. **Migration 0038 sertleştirmesi:** yalnız `DROP INDEX` + `CREATE UNIQUE INDEX` idi → 0037 uygulanmış ve ARADA Türkçe-ikiz kategori oluşmuş bir veritabanında CREATE patlar ve migration boot'ta koştuğu için **API HİÇ AÇILMAZDI**; ön temizlik eklendi (ikizler deterministik yeniden adlandırılır, ürün SİLİNMEZ — `ON DELETE SET NULL` sözleşmesiyle tutarlı), gerçek Postgres'te kirli veriyle doğrulandı ve ikinci koşu idempotent. Uygulanmış veritabanları etkilenmez (drizzle sırayı zaman damgasından yürütür). | ✅ typecheck 4/4 (artık test dosyaları DAHİL) · check-use-server 23 dosya/77 export · admin **74/74** · api birim **94/94** · VPS izole test DB **entegrasyon 240/240 + yarış 3/3** · build 3/3 · `pnpm audit --prod` **"No known vulnerabilities found"** · prod `deploy.sh api admin` (rollback'li) → `/v1/health` **200 v1.1.0** (db+redis ok), migration tracking **39** (yeni migration yok — doğru), 5 admin rotası **307** (auth açık, beklenen), api hata logu yalnız bilinen mail-relay uyarısı, disk %54 (68G boş). |
| 2026-08-14 | fcf0b43 | api + admin | **Beş parti tek dağıtımda** (migration **0037 + 0038**). Prod `815bedb`'de kalmıştı; aradaki tüm iş birlikte alındı. **[1] Rozet dili canlı yüzeye** — referans (shadcnspace) rozeti tek renkten kuruyor ve metin kontrastı ölçüldüğünde AA'nın ALTINDA kalıyordu (teal **2.21**, kırmızı **3.30**) → iki katmanlı token: `--<hue>` metin (AA zorunlu), yeni `--<hue>-vivid` yalnız `color-mix` ile dolgu/halka üretir. Rozet dolgusu **1,5–2,3× doygunlaştı**, kontrast açık temada **4.64–5.34**, koyu temada **5.35–7.27**. Ayrıca **sRGB DIŞI 4 token** düzeltildi (tarayıcı kırpıyordu → ekrandaki renk yazılan renk değildi). Soluk tint dört ayrı oranda (%12/13/14/16) kopyalanmıştı → tek `--<hue>-fill`/`-ring`. Geometri sabitlendi (`h-5`, `px-2`, 12px/600, `whitespace-nowrap`). **[2] Müşteriler: mağaza → müşteri hiyerarşisi** — giriş ekranı artık mağaza kartları; `?site=` daralt, `?q=` **hiyerarşiyi ATLAYAN** sunucu araması (mağaza içindeyken arama o mağazayla sınırlı kalsaydı operatör "müşteri yok" sanardı). Yeni uç `GET /v1/admin/customers/site-summary` — rota `:email`'den **ÖNCE** tanımlandı (altta kalsaydı adres detay ucuna düşerdi). Veri modeli değişmedi (müşteri hâlâ e-posta bazlı global). **[3] Ürün kategorileri (0037+0038)** — ayrı `/categories` ekranı (kullanıcı kararı: ürün formunda serbest metin YOK) + `/stock` üç hâlli (kategori kartları / `?q=` / `?category=`); kart sayacı **atanabilir KAPASİTE** (Σ max_uses−use_count, satır sayısı değil — MAK'ta 1 anahtar 500 kullanım taşır) ve süresi geçmiş kalem sayılmaz. `ON DELETE SET NULL`: kategori silinince ürün silinmez, "Kategorisiz" olur. **0038 — Türkçe ikiz açığı (dev'de ÖLÇÜLDÜ, ilk sürümümün kusuru):** düz `lower()` "WINDOWS LİSANSLARI"yı kabul ediyordu (201) çünkü İ/I/ı/i dört varyantı farklı sayılıyor → index `lower(translate(name,'İIı','iii'))`. **[4] Kategori sadeleştirme** — rehber şerit 110→**34px**, kart 110→**60px**, sıralama "stoğu çok olan önce" (Kategorisiz hep sonda), 8'i geçince `includesTr` süzme kutusu; iki Postgres hatası ölçülerek bulundu (**0A000** UNION'ın doğrudan ORDER BY'ı yalnız kolon ADI kabul eder → `cards` CTE; **42601** eksik virgül). Sayfa boyu ÜÇ yerde tanımlıydı → `lib/license-page-sizes.ts` (kutuda "10 kayıt" yazarken tablo 25 satır gösteriyordu). **[5] Lisans görünürlüğü + kapsamlı arama + kopyala düğmesi** — kırpma kaldırıldı (kaybolan kısım tam da kalemi AYIRT EDEN son hanelerdi), arama üç eksende (TAM anahtar `payload_hash` eşitliği — hash anahtarlı, DB'yi ele geçiren bu aramayı yapamaz · son 5 hane · ürün adı/SKU; ≤6 kanonik biçim varyantı), dar ekranda satır KARTA dönüşür; kopyala düğmesindeki `flex-1` metni hücrenin tamamına yaydığı için düğme kolonun sağ ucuna itiliyordu (kısa anahtarda ~330px) → artık metnin 6px sağında. | ✅ `deploy.sh api admin` (rollback'li) → **`/v1/health` 200 v1.1.0** (db+redis ok) · migration tracking **37→39**, `product_categories` + `product_categories_name_lower_idx` **`lower(translate(name,'İIı','iii'))` ile canlı** · 6 admin rotası 307 (auth açık, beklenen) · api log hatası: yalnız bilinen `MailConfigGuardService` uyarısı (prod SMTP hâlâ mailpit — ops maddesi, bu dağıtımın regresyonu değil) · disk 50% (73G boş). **Ön doğrulama (yerel dev, gerçek veriyle ölçüm):** 375px kart — anahtar **227px / 2 satır**, kopyala **4px** sağında ve aynı satırda, yatay kayma **0** · 1280px tablo — gap **6px**, tek satır, kayma **0** · 1600px (9 kolon) — lisans hücresi 420→**351px** daraldığı için tablo içi kaydırma **110px → 0** (önceki turda "bilinçli kabul" denen artık bant kendiliğinden kapandı) · arama: tam anahtar (büyük+küçük harf) 1 kayıt, son-5 hane 1, "Windows" 35, "WIN11-PANEL" 35 · typecheck 4/4 · check-use-server 23/77 · admin production build. |
| 2026-08-14 | c4f6e04 | admin | **Canlı akış: yeni sipariş fark edilirliği** (migration YOK). Kullanıcı ekran görüntüsüyle: *"anlık sipariş düşüyor ama yeni sipariş olup olmadığı ekranda pek ayırt edilemiyor"*. Kök neden KODDAN ölçüldü: yeni kayıt vurgusu **12 sn sonra siliniyordu** (`live-provider` freshTimer) ve vurgunun tamamı 2px sol şerit + soluk zemindi ("Yeni" kelimesi yalnız `sr-only`) → 15 sn başka yere bakan operatör siparişi kaçırıyor, ekranda hiçbir kalıcı iz kalmıyordu; ayrıca **sekme arkadayken poll TAMAMEN duruyordu** (başka sekmedeyken gelen sipariş ne sayaçta ne başlıkta görünüyordu) ve kart sayacı toplam satırdı ("kaçı yeni" yok). **[1]** Sönen `fresh` KORUNDU ama artık yalnız giriş animasyonunu sürüyor; kalıcı **`unseen`** kümesi eklendi (satıra tıklayınca/"Okundu" ile kalkar, **her turda budanır** → pencereden düşen id sayaçta kalıp listeyle çelişmez): satırda görünür `YENİ` pill'i, başlıkta "N yeni" + "Okundu", **okunmamış sınır çizgisi** (yalnız yeni kayıtlar listenin BAŞINDA kesintisizse — yanlış yerdeki sınır hiç olmamasından yanıltıcıdır). Yeni hue EKLENMEDİ: "yeni olmak" durum değil okunmamışlık işareti → kenar menü aktif pill diliyle aynı dolu `primary`. **[2]** Yeni `components/live/live-alerts.tsx` (kabukta TEK mount, render'ı yok): sekme başlığında `(N)` sayacı (Next gezinmede `<title>`'ı kendi effect'inde ezdiği ve sıra garantili olmadığı için **MutationObserver** ile yeniden basılır) + tıklanabilir **toast** (tek kayıt → doğrudan siparişe; çok kayıt → TEK özet toast, satır başına değil). **[3]** Arka plan poll'u durmak yerine **15→60 sn** seyreliyor (ETag/304). **[4]** Sıcak sipariş ayrımı: "Tümü / İşlem bekleyen" filtresi (`held` + eşlemesiz + `pending`/`partial`) + bekleyen satırda **5 dk**'yı geçen sürenin uyarı tonuna dönmesi (teslim edilmişte ton HİÇ değişmez). | ✅ Tarayıcıda ÖLÇÜLDÜ (canlı API'ye bağlı yerel dev; canlı yanıta sentetik sipariş enjekte edilerek): gizli sekmede **87 sn'de tam 1 poll (t=60 sn)** — eskiden 0 · başlık `(1)`→`(2)`→"Okundu" sonrası temiz · satır `YENİ \| az önce \| #9002 \| … \| Bekliyor` · #9001 eklendikten **~40 sn sonra hâlâ `YENİ`** (eski davranışta 12 sn'de silinirdi) · toast `Yeni sipariş #9002 … [Aç]` · filtre "İşlem bekleyen 2" → teslim edilmiş sipariş listeden düştü · 1 ayraç + 1 `animate-feed-in`. typecheck 4/4 · check-use-server 22/74 · production build (`/dashboard` 9.53→**10.5 kB**). **Ölçülen sınır (dürüstlük):** sekme GERÇEKTEN gizliyken sonner toast'ı DOM'a basmıyor (zaten görünmezdi) — o senaryoyu başlık sayacı karşılar. Prod: `deploy.sh admin` (rollback'li) → `/v1/health` **200 v1.1.0**, admin **healthy**; dev stack de güncellendi (`dev-admin…/dashboard` 200). |
| 2026-08-14 | 2b11ee1 | admin | **Segment çubuğu tek kaynağa + dokunma hedefleri + dar ekranda otomatik daralma** (migration YOK). Önceki turlarda "kapsam dışı" bırakılan kalemler kapatıldı. **[1]** Panelde aynı anlamı ("bir kümeden TEK seçim") taşıyan çubuk **dört ayrı lehçede** çiziliyordu: `ui/tabs.tsx` (kap `bg-muted/40` + aktif KABARTMA) · `support-table` (kap `bg-card` + aktif DOLU `bg-primary`) · `claims/quarantine-nav` (aktifte fazladan `ring-1 ring-border`, öğe `px-3.5 py-2`) · `stock/import` parti modu (kap `rounded-md p-0.5`, öğe `rounded-[0.3rem] text-xs`). Tek tek yamamak yerine **TEK KAYNAK** yazıldı: `lib/segmented.ts` (`SEGMENTED_LIST` / `SEGMENTED_ITEM` / `_ACTIVE` / `_IDLE` + `segmentedItem()`), kanonik dil `tabs.tsx`'ten alındı; **beş çağrı yeri** bağlandı. `quarantine-table`'daki iki tek-seçim grubu (kalem durumu, kusur tarihi) dolu/outline Button çiftinden segment çubuğuna çevrildi — semantik korundu (`role="group"` + `aria-pressed`). Sabitte odak halkası ve yeni hue YOK. **[2] Dokunma hedefleri:** `claims/claim-detail`'in başlık+satır seçim kutuları 36px hücrede duruyordu → `license-items-table` deseni (`w-11 p-0` + `size-11` label = **44px**, görsel kutu 16px kalır); `quarantine-table` kopyala düğmesindeki `size-7` ezmesi kaldırıldı (icon-sm **32px**), "mağazada aç" bağlantısı 24 → **32px daire**. **[3] Dar masaüstünde otomatik daralma:** 768-1023px bandında 256px menü içeriğe ~500px bırakıp sayfayı sıkıştırıyordu → o bantta menü otomatik ikon rayına iner, 1024px üstünde kullanıcının KAYITLI tercihine döner. **Cookie YAZILMAZ** (`_setOpen`, `setOpen` değil): otomatik bir sunum kararı kullanıcı tercihini kalıcı ezmemeli; bant içinde elle açılırsa açık kalır (efekt yalnız eşik geçişinde çalışır). **[4]** `tabs.tsx`'teki çelişkili yorum bloğu temizlendi (eski "istisna geçerli" gerekçesi ile sonradan eklenen "halka kaldırıldı" notu yan yana duruyordu). | ✅ dev'de ÖLÇÜLDÜ — segment: `/quarantine`, `/stock/import`, `/support` üçünde de kap `8px\|4px\|4px`, öğe `12px\|4px\|14px\|6px` **tek stil**, eski lehçe kalıntısı **0**. Otomatik daralma: 880px → menü `collapsed` ve **cookie `true` KORUNDU**, içerik 590 → **840px**; 1200px → `expanded` (256px). Üst bar taşması 0, kırpılan 0, hata sınırı yok. typecheck temiz · check-use-server 22/74 · production build. **SINIR (dürüstlük):** otomatik daralmanın CANLI yeniden-boyutlandırma yolu bu doğrulama panelinde denenemedi — kontrol denemesi: viewport 1200→880 olurken `matchMedia.matches` doğru değişti ama `change` sayacı **0**, hatta `resize` sayacı **0** (panel viewport'u olay tetiklemeden değiştiriyor; donmuş CSS animasyonları/klavye olaylarıyla aynı sınıf sınırlama). Sayfa-yükleme yolu iki genişlikte de doğrulandı. Prod: `deploy.sh admin` (rollback'li) → `/v1/health` **200 v1.1.0**, admin **healthy**, log hatası **0**. |
| 2026-08-14 | 52348fb | admin | **Menü–içerik sınırı + üst bar taşması** (migration YOK). Kullanıcı: *"sol admin menü ile sağdaki içerik alanı arasındaki çizgi geçişi bozuk gibi; üst menü üzerinde de sorunlar var gibi"*. İkisi de ölçüldü, ikisi de gerçek çıktı. **[SINIR]** menü ile yüzen içerik kartı arasında oluk **0px**'ti ve menünün **10px kaydırma çubuğu** tam o sınıra oturuyordu; menü zemini = sayfa zemini olduğundan çubuk, zeminde başıboş duran dikey bir çizgi gibi okunuyor ve yanındaki 1px kart outline'ıyla "çizgi–boşluk–çizgi" veriyordu. Referans (shadcnspace) menü içeriğinde `no-scrollbar` + wrapper'da `gap-1` kullanıyor → ikisi de uygulandı: `globals.css`'e `.no-scrollbar` (kaydırma çalışır, çubuk görünmez; YALNIZ SidebarContent'e — tablo/sayfa kaydırıcıları çubuklu kalır) ve wrapper `has-data-[variant=inset]:gap-1` (**oluk 0→4px**). **[ÜST BAR]** orta genişlikte sağdaki kontroller barın DIŞINA taşıp kırpılıyordu — ölçüldü (789px + açık menü): barın sağ kenarı **771**, tema düğmesi **x=791** → düğme erişilemez. Sebep: breadcrumb esnemiyor, arama sabit 224px, sağ blokta `shrink-0` yok. Düzeltme: breadcrumb `min-w-0 flex-1 overflow-hidden`, sağ blok `shrink-0`, arama kademeli (`sm:w-44 lg:w-56`). Ayrıca üst bar yatay dolgusu ana içerikle eşitlendi (`px-4 md:px-6`; 16 vs 24px olduğu için başlık çubuğu **17px kayıktı**). **KENDİ REGRESYONUM (görsel doğrulamada yakalandı):** breadcrumb'a esneklik verince `BreadcrumbList`'in taban `flex-wrap` sınıfı devreye girdi ve dar alanda "Panel >" / "Genel Bakış" alt alta düşüp 56px'lik bardan taştı — SAYISAL kontrol bunu göremedi ("taşan öğe 0" diyordu, çünkü taşma değil satır kırılmasıydı); `[&_ol]:flex-nowrap [&_ol]:whitespace-nowrap` ile tek satıra sabitlendi. | ✅ dev'de ÖLÇÜLDÜ: menü çubuğu **0px** (kaydırma çalışıyor), oluk **4px**, üst bar **56px sabit**, breadcrumb **tek satır 20px**, üst bardan taşan **0**; 789px'te 7 sayfa (stok/siparişler/karantina/raporlar/destek/siteler/pano) temiz; mobil 375px'te üst bar tam genişlik, kırpılan **0**, sayfa yatay kaymıyor. typecheck temiz · production build. Prod: `deploy.sh admin` (rollback'li) → `/v1/health` **200 v1.1.0**, admin **healthy**, log hatası **0**. |
| 2026-08-14 | 8accf3f | admin | **Kenar menü hizasızlığı — ikisi de BU TURDA benim soktuğum regresyon** (migration YOK). Kullanıcı ikon rayının ekran görüntüsünü gönderip *"sol menüde hâlâ sorunlar var, daraltınca kaymalar"* dedi. ÖLÇÜLDÜ: **[1]** `SidebarHeader`/`SidebarFooter` dolgusunu ferahlık için `p-2`→`p-3` yapmıştım; `SidebarGroup` 8px yatay dolguda kaldığı için açık menüde marka/kullanıcı satırı **x=12**, menü öğeleri **x=8** → menü boyunca 4px kayma; 48px ikon rayında düğme merkezi **28** vs ray merkezi **24** ve kullanıcı avatarı **x=47**'de kırpılıyordu. Düzeltme: `px-2 py-3` (yatay dolgu grup ile AYNI, dikey ferahlık korunur). **[2]** ikon moduna eklediğim `justify-center`, içerik kaba sığmadığı için taşmayı **İKİ YANA** dağıtıyordu: düğme 32px'e inerken çocuklar (ikon + etiket span'i + `gap-3`, nav-user'da ayrıca chevron) ~74px yer kaplamaya devam ediyor → nav-user avatarı **x=−9** (düğmenin SOLUNDA, kırpık), menü ikonları 6px sola kaymış. `justify-start`ta taşma yalnız sağa gidip kırpılıyordu, `justify-center` sola da taşırdı. Düzeltme: ikon modunda fazla çocukları GİZLE (`gap-0` + `[&>span:last-child]:hidden`; nav-user'da metin span'i ve chevron `span:last-child` OLMADIĞI için elle `group-data-[collapsible=icon]:hidden`) → tek çocuk kalır, `justify-center` gerçek merkezi verir. **DERS:** bir bileşenin GENİŞ halini ferahlatan dolgu değişikliği DAR (ikon) halinde hizayı bozar — iki modu da ölç; ve `justify-center` içerik sığmıyorsa taşmayı iki yana dağıtır, önce içeriği kısıtla sonra ortala. | ✅ dev'de ÖLÇÜLDÜ — ikon rayı: marka/menü/kullanıcı **üçü de x=8 · w=32 · merkez=24 = ray merkezi**, kullanıcı avatarı **x=−9→x=8**, **27 düğmenin hiçbirinde görünür taşma yok**; açık menü: marka + grup etiketi + menü + kullanıcı **hepsi x=8**. Ayrıca /dashboard 375·551·768·820·1102·1440px'te taşan eleman **0**. typecheck temiz · production build. Prod: `deploy.sh admin` (rollback'li) → `/v1/health` **200 v1.1.0**, admin **healthy**, log hatası **0**. **Bilinen artık:** kaydırma çubuğu göründüğünde menü listesi sağdan ~10px daralır (marka/kullanıcı sabit) — sol eksen hizalı, upstream shadcn ile aynı davranış; platforma bağlı telafi hack'i eklenmedi. |
| 2026-08-14 | 38d2ba3 | admin | **shadcnspace uyum denetimi — 43 doğrulanmış bulgu** (migration YOK). Kullanıcı: *"dev tarafına entegre edilmemiş; verdiğim referanslardan çok alakasız; UI/UX sorunları olmadan masaüstü ve mobil tarafını detaylıca"*. **İKİ KENDİ HATAM:** (1) v1.1.0'da referansı **hiç GÖRMEMİŞTİM** — tarayıcı paneli kare üretmediği için yalnız token seviyesinde (font/yarıçap/gölge) uyarlama yapılmış, sayfa kompozisyonu karşılaştırılmamıştı; (2) **dev güncellenmemişti** (yalnız prod'a dağıtılmıştı) → kullanıcı büyük olasılıkla ESKİ temaya bakıyordu. Bu turda ekran görüntüsü alındı; oluşan "referansın kenar menüsü koyu" izlenimi görüntüye **kalibrasyon şeridi** (siyah/orta gri/beyaz) basılarak **ÇÜRÜTÜLDÜ** — beyaz bant menü zeminiyle ayırt edilemedi, yani palet doğruymuş. **ÖLÇÜLEN KUSURLAR:** tablo başlık dili çelişiyordu (sıralanabilir `12px/500/BÜYÜK HARF/muted` ↔ düz `14px/600/koyu`; 15 dosya/48 çağrı) · mobilde hücreler eziliyordu (/stock 375px: kap 341px, tablo 862px, hücre 4 satır, satır 85px — kök neden: `width:100%`+`table-layout:auto` tablonun genişliği `max(kap, MIN-CONTENT)`, hücre sarabildiği için min-content "en uzun kelime"ye iniyor → `overflow-x-auto` HİÇ tetiklenmiyor) · sipariş detayı mobilde **sessizce kırpılıyordu** (ızgara 343px, çocuk 380px; sayfa `overflow-x-clip` yüzünden kaymadığı için taşan 21px görünmez+erişilemez, "İptal" düğmesi 383px'te). **DENETİM:** 6 lensli çekişmeli workflow (54 ajan) → 48 bulgu → **43 doğrulandı** (10 yüksek/26 orta/7 düşük), 5 çürütüldü; 4 ayrık-dosya işçisi + çekirdek primitifler. **ÇEKİRDEK:** th/td `whitespace-nowrap` (+4 uzun-metin muafiyeti) · ölü `:has([role=checkbox])` kuralı silindi (native input `role` ÖZNİTELİĞİ taşımaz) · DataTable başlık tipografisi TableHead'e bağlandı, `h-10` ezmesi kalktı, yüzey kart sözleşmesine alındı, kolon-görünürlüğü menüsü mobilde açıldı · sidebar ikon modunda dikey kaydırma (25 öğe ~1170px, alttaki 5-6 öğe erişilemiyordu) · `SidebarInset` `<main>`→`<div>` (iç içe iki landmark) · Dialog `vh`→`svh`, kapat düğmesi 16→32px daire, Popover `max-w`+kaydırma, sheet menüde tıklayınca kapanma + `aria-current` · odak halkaları kaldırıldı (tek kaynak outline) · Select/Combobox `controlBase`'e. **DOĞRULAMA SIRASINDA KENDİ DÜZELTMEM YETERSİZ ÇIKTI:** sol sütuna `min-w-0` verdim, sütun hâlâ 380px kaldı → ölçüm: ray 379.7px, asıl zorlayan **sağ ray** (min-content 380px); mobilde tek kolona düşen ızgarada iki çocuk AYNI rayı paylaşır, ray tabanı çocukların EN BÜYÜĞÜdür. Tek tek aramak yerine 22 ızgara kabına `[&>*]:min-w-0` koruması eklendi (aynı hata daha önce SidebarInset'te yaşanmıştı). | ✅ dev'de (`dev-admin`) gerçek veriyle ÖLÇÜLDÜ: başlıklar artık hepsi `14px/600/normal` · /stock mobil satır **85px→57px**, `nowrap`, kap 341→**1059px gerçekten kayıyor** · sipariş detayı ray **379.7→343px**, "İptal" **383px(ekran dışı)→346px(içeride)**, kırpılan eleman **93→0** · **16 rota 375px'te temiz** (kayma yok, kırpma yok, hata sınırı yok) · masaüstü açık+koyu: th 14px/600/none, td nowrap, yüzey 12px+`shadow-xs`, aktif menü dolu pill + `aria-current=page`, font Geist. typecheck temiz · check-use-server 22/74 · production build. Prod: `deploy.sh admin` (rollback'li). |
| 2026-08-14 | 7d7acf1 (**v1.1.0**) | api + admin | **Admin arayüzü shadcnspace tasarım diline uyarlandı** (migration YOK). Kullanıcı üç referans URL paylaştı (`dashboard.shadcnspace.com` analytics + ecommerce); tasarım **tarayıcıda ÖLÇÜLEREK** çıkarıldı (CSSOM token değerleri, hesaplanmış stiller, yerleşim kutuları — tahmin değil). **PALET DEĞİŞMEDİ:** ölçüm, referansın da standart shadcn nötr oklch paletini kullandığını gösterdi; değişen tipografi/yarıçap/kabuk/yoğunluk. **Font Inter+JetBrains → Geist+Geist Mono** (`next/font/google`, yeni bağımlılık YOK; `latin-ext` ŞART — Geist `latin` setinde ş/ğ/İ/ı/ç yok, eksik olsa Türkçe metin yedek fonta düşerdi). **`--radius` 0.625→0.5rem.** **Kabuk `variant="inset"`** (sayfa zemini `bg-sidebar`, içerik `m-2 rounded-xl outline` ile yüzen kart). **Sidebar aktif öğe DOLU pill** (`bg-primary`) + hover `bg-primary/5`+kayma (ikon modu ve `motion-reduce` hariç). **Tablo başlığı `h-11`/14px/600 KOYU** (uppercase 11px muted kalktı), hücre `px-4 py-3`. **`shadow-sm`→`shadow-xs`**, kart `px-6`+16px başlık, yalnız-ikon düğme DAİRE, select/combobox/popover/dropdown yarıçap hizası. **BİLİNÇLİ 3 SAPMA:** (1) referansın `main` elemanı `overflow:hidden` olduğu için **sticky başlıkları fiilen çalışmıyor** (ölçüldü: 600px kaydırınca başlık −592 px) → bizde `overflow-x-clip` + `sticky top-2` + `rounded-t-xl` ile gerçekten sabit; (2) `--ring` 0.48 KALDI (referans 0.708 odak kontrastını 6.54:1 → ~2.5:1 düşürürdü — belgelenmiş a11y kararı, §17); (3) hücre `py-3` (referansın `py-6` değeri 8-12 kolonlu operasyon tablolarında satır başına ~95px ile ekranı kullanılamaz kılardı). 5 hue semantik durum dili korundu. **ESKİ TEMA YEDEKTE:** `apps/admin/theme-backup/legacy` (16 dosya birebir) + `README.md` + `restore.sh`; klasör `tsconfig.json` `exclude` ve `check-use-server.js` `SKIP_DIRS` içinde (girseydi göreli import-lar typecheck-i kırardı). **API kodu DEĞİŞMEDİ** — yalnız sürüm 1.1.0 hizası için yeniden dağıtıldı. | ✅ `deploy.sh api admin` (otomatik rollback-lı) → **`/v1/health` 200 `version:1.1.0`** (db+redis ok). Ön doğrulama: typecheck temiz · check-use-server 22/74 · production build (15/15 statik sayfa) · yerelde 9 rota 200 / hata sınırı 0 · yerelde ölçüldü: aktif pill açık 0.205↔0.985 (~16:1), koyu 0.922↔0.205, ikon modunda sidebar 48px / öğe 32×32. Canlıda ölçüldü: Geist yüklü (`document.fonts.check`), `--radius .5rem`, kart 12px+`shadow-xs`, alan/düğme 36px+8px+`shadow-xs`. **Kimlik doğrulama açık olduğu için oturum-İÇİ kabuk (sidebar/inset) canlıda doğrulanmadı** — yerelde doğrulandı. |
| 2026-08-14 | 526927e | admin | **Dar/dik ekranlarda SAYFA GÖVDESİ yatay kayıyordu** (salt sunum, migration YOK). Kullanıcı: *"dar monitörlerde veya dik monitörlerde içerikler tam sığmıyor ve orantılı değiller"*. 4 lensli çekişmeli workflow (40 ajan; keşif → çürüt → dosya-ayrık düzelt) → **15 doğrulanmış / 17 çürütülmüş** (3 yüksek · 10 orta · 2 düşük). **ÖLÇÜLEN YER GERÇEĞİ:** 810 px viewport'ta `/stock` → `document.scrollWidth` **856** (46 px taşma), 10 öğe sağ kenarı aşıyor → üst bardaki arama, kimlik-doğrulama uyarı bandı ve tablo sağ kenarları KESİLİYOR. **YANLIŞ HEDEF ELENDİ:** tabloların kendisi sorun DEĞİLDİ — 8 liste rotasında da `<table>` zaten `overflow-x-auto` sarmalayıcı içindeydi (ölçüldü 8/8); taşan şey sayfa KABUĞUYDU (workflow'a bu açıkça yazıldı, yoksa dört ajan da "tabloya kaydırma ekle" diye boşa çalışırdı). **[KÖK NEDEN]** `ui/sidebar.tsx` `SidebarInset`: flex çocuğu olarak varsayılan `min-width:auto` alıyordu → otomatik alt sınırı İÇERİĞİN min-content genişliği oldu. `app-shell`'deki `main` zaten `min-w-0` taşıyordu ama zincirde BİR SEVİYE DERİNDE kaldığı için kabuğu kurtaramıyordu. `min-w-0` + emniyet kemeri `overflow-x-clip` (bilinçli `clip`, `hidden` DEĞİL: clip kaydırma kabı yaratmaz → sticky üst bar çalışır; sarmalayıcı transform/filter/contain taşımadığı için kapsayıcı blok oluşturmaz → sidebar'ın `position:fixed` katmanı kırpılmaz). **Yerel örnekler:** uzun kırılmaz dizeler (site domaini, müşteri e-postası) `break-all` (`break-words` TEK BAŞINA yetmiyor — min-content boyutunu küçültmez; ölçüldü 43 px → 0) · Alert içeriklerine `min-w-0 flex-1` · sarmayan çip/düğme satırlarına `flex-wrap` · breakpoint'siz `lg:grid-cols-5` → `lg:grid-cols-3 xl:grid-cols-5` · native input'ta `min-w-0` · stok girişi iskeleti içerikle aynı kademelere hizalandı (iskelet↔içerik zıplaması). | typecheck 4/4 + check-use-server · admin production build · rota taraması 28/28 temiz · **DÜZELTME KANITI (aynı ölçüm tekrarlandı):** 740 px → sayfa 740 (öncesi 810→856), 390 px → `/stock`, `/quarantine/records` ve sipariş detayında sayfa 390, `kayiyor:false`, **kaydırma kabı DIŞINDA taşan öğe 0** · üst bar hâlâ `position:sticky` (emniyet kemeri onu bozmadı) · prod deploy (rollback'li) → admin sağlıklı. NOT: iframe ile toplu ölçüm panelin `X-Frame-Options` başlığı yüzünden ENGELLENDİ (doğru davranış) → sayfalar tek tek gezilerek ölçüldü |
| 2026-08-14 | bcd714b | admin | **Fiş rozetleri ayırt edilebilir + bölüm çubuğu yenilendi** (salt sunum, migration YOK). Kullanıcı: *"badgeler ayırt edilemiyor yeterince renkten dolayı"* + *"tab menüyü de daha iyi hale getir"*. **ROZET:** dört durumun ÜÇÜ griydi ve `closed`/`canceled` BİREBİR aynı nötr rozetti (yalnız ikon farklı) → listede tarama imkânsızdı. Yeni hue EKLENMEDİ, mevcut beş ton içinde ayrıştırıldı: Taslak outline→**attention (mor)** = "iş sende, indirip gönder" (panelde mor zaten insan-kararı demek) · Gönderildi info (değişmedi) · Kapandı neutral (değişmedi) · İptal neutral→**danger (kırmızı)** = ölü kayıt; ayrıca panelin geri kalanıyla TUTARSIZDI (SUPPLY_STATUS `canceled` zaten danger). Kalem yanıtlarında "Yenisi geldi" ile "Bedeli iade edildi" aynı yeşil VE aynı ikondu → RefreshCw / Banknote (aynı ton içinde ayrım ikonla). **ÇUBUK:** (a) üstündeki üç tanımlı satır dar ekranda sağdan KESİLİYORDU ve her sayfanın kendi başlığı zaten aynı şeyi anlatıyordu → paragraf kaldırıldı; (b) `overflow-x-auto` masaüstünde bile görünür kaydırma çubuğu artefaktı bırakıyordu → `flex-wrap`; kap kırpmadığı için ring-inset istisnası da kalktı (tek odak göstergesi kuralı). Öğeler büyütüldü + bölüm ikonları (Inbox/FileText/Archive); aktif öğe yükseltilmiş yüzey + ince kenarla ayrışıyor. | typecheck admin temiz · admin production build · **dev ölçümü:** "Taslak" artık `bg-attention/14 text-attention ring-attention/28`, "Kapandı" `bg-secondary` → sınıf dizeleri FARKLI (eskiden ikisi de griydi); `--attention` oklch hue 300 vs `--info` 250 vs `--destructive` 22 → dört durum dört ayrı hue · çubukta yatay kaydırma YOK, tanım paragrafı DOM'da yok, üç öğede de ikon var, aktif öğede `aria-current="page"` · prod deploy (rollback'li) → admin sağlıklı. **NOT:** `sent` (mavi) ve `canceled` (kırmızı) canlı ölçülemedi — dev'de o durumda fiş yok; eşleme kodda ve build'den geçti |
| 2026-08-14 | cfa64ec | admin | **Kusurlu Stok: üç sekme → ÜÇ GERÇEK ROTA** + fiş gönderildikten sonra listeye dönüş (migration YOK, salt admin sunum/rotalama). Kullanıcı: *"claims sayfası yok, hepsi tek sayfada; sayfalı sistem daha mı iyi olur?"*. **Ölçülen üç gerekçe:** (1) `/quarantine/claims` **404** veriyordu ama her fiş detayının breadcrumb'ı oraya LİNK basıyordu (/products'ta düzeltilen kırık-breadcrumb sınıfının tekrarı); (2) sayfa her açılışta ÜÇ veri kümesini birden çekiyordu — yalnız havuza bakmak için ağır defter sorgusu da koşuyordu; (3) **havuz aslında defterin JS süzgeciydi** (`rows.filter(r => !r.claimId)`) → defter tavana dayanınca havuz SESSİZCE eksilirdi ve kırpma uyarısı yalnız defter sekmesindeydi (API `?claimed=none` süzgecini zaten destekliyordu, kullanılmıyordu). **Yeni yapı:** `/quarantine` Bildirilecekler (havuz, artık SUNUCUDA `claimed=none`) · `/quarantine/claims` Değişim Fişleri (YENİ, 404 kapandı; 500 tavanına dayanılırsa görünür uyarı) · `/quarantine/records` Tüm Kayıtlar (defter + süzgeç + dışa aktarma). Ortak alt-navigasyon gerçek `<Link>`lerle (`aria-current`), **sayaç KOYULMADI** (her sayfa yalnız kendi verisini çeker, başkasının sayısını uyduramaz — sayaç kendi başlığında). Her rotanın kendi loading/error durumu. **Fiş "Tedarikçiye gönderdim" → `/quarantine/claims`'e döner** (kapatma/iptal detayda kalır). revalidate hedefleri alt rotaları kapsayacak şekilde genişletildi; rehber/ürün-detayı/karantina-tablosu linkleri hizalandı; ölü `quarantine-tabs.tsx` silindi. **SAYFALAMA BİLİNÇLİ EKLENMEDİ:** defterde zaten sunucu süzgeci + üst sınır + dürüst kırpma uyarısı + istemci sayfalama var; sunucu-sayfalamanın gizli maliyeti DIŞA AKTARMADIR (süzgeçlenmiş kümenin tamamı aktarılmazsa operatör "hepsini indirdim" sanıp 1. sayfayı indirir — bu depoda yaşanmış sınıf). Tetikleyici: kırpma uyarısı gerçekten çıkmaya başlayınca, /stock'taki mevcut sunucu-sayfalama deseniyle. | typecheck 4/4 + check-use-server · admin production build (`/quarantine` 201 B · `/quarantine/claims` 201 B · `/quarantine/records` 11.5 kB) · **dev doğrulama:** `smoke-routes.sh` **28 rota** (iki yenisi eklendi) hepsi 200, hata sınırı YOK; üç rotada da `aria-current` aktif; fiş detayının breadcrumb'ı artık gerçek sayfaya gidiyor; **API ölçümü** `claimed=none` → 0 satır vs tam defter → 17 satır (havuz gerçekten sunucuda süzülüyor); havuz boş-durum metni doğru · prod deploy (rollback'li) → admin sağlıklı, üç rota da 307→/login (auth AÇIK; olmayan rota 404 ile ayrışıyor) |
| 2026-08-13 | 9a57079 | admin | **"Gönderildi olarak işaretle" → "Tedarikçiye gönderdim"** (salt sunum, migration YOK). Kullanıcı bulgusu: buton belirsizdi. Kök neden — buton panelin YAPTIĞI bir şeyi değil, operatörün panel DIŞINDA yaptığı işi KAYDEDİYOR; edilgen "Gönderildi" ikisini de anlatabiliyordu ("kim, kime?"). Birinci tekil şahıs bunu tek kelimeyle çözer ve üstteki 2. adımla eşleşir (gönderin → gönderdim). "Değişim için" EKLENMEDİ: ekran zaten baştan sona değişim fişi, eksik bilgi "ne için" değil "kim/kime". **Aynı sınıftan ikinci bulgu:** `sent` rozeti "Gönderildi" → **"Tedarikçiye gönderildi"** — bu, `pending` → "Tedarikçi yanıtı bekleniyor" düzeltmesinin ikizi (rozet listelerde TEK BAŞINA görünür, aktörü kendi içinde taşımalı); o turda `sent` atlanmıştı. Onay modali + durum ipuçları + rehber + havuz akış şeridi + fiş listesi tarih damgası aynı sözcüklere hizalandı; "panel dosyayı göndermez" açıkça yazıldı. | typecheck admin temiz · admin production build · **dev canlı doğrulama:** kullanıcının bildirdiği fişte (…3264da86) buton artık "Tedarikçiye gönderdim", eski ifade DOM'da YOK · prod deploy (rollback'li) → admin sağlıklı |
| 2026-08-13 | 26dfabf | api + admin + eklenti v1.0.4 | **RE-DOĞRULAMA — 16 bulgu, KENDİ H1 REGRESYONUM dahil** (migration **0036**, additive). Bir önceki denetim partisinin (1a1df51) düzeltmelerini ÇÜRÜTMEYE çalışan 5 lensli workflow (27 ajan) → 16 CONFIRMED / 6 çürütüldü. **[ORTA — kendi regresyonum]** per-atama iptalinde satırı terminal yapmayıp `qty` düşürmek H1'i YENİDEN AÇTI: `reconcileOrder`'ın iade koruması TEK yükleme dayanıyor (`if (line.canceled) continue`), satır terminal olmayınca mağaza re-push'u (`woocommerce_saved_order_items` → resync) adedi geri yükseltip iptal edilen birime TAZE anahtar teslim ediyordu; ters yönde de "kusurlu anahtar" iptalinde müşterinin ÖDEDİĞİ hak sessizce kısılıyordu. **Kök neden:** `qty`'ye iki anlam yükledim (mağaza gerçeği + doldurma hedefi). **Düzeltme (0036):** `order_lines.canceled_units` defteri — `qty` mağazadan gelir ve DOKUNULMAZ, iptaller ayrı kolonda birikir, hedef TEK noktada `qty − canceled_units` (yeni `orders/fill-target.ts`; completeLine + all-or-nothing + satır durumu + "neden bekliyor" tanısı + getDeliveries toplamı + bulkStatus hepsi oradan okur). Mağaza adedi DÜŞERSE defter aynı miktarda azaltılır (çift sayım yok); adedi artıp defter doluysa GÖRÜNÜR olay yazılır. **[düşük]** `liveSiblings` satırın canceled durumuna bakmıyordu → tam iadede defter şişiyor + yanıltıcı olay yazılıyordu. **[ORTA/güvenlik]** `supplier_claim_items.key_snapshot` readonly-sql denylist'inde YOKTU → fiş maskesi AI NL→SQL yolundan tamamen atlatılabiliyordu (AI varsayılan KAPALI; savunma derinliği) → kolon + iki tablo denylist'e + regresyon testi. **[düşük ×5 admin]** `includesTr` ASCII büyük 'I'da SESSİZ boş sonuç ("ai" → "AI Operasyon" bulunmuyordu) → iki geçişli katlama, MERKEZÎ düzeltme ~15 çağıranı onarır · parti seçicisi artık sunucu-taraflı `?productId=` süzgeci (global 500'lük pencere + istemci süzgeci geçerli partiyi "yok" gösteriyordu) · kırpma bayrağı hata/temizleme yollarında bayat kalıyordu · iptal onay modali artık gerçekleşen davranışı anlatıyor · maskeli fiş raporu sessizce indiriliyordu → uyarı. **[düşük]** `truncated` tam sınırda yanlış pozitifti → "tavan+1 çek, JS'te kırp" · hesap tipli fiş kaleminde maske sır OLMAYAN alanları da siliyordu. **[düşük ×5 WP → v1.0.4]** yeni eklediğim "paket reddedildi" uyarısı Docker/aynı-sunucu kurulumunda KALICI kırmızı banda dönüşüyordu (`is_valid_package_url` ↔ 1.0.3'ün genişlettiği `is_secure_panel_url` çelişkisi) → iki kapı tek tanıma bağlandı (host eşitliği AYNEN korundu) · multisite bayrak kapsamı · `substr` çok baytlı karakteri bölünce mesaj boşalıyordu → mb_substr + görünür kırpma · panel erişilemezken bayrak bayat kalıyordu → "bayat" damgası. | typecheck 4/4 + check-use-server (22 dosya / 74 export) · admin production build · **VPS izole test DB: entegrasyon 210/210 + yarış 3/3** (+ yeni "re-push adedi geri yükseltse bile iptal edilen birim TAZE anahtarla DOLMAZ" regresyon testi — doğrulayıcı bu yolun testsiz olduğunu işaretlemişti) · **PHP-lint 12/12** (bir önceki partide HİÇ koşulmamıştı) · prod deploy (rollback'li) → `/health` **200 v1.0.0** (db+redis), migration tracking **37**, `order_lines.canceled_units` CANLI (default 0, NOT NULL), api ERROR 0 · dev stack güncel · **eklenti v1.0.4 panele yayınlandı** (HTTP 201, 102.001 bayt, HEAD 26dfabf ile birebir) |
| 2026-08-13 | 1a1df51 | api + admin | **PROJE GENELİ DENETİM — 17 doğrulanmış bulgu** (migration **0035**, additive). 5 boyutlu çekişmeli-doğrulamalı denetim (güvenlik/correctness/perf/UI-UX/WP+docs; 10 ajan, bul→çürüt) → 17 CONFIRMED (0 yüksek · 2 orta · 15 düşük); 3 paralel işçi + merkezî orders/şema işi. **[ORTA/correctness]** per-atama iptali çok-adetli satırın TAMAMINI `canceled` yapıyordu → müşteri elinde HÂLÂ GEÇERLİ kardeş anahtarlar dururken 0 lisans görüyordu; artık kardeş kaldıysa satır terminal YAPILMAZ ama qty geri alınan birim kadar DÜŞER (fulfilled==qty ⇒ taze anahtarla refill YOK — H1 korunur), kardeş kalmadıysa eski davranış; adet düşüşü fulfillment_events'e GÖRÜNÜR yazılır (re-push adedi geri yükseltebilir). **[ORTA/ui-ux]** 5 arama noktası (notifications/purchase-orders/review/dead-letter/**Ctrl+K**) `includesTr` yerine ham toLowerCase kullanıyordu → Türkçe İ/I'da SESSİZ boş sonuç. **[güvenlik]** tedarikçi fişi detayı keySnapshot'ı rol-farkında maskelemiyordu (owner'ın kestiği fişi owner-olmayan admin düz metin görüyordu) → @AdminRole + canRevealPlaintext + reveal audit; readonly-sql denylist'ine konfig görünümleri (pg_settings/pg_stat_activity/pg_config/pg_hba_file_rules/pg_file_settings). **[perf/0035]** `assignment_history`'de HİÇ index yoktu (sipariş detayı + karantina tam-tablo) + `assignments(created_at)` (velocity seq-scan) → 3 index; listQuarantine 4 korele alt-sorgu → tek join; /batches picked CTE + LIMIT 500, /customers LIMIT 2000 — İKİSİ DE `truncated` ile DÜRÜSTÇE raporlanır (parti seçicisi dahil, sessiz kırpma yok); detailVelocity 30g budaması. **[correctness]** bulkStatus iptal satırları paydaya katıyordu → FILTER ile getDeliveries'e hizalandı. **[a11y]** fiş sheet'i etiketsiz alanlar (htmlFor/id/ariaLabel) + product-edit hata `role="alert"`. **[ui-ux]** dağıtım durumu /releases↔/deployments'ta farklı renk+etiketti → labels.ts tek kaynak (deployStatusMeta). **[wp]** güncelleyici paket-URL reddini SESSİZCE düşürüyordu → görünür admin uyarısı (güvenlik gevşetilmedi); eski marka kalıntıları jl_/jl- → wpt_/wpt- (API anahtarı öneki, webhook nonce transient, HTML id'leri); readme.txt changelog 1.0.1/1.0.2/1.0.3. Ayrıca kullanıcı bildirimi: fiş oluşturma Sheet gövde dolgusu/kaydırma + havuzda karantina tarihi (f76d63f). | prod `/health` 200 **v1.0.0** (db+redis) · migration tracking **36**, `assignment_history_assignment_idx` + `assignment_history_old_item_idx` + `assignments_created_idx` CANLI · api ERROR 0 · typecheck 4/4 + check-use-server (22 dosya / 74 export) · admin production build · **VPS izole test DB: entegrasyon 205/205 + yarış 3/3** (+3 yeni per-atama-iptal regresyon testi, +2 denylist/maskeleme) · yerel birim 72/72 · **dev doğrulama: 26 rota + 6 detay sayfası 200, hata sınırına düşen YOK**; /batches yeni `{items,truncated}` zarfıyla 8 satır render etti (sessiz boş liste riski kapandı), küçük-harf dağıtım durumu kalmadı |
| 2026-08-13 | 44d233d | api + admin | **KUSURLU STOK İŞ İSTASYONU — üç sekme, tek süzgeç, kapsayıcı terimler** (migration **0034**, additive) — kullanıcı: *"sadece anahtar değil, hesap/ürün varyasyonu da olabilir; /quarantine çok karışık; 'Cevap bekleniyor' anlaşılmıyor; rehber niteliğinde açıklamalar olsun"*. **TERİM:** panel yalnız lisans ANAHTARI satmıyor (hesap · kod/hediye çeki · özel) → `labels.ts` `itemNoun/itemNounPlural/itemCount`: liste TEK tipteyse o tipin adı ("3 hesap"), karışıksa ya da tip bilinmiyorsa nötr "kalem". Karantina/fiş/parti/envanter/sipariş ekranları ve rehber bu dile taşındı; menü **"Kusurlu Anahtarlar" → "Kusurlu Stok"**. **"Cevap bekleniyor" → "Tedarikçi yanıtı bekleniyor"** (rozet tek başına da listelerde görünüyor → aktörü kendi içinde taşımalı), "Reddedildi" → "Tedarikçi kabul etmedi"; her yanıt/durum için tek cümlelik ANLAM sözlüğü (`claimOutcomeHint`/`claimStatusHint`) ve fiş detayında rozet sözlüğü. **ÜÇ SEKME:** eskiden "Bekleyenler" sekmesi 1 derken altındaki tablo 16 satır gösteriyordu (havuz + defter aynı sekmedeydi) → **Bildirilecekler** (iş listesi; tedarikçi→parti, parti satırı KATLANIR ve içindeki kalemleri gösterir, ilk 50) · **Değişim Fişleri** · **Tüm Kayıtlar** (değişmez defter + süzgeç + dışa aktarma). **TEK ARAÇ ÇUBUĞU:** iki süzgeç kartı ve İKİ arama kutusu ("Sunucu süzgeci" / "Yüklenen liste içinde süz") tek kutuda birleşti — yazdıkça yerel süzer, **Enter** ile tüm kayıtlarda arar; uzun açıklamalar katlanır **"Nasıl çalışır?"** kutusuna taşındı. Yeni **"Tedarikçiye bildirim"** kolonu + hızlı süzgeç (Bildirilmedi / Fişte, yanıt bekliyor / Tedarikçi yanıtladı) — sekme sayacı ile liste arasındaki farkın sebebi artık HER SATIRDA yazıyor; sayaçlar bildirim eksenine çevrildi. Sunucu süzgeci etkinken "Bildirilecekler" havuzunun da daraldığı açıkça söyleniyor. **Yeni primitifler** `ui/help-note.tsx`: `HowItWorks` (3 adımlı akış şeridi) · `HelpNote` (katlanır ayrıntı) · `LegendList` (rozet sözlüğü) — sunucu bileşeni, native details, JS gerektirmez. **Fiş detayı:** duruma göre "sırada ne var" şeridi, "Sonuç"→"Tedarikçi yanıtı", kalem tipi satırda ve raporda (**"Tür"** kolonu), kapanmış fişte yanıtsız kalem varsa dürüst bilgi bandı. **0034:** `supplier_claim_items.product_kind` snapshot + mevcut satırlar için geriye dönük doldurma (`UPDATE … FROM products`); `listBatches`e `p.kind` (parti tek ürüne bağlı) → parti ekranları da doğru adı yazıyor. **Migration `when` tuzağı kontrol edildi:** üretilen damga (1786643071095) 0033’ten (1786639201572) büyük. | prod `/health` 200 **v1.0.0** (db+redis) · migration tracking **35**, `supplier_claim_items.product_kind` CANLI (dev backfill: 18/18 satır dolu) · api boot ERROR 0 · typecheck 4/4 + check-use-server (22 dosya / 74 export) · admin production build (`/quarantine` 18.2 kB · `/quarantine/claims/[id]` 10.1 kB) · **VPS izole test DB: entegrasyon 200/200 + yarış 3/3 + api birim 72/72** (regresyon yok) · **dev canlı doğrulama:** üç sekme (Bildirilecekler 1 · Değişim Fişleri 2 · Tüm Kayıtlar 16), defter kolonları `Ürün · Kalem durumu · Tedarikçiye bildirim · Lisans/hesap değeri · Sebep · Tedarikçi/parti · Kaynak sipariş · Müşteri · Kusur tarihi`, satırlar "Bildirilmedi" / "DEG-20260813-02 + Tedarikçi yanıtı bekleniyor", şerit "Kusurlu kalem 16 · Bildirilmedi 1 · Fişte 15 · Yanıtladı 0", TEK arama kutusu + Bildirim/Ürün/Tedarikçi/Site facetleri, breadcrumb "Kusurlu Stok › Değişim Fişleri › Detay" |
| 2026-08-13 | 70022e9 | api + admin | **TEDARİKÇİ DEĞİŞİM FİŞLERİ — kusurlu anahtarları bildir ve süreci takip et** (migration **0033**, additive) — kullanıcı: *"karantina düz liste; partiden hatalı çıkanları tedarikçiye değişime gönderiyorum, gün sonunda/tarih bazlı Z raporu alıp indirebilmeliyim, süreç takip edilebilir olmalı"*. **DOĞRULANAN BOŞLUK:** "bu kusurlu anahtarı tedarikçiye bildirdim mi?" bilgisi şemadaki 28 tablonun HİÇBİRİNDE yoktu; tek bildirim yolu izi olmayan bir tarayıcı indirmesiydi → aynı anahtar defalarca bildirilebiliyor, tedarikçinin yanıtı kayıt dışı kalıyordu. **0033:** `supplier_claims` + `supplier_claim_items`. `license_item_id` PLAIN uuid (FK YOK — `stock_adjustments` deseni). Kalem alanları SNAPSHOT: fiş kesildiği andaki parti/ürün/anahtar/sebep donar → aynı fiş bir ay sonra da BİREBİR aynı dosyayı verir. **Çift bildirimi DB'de engelleyen tek satır:** kısmi unique index `(license_item_id) WHERE outcome <> 'rejected'`. `rejected` bilerek dışarıda — tedarikçi reddederse anahtar HAVUZA GERİ DÖNER ve yeniden bildirilebilir (kullanıcı kararı). **Aday sorgusu YENİDEN YAZILMADI:** `listQuarantine`'e tek EXISTS + `?claimed=none|open|any`. İkinci bir tanım yazmak, bu projede "satılmış 6 birim" yanılgısını üreten hatanın aynısıydı; ekrandaki süzgeç yüklemi ile unique index yüklemi BİREBİR aynı tutuldu. Satıra `defectKind` (customer_return|recall|damage|manual_void) türetildi. **Yeni modül** `supplier-claims` (6 uç): Z raporu önizleme · liste · detay · **fiş kes** (GLOBAL advisory-lock + kilit altında adayları TAZEDEN oku + snapshot + `DEG-YYYYAAGG-NN`) · durum geçişi (draft→sent|canceled, sent→closed; iptal kalemleri SİLER → havuza döner) · kalem sonucu. `audit_action` PG enum'una DOKUNULMADI (mevcut `adjust` + `meta.op`). **Fiş içeriği kesildiği andaki YETKİYLE donar:** owner-olmayan admin fiş keserse snapshot MASKELİ yazılır ve sonradan owner açsa da maskeli kalır (bilinçli). **Tedarikçi karnesine KUSUR bloğu:** anahtar düzeyinde defect rate panelde HİÇ hesaplanmıyordu (`recallRate` yalnız "kaç parti geri çekildi" diyor). `wastage()` ile AYNI tedarikçi zinciri + bildirilmemiş kusur + açık fiş + ort. çözülme süresi + yenilenen/reddedilen. **Admin:** `/quarantine` → **"Kusurlu Anahtarlar"** iş istasyonu, iki sekme (Bekleyenler · Fişler). Bekleyenler üstünde **tedarikçi→parti gruplu panel** (partinin STOK GİRİŞ tarihiyle) + tek tık "Fiş oluştur" + başlıkta **"son fiş: KOD · tarih"**. Fiş kesme Sheet'i (7/30/90/özel ön ayar, adaylar otomatik, tek tek çıkarma). `/quarantine/claims/[id]` detay: özet şerit + **.txt/.csv indirme** + durum düğmeleri + çoklu seçimle kalem sonucu. Rapor SNAPSHOT'tan üretilir ve **KİŞİSEL VERİ İÇERMEZ** (KVKK — karantina "Tedarikçi bildirimi" varyantının kuralı). **`ClaimStatusBadge`/`ClaimOutcomeBadge` AYRI:** fiş sözlüğü mevcut rozet anahtarlarıyla çakışıyor ve AYNI anahtar farklı şey demek (`replaced` sipariş dilinde ölü anahtar, fişte İYİ haber). Tek haritada birleştirmek `SupplyStatusBadge`'i doğuran hatanın aynısı olurdu. **Yeni hue EKLENMEDİ.** **Yol boyunca:** bayat `QuarantineRow` tipi (11 alan eksikti; `Partial<>` yaması tip güvenliğini fiilen kaldırmıştı) gerçek yanıtla hizalandı · `stock_adjustments.license_item_id`'ye index (karantinanın iki sıcak sorgusu onu kullanıyordu, index YOKTU) · breadcrumb ham "claims" · bayat sayaç ipucu. **Bilinen sınır (planda AÇIK):** MAK/çok-kullanımlı kusurlu anahtar hiç karantinaya giremediği için fişe de giremez (atama çekirdeği ayrı iş) · geri çekilen partide satılmış kalemler "Toplu Değiştir" koşulmadan havuza düşmez · partisiz girişte tedarikçi bilinmez → fiş kesilemez · tedarikçiden gelen yeni anahtarların fişe bağlanması kapsam dışı (kullanıcı "tam kapanış"ı seçmedi). | prod `/health` 200 v1.0.0 (db+redis) · migration 0033 boot'ta uygulandı, `supplier_claims` + `supplier_claim_items` CANLI · typecheck 4/4 + check-use-server (22 dosya / 74 export) · admin production build (`/quarantine` 16.3 kB · `/quarantine/claims/[id]` 9.07 kB) · **VPS izole test DB: entegrasyon 200/200** (+8 yeni fiş testi; 192 → 200, regresyon yok) · **dev canlı E2E (gerçek 15 kusurlu kalem):** fiş kes `DEG-20260813-01` 15 kalem → havuz 0 → "Gönderildi" + tedarikçi referansı → 3 kalem reddet → **havuz 3 (reddedilenler geri döndü)** → fiş özeti "15 kalem · 12 bekliyor · 3 red" → tedarikçi karnesi `defectRate 0.68 · unclaimed 3 · openClaims 1 · rejected 3` · tarayıcı: sekmeler + gruplu panel + "son fiş" damgası + fiş detayı (kusur kaynağı "Müşteri iadesi"/"Parti geri çekme" ayrımıyla) doğrulandı |
| 2026-08-13 | a6747e6 | api + admin | **PARTİ SAYAÇLARI ATAMADAN TÜRÜYOR + GERİ ÇEKME SONRASI SATIR-SATIR DEĞİŞİM + TEK ODAK GÖSTERGESİ** — kullanıcı geri çekme onayında "Satılmış 6 birim müşterilerde — bunlar için değişim gerekir" gördü ama 6 birim satılmamıştı (bir kısmı ELLE geçersiz kılınmıştı), "geri çekilen anahtar gösterilmeye devam edecek mi? manuel kontrolümden geçip değiştirebilmem gerek" diye sordu ve inputlarda odak tasarımı sorununu bildirdi. **[SAYAÇ]** `listBatches` sayacı `status <> 'available'` idi → voided/quarantined/replaced/revoked/expired HEPSİ "satılmış" kovasındaydı; aynı serviste `recallBatch` çoktan doğru yüklemi (EXISTS aktif atama) kullanıyordu, iki tanım aynı ekranda çelişiyordu ve `canBulkReplace` yanlış sayaca bağlıydı (0 adayla menü açılıyordu). Tek `LEFT JOIN` + `count(*) FILTER` + `LEFT JOIN LATERAL bool_or` ile beş sayaç: totalCount / unsoldCount (recall'ın void kümesi, DEĞİŞMEDİ) / customerCount (active|suspended) / replaceableCount (bulkReplaceBatch aday kümesiyle BİREBİR: aktif atama + status<>available + usage_mode<>multi) / deadCount. `onlyId` alt sorguya da iner. MAK NOTU: kovalar TOPLANMAZ (kısmen satılmış MAK anahtarı hem stokta hem müşterilerde) → totalCount ayrı gelir. **[TAKİP]** `getDeliveries` yalnız `assignments.status='active'` süzer, parti durumuna BAKMAZ → geri çekilen partinin teslim edilmiş anahtarı müşteride ÇALIŞMAYA DEVAM EDER (doğrusu da budur, §15 "insan onaylar"). Yeni "kim tutuyor" ekseni `?holder=customer` (durum süzgecinden AYRI — MAK anahtarı kısmen satılmışken hâlâ 'available'); envanterde teslim edilmiş satır artık devre dışı iki düğme yerine **"Yeni anahtarla değiştir"** gösteriyor (mevcut `POST /v1/admin/assignments/:id/replace`, iş kuralı tekrarlanmadı); parti detayında **"Müşterilerdeki lisanslar"** kartı (kilitli kapsam) — iş listesi değişim yapıldıkça kendiliğinden kısalır. **[ODAK]** globals.css'teki `:focus-visible` kuralı @layer DIŞINDA; Tailwind v4 utility'leri `@layer utilities`'te ve katman sırası specificity'den ÖNCE geldiği için bu kural her `outline-none`'ı yeniyor → kenarlık + soluk hale + boşluk + outline = 3-4 bant üst üste biniyordu. TARAYICIDA CSSOM ile ÖLÇÜLDÜ. Halkalar KALDIRILDI, outline tek gösterge bırakıldı — çünkü ölçülen kontrast halkalarda eşik ALTINDA (ring/40 1.85:1, ring/60 2.68:1, sidebar-ring 2.48:1), outline'da ÜSTÜNDE (6.54 açık / 4.18 koyu). Yani "kuralı katmana taşı" naif düzeltmesi bir a11y REGRESYONU olurdu; ölçüm bunu gösterdi. Hata durumu artık halka değil `--ring` ezilerek kırmızıya döner. TEK istisna `ring-inset` (kırpılan konteynerler). **[ONAY MODALİ]** `confirm({details})` dizi geçirildiğinde React ayraçsız BİTİŞİK metin basıyordu ("…geri alınamaz).Satılmış 6 birim…") ve metin düğümleri `space-y-3`'e girmediği için alt alanla boşluk kalmıyordu → dizi artık madde listesi (iki çağıran etkileniyordu). **ÇEKİŞMELİ DOĞRULAMA (3 lens, kendi değişikliklerimi çürüt):** 5 gerçek bulgu çıkardı, hepsi deploy ÖNCESİ kapatıldı — (a) askıdaki atamada "değiştir" düğmesi API tarafından HER ZAMAN 400 alıyordu (çıkmaz sokak) → sebebiyle devre dışı; (b) replaceableCount MAK'ta bulkReplaceBatch ile birebir DEĞİLDİ = bildirilen hatanın MAK'taki tekrarı; (c) üç korele EXISTS agregat FILTER içinde semi-join'e çevrilemiyordu → LATERAL+bool_or (coalesce ŞART: bool_or NULL döner, NOT NULL de NULL → atamasız kalemler dead_c'ye girmezdi); (d) geri çekme bandı ile onay modali farklı küme sayıyordu → `customerHeld` eklendi; (e) parti detayındaki iki tablo ayrı state tutuyordu → paylaşılan `refreshKey`. **KENDİ HATALARIM (raporlanıyor):** dev'de yanlış aracı (`deploy.sh`, prod içindir) çalıştırıp ayrı bir compose projesi yarattım — temizlendi, prod ETKİLENMEDİ; regex ile yaptığım bir düzenleme `reloadAfterMutation` içindeki `reload()` çağrısını kendisiyle değiştirip sonsuz özyineleme üretmişti (okuyarak yakalandı); sql şablonu içinde backtick kullanıp template'i erken kapattım; yeni testte `createSite(db, crypto, opts)` imzasını yanlış çağırdım (izole test DB'sinde 2 test düştü, düzeltildi). Migration YOK. | prod `/health` 200 v1.0.0 (db+redis) · typecheck 4/4 + check-use-server (21 dosya / 70 export) · admin production build · api birim 72/72 · **VPS izole test DB: entegrasyon 192/192** (+4 yeni parti-sayaç testi — bu sayaçların HİÇ testi yoktu, hatanın yaşama sebebi buydu) · **dev canlı E2E:** kullanıcının ekran görüntüsündeki partide ölçüldü — eski sayaç "6 satılmış" derken gerçekte müşteride **3** anahtar vardı; satırdan değişim yapıldı → müşteride 3→**2**, düşmüş 12→**13**, taze anahtar **başka partiden** geldi (o partide stok 1→0), eski anahtar karantinada · tarayıcıda CSSOM ölçümü: `.outline-none` → @layer utilities, global kural → KATMANSIZ (iddia kanıtlandı) |
| 2026-08-13 | cc3f7d8 | api + admin | **UX PARTİSİ: adım kilidi · katlanır formlar · ürün detayı sekmeler · TOPLU geçersiz kılma · PARTİ DETAYI** — kullanıcı 5 madde bildirdi. (1) Stok Girişi ürün seçilmeden 2. ve 3. adımı AÇMIYOR (`fieldset[disabled]` + kilit notu; eskiden yalnız soluklaşıyordu, tıklanabiliyordu). (2) "Yeni Tedarikçi" / "Yeni Admin" formları varsayılan KAPALI (yeni `CollapsiblePanel`; kapalıyken DOM'a hiç girmez). (3) Ürün detayı 7 kart → 4 SEKME (Envanter / Eşlemeler / Tedarik / Hareketler) + iki özet şeridi yan yana (yeni `ui/tabs.tsx`; içerikler SUNUCUDA render edilip prop olarak geçer). (4) Stok düzeltme mimarisi: bozuk anahtarı tek tek combobox'tan seçmek yerine ENVANTER TABLOSUNDA çoklu seçim + toplu aksiyon çubuğu + zorunlu sebep modali. API `licenseItemIds[]` (tek UPDATE, kalem başına ayrı `stock_adjustments` satırı → Karantina sebebi kalem bazında görünür), "hepsi ya da hiçbiri" DEĞİL: arada kapılan kalem atlanır ve `{requested, affected, skipped, qtyTotal}` ile dürüstçe raporlanır, hiçbiri işlenemezse 400. (5) YENİ `/batches/[id]` PARTİ DETAYI: partiye kilitli envanter (teslim edilenler + geçersiz kılınanlar dahil), parti dilinde başlıklar ("Bu partideki lisanslar", "Partiyi geri çek"), 4 sayaç + beyan/kayıt farkı uyarısı; `GET /v1/admin/batches/:id`; parti etiketi listede artık detaya link. **İKİ KENDİ-REGRESYONUM (ikisi de build'de görünmedi):** sunucu bileşeninden istemci bileşenine lucide BİLEŞENİ prop'u (`icon={Plus}`) React'i ÇALIŞMA ANINDA patlattı → /suppliers ve /admins hata sınırına düştü (typecheck + next build TERTEMİZ) → prop `ReactNode` (element) oldu. Ve `= ANY(${dizi}::uuid[])` drizzle şablonunda BOZUK SQL üretti (Postgres 42846) → parametreli `IN (...)`; bu tuzak projede daha önce KVKK anonymize yolunda yaşanmıştı, tekrarı 5 entegrasyon testiyle kilitlendi. Yeni `scripts/smoke-routes.sh`: 26 rotayı gezer ve YALNIZ HTTP koduna bakmaz — Next hata sınırı 200 döndüğü için gövdede `error.tsx` imzasını arar (önceki elle taramam dar bir metin kalıbı kullandığı için kırık /suppliers'ı TEMİZ raporlamıştı). | prod `/health` 200 v1.0.0 (db+redis) · typecheck 4/4 + check-use-server (21 dosya / 69 export) · admin production build · **VPS izole test DB: entegrasyon 188/188** (+5 yeni toplu-düzeltme testi) · dev: 26 rota + 4 detay sayfası — hata sınırı 0 · **dev canlı E2E:** toplu düşme 3 istendi → 2 etkilendi / 1 atlandı / 2 defter satırı, ikisi de Karantina'da sebebiyle listelendi; parti detayı 15 kalem (11 stokta · 3 teslim edilmiş · 1 geçersiz) |
| 2026-08-13 | 6ffdafa | api + admin | **DURUM RENGİ SİSTEMİ 3→5 HUE + /products 404 + tarama** (migration **0032**) — kullanıcı: "teslim edildi rozetlerinin özel rengi olmalı, hafif soluk renklerle, ilgili tüm yerlerde; /products boş 404; başka eksik ne var". Yeni token `--info` (mavi: teslim edildi/tamamlandı) + `--attention` (mor: İncelemede/Askıda); kontrast HESAPLANDI (oklch→sRGB→WCAG, %14 tint zemini): 5.29/5.67 açık, 6.13/5.86 koyu — AA üstü, sRGB içi. Rozet = soluk tint + saç teli `ring-inset`. Alert `info` gerçekten mavi oldu (sessiz gri kutu → `muted`). Ad-hoc küçük harfli rozetler cümle düzenine; `pasif` tonu birleştirildi; katalog "Eşlenmemiş" warning→outline; /orders kırpma bandı info→warning. **/products:** breadcrumb ölü linki → **middleware** 307 `/stock` (sayfa içi `redirect()` async root layout yüzünden meta-refresh'e düşüyordu — kök yoldaki tuzağın aynısı, dev'de ölçüldü). **0032:** `license_items.product_id` üzerinde koşulsuz index YOKTU (ikisi de kısmi `WHERE status='available'`) → ürün bazlı envanter tam tablo taramasıydı. **Ops:** takılı `pending` dağıtım temizliği `request()` yoluna da kondu (runner ölünce `claimNext` içindeki temizlik hiç çalışmıyor, kuyruk kalıcı tıkanıyordu) + `/deployments` uyarı bandı | prod `/health` 200 v1.0.0 · migration tracking 33 · `license_items_product_created_idx` canlı · typecheck 4/4 + check-use-server · admin build · **dev: 26 rotanın tamamı 200, hata sınırı 0** · tarayıcıda ölçüm: "Stokta" emerald vs "Teslim edildi" mavi (iki temada) · `/products` 307→`/stock`, `/products/<id>` etkilenmedi |
| 2026-08-13 | dafad0e | api | **LİSANS LİSTESİ: SATIR SATIR EN YENİ ÜSTTE** — kullanıcı ekran görüntüsüyle netleştirdi: bir girişte `test1, test2` yapıştırıldıysa listede **`test2` ÜSTTE** olmalı. Önceki yorum ("blok DESC ama blok İÇİ yapıştırma sırası ASC") yanlıştı; doğru kural envanterin bir AKIŞ olması. `listLicenseItems` → `created_at DESC, seq DESC` (`assigned_desc` de aynı yöne); karantina tie-break `seq DESC` (aab1701'de ASC'ye alınmıştı, geri alındı); `created_asc` tam ayna (`created_at ASC, seq ASC`). **TESLİMAT DEĞİŞMEDİ** ve bilinçli olarak TERS yönde: `assign.ts` FEFO → `created_at` → `seq ASC` (önce girilen önce teslim). **Migration YOK:** `(created_at DESC, seq DESC)` mevcut `license_items_created_asc_idx (created_at ASC, seq ASC)` btree'sinin GERİYE taramasıyla karşılanır — bu yüzden ORDER BY'da `NULLS LAST` YAZILMAZ (geriye tarama `DESC NULLS FIRST` üretir, `DESC`in SQL varsayılanı da odur; iki kolon da NOT NULL) | prod `/health` 200 v1.0.0 (db+redis) · **VPS izole test DB: entegrasyon 183/183** (5 sıra testinin beklentileri güncellendi; FIFO atama + müşteri `getDeliveries` testleri DEĞİŞMEDİ → ters yön kilitli) · api typecheck temiz · **dev canlı doğrulama:** liste `test2 → test1`, `SIRALI-TEST-10 → 01` |
| 2026-08-13 | aab1701 | api | ~~Karantina tie-break yönü~~ (dafad0e ile geri alındı — kullanıcı yönü netleştirdi) · orijinal not: **Karantina tie-break yönü** — kullanıcı sıra mantığını netleştirdi ("son eklenen GİRİŞ en üstte, ama 10 en sonda; teslimatta önce girilen gider"). Envanter/teslimat/mail/sipariş detayı bu kurala uyuyordu; karantina listesi eşit damgalı satırlarda `seq DESC` ile TEK İSTİSNAYDI → bir recall/void tüm partiyi aynı damgayla öldürdüğü için o parti operatörün girdiği listenin TERSİ sırada görünüyordu. `seq ASC` (sonraki JS sıralaması stabil → eşit `quarantinedAt` gruplarında SQL sırası korunur). Ctrl+K anahtar araması bilinçli `seq DESC` kaldı (blok listesi değil, sonek-hash araması; operatör son girdiğini arar) | prod `/health` 200 v1.0.0 (db+redis) · dev `/health` 200 · api typecheck temiz |
| 2026-08-13 | 6efc038 | api + admin | **LİSANS SIRASI: GİRİŞ SIRASI KORUNUR** (migration **0030+0031**) — kullanıcı: "sıralı bir stok eklemiştim ama listede karışık görünüyor; sonuncu eklenen hep en üstte, verdiğim liste gibi sıralı, sıra şaşmadan". **KÖK NEDEN (dev DB'de gerçek veriyle ÖLÇÜLDÜ):** bir içe aktarmanın TÜM satırları tek transaction'da yazılıyor → `created_at` = transaction damgası, hepsinde AYNI (15 satırlık giriş tek damga); tie-break ise rastgele `uuid v4` (`id`) → blok içi sıra keyfi. Aynı kusur ATAMA yolunda da vardı (`ORDER BY expires_at, created_at` — üçüncü anahtar yok) → aynı partiden hangi anahtarın teslim edileceği de rastgeleydi. **0030** `license_items.seq` (bigserial) + `license_items_created_idx (created_at DESC NULLS LAST, seq)`; **0031** `license_items_created_asc_idx (created_at ASC, seq)` — "En eski" sıralaması eski index'in TERS taramasıyla karşılanıyordu, yönler ayna olmadığı için ayrı index ŞART (yoksa büyük tabloda tam sort). Listeleme: `created_at DESC NULLS LAST, seq ASC` → **en yeni giriş üstte, blok İÇİNDE operatörün yapıştırdığı sıra**; `NULLS LAST` açıkça yazıldı (DESC varsayılanı NULLS FIRST, index NULLS LAST → yazılmazsa planlayıcı index'ten sıralayamaz). Atama (assign.ts ×2): `seq` ÜÇÜNCÜ anahtar → FEFO önceliği bozulmadan "önce yapıştırılan önce teslim edilir". **SEQ TEK BAŞINA YETMİYORDU** (çok-ajanlı keşif bulgusu): müşteri teslimatı (`getDeliveries`), teslimat maili ve admin sipariş detayı sorgularında ORDER BY **HİÇ YOKTU** → mail/My Account/panel farklı sıra gösterebiliyordu; üçüne de `ORDER BY licenseItems.seq` eklendi. **Çekişmeli doğrulama kendi değişikliğimde boşluk buldu:** WP meta box (`siteAdminView`) tek başına `deliveredAt DESC` kalmıştı (ters yön + tek teslimatta keyfi) → `deliveredAt DESC, seq ASC` (birincil anahtar korundu: kısmi teslimatta son parti üstte). Aynı sınıf belirsizlikler kapatıldı: karantina listesi (LIMIT'li ORDER BY tie-break'siz → hangi satırların pencereye gireceği keyfiydi), Ctrl+K anahtar araması (ORDER BY'sız LIMIT 10), toplu değiştirme adayları. UI: "En yeni giriş üstte" / "En eski giriş üstte". **BİLİNEN SINIR (dürüstlük):** migration ÖNCESİ satırların `seq`'i heap FİZİKSEL sırasıyla dolar; o satırlar UPDATE görmüş olabileceğinden ESKİ bloklarda sıra garanti DEĞİL — yapıştırma sırası daha önce hiçbir yere yazılmıyordu, geri kazanılamaz. Migration tabloyu yeniden yazar (volatile default); uygulandığında tablo küçüktü (prod 3, dev 22 satır — ölçüldü), SQL başına gelecekteki büyük tablolar için rewrite'sız reçete yazıldı | **prod /health 200 v1.0.0** (db+redis) · migration tracking 32 (0030+0031 canlı) · api ERROR 0 · typecheck 4/4 · **VPS izole test DB: entegrasyon 183/183 + yarış 3/3** (çifte atama 0; 5 yeni sıra testi: liste sırası · iki blok · ters sıra · FIFO atama · müşteri getDeliveries) · **dev canlı E2E:** 10 anahtarlık sıralı giriş API'den yapıldı → envanter listesi `SIRALI-TEST-01..10` BİREBİR sırada, altında önceki blok kendi sırasında |
| 2026-08-13 | 258ddc6 | admin | **İKİNCİ ONAY MODALİ (panel geneli) + ROZET DİLİ TEK KAYNAĞA** (kullanıcı: "stok girişinde 2. onay gereksin, modal + lisans listesi — eklenip eklenmediği anlaşılmıyor; başka yerlerde de teyit gerekiyorsa yapalım; /stock rozetleri /orders ile senkron değil"). **Yeni primitifler:** `ui/dialog.tsx` (Radix Dialog; Sheet=çalışma yüzeyi, Dialog=KARAR yüzeyi) + `ui/confirm.tsx` **`useConfirm()`** söz-tabanlı API (`await confirm({...})` → `window.confirm`'den birebir geçiş); `details` (serbest liste/özet), `tone:'danger'` (kırmızı onay + odak İPTAL'de başlar), `reason` (zorunlu/opsiyonel; textarea/text/**password**+minLength); dropdown'dan çağrılınca bir makro-görev gecikme (menü kapanışının odak geri-vermesi modalin odak tuzağıyla yarışmasın). **Stok girişi 2. onay:** "Onayla ve Dağıt" artık modal açar — ürün + kayıt sayısı (+MAK kullanım hakkı) + tedarik özeti + **girilecek kayıtların LİSTESİ** (ilk 20; hesap ürününde secret alanlar maskeli) + mükerrer/boş satır notu + **"bu giriş N bekleyen birimi HEMEN teslim eder"** uyarısı; gerçek gönderim gizli submit düğmesiyle (`name=dryRun value=false` yalnız submitter'dan gövdeye girer); sonuç ayrıca **toast** (imported=0 asla yeşil değil). **19 yerli tarayıcı kutusu → panel modali** (kod tabanında `window.confirm/prompt/alert` SIFIR): sipariş askıya al/iptal(gerekçe)/değiştir(gerekçe) · değişim onayla/reddet(gerekçe) · inceleme onayla/reddet · site askıya al/aktifleştir (2 yer) · HMAC secret yenile · bağlan kodu üret · eşleme kaldır (2) · şablon sil (2) · admin sil + parola sıfırla (MASKELİ alan; alert yerine kilitli düğme) · KVKK anonimleştir · bildirimleri okundu yap · görünüm kaydet. /review'daki elle yazılmış modal (portalsız, odak tuzağı yok) silinip paylaşılana taşındı (−85 satır). **Rozet dili:** /stock envanteri ikonsuz düz Badge + yerel sözlük kullanıyordu → `StatusBadge`; `available/reserved/assigned/depleted` paylaşılan haritaya eklendi · karantina `voided`'ı amber basıyordu (envanterde kırmızı) → tek kaynak · tedarik zincirinde **4 ayrı** rozet uygulaması çelişiyordu (`voided` karnede amber/ürün detayında kırmızı; `ordered` PO'da gri/ürün detayında amber; PO etiketleri sözlüğü atlayıp elle küçük harf + ikonsuz) → yeni **`SupplyStatusBadge`** (tek ton+ikon+sözlük) + PO facet etiketleri sözlükten · `SupportStatusBadge` → `StatusBadge`'e devredildi · `ui/badge.tsx`'e **TON KURALI** yazıldı (success=sağlıklı · warning=bekliyor · danger=ölü · neutral=kapanmış; aynı ton içinde ayrım İKON ile — yeni renk hue'su YOK, monokrom kimlik korunur). Ayrıca dropdown/popover/select/tooltip'e durum-kapılı giriş/**çıkış** animasyonu (Sheet/Dialog deseniyle hizalı). **DÜZELTİLEN İDDİA:** ara commit'te "kapanan menü görünmez tıklama ölü bölgesi bırakıyor" diye CANLI hata raporlanmıştı; kontrol denemesi bunu çürüttü — kullandığım tarayıcı paneli CSS animasyonlarını hiç koşturmuyor (sıfırdan 60 ms'lik animasyon bile `animationend` üretmedi), gerçek tarayıcıda hayalet katman KANITLANMADI (258ddc6 kaydı düzeltti). Migration YOK | **prod /health 200 v1.0.0** (db+redis ok) · admin ERROR 0 · /stock /orders /sites /purchase-orders 307 (auth) · typecheck 4/4 + check-use-server · admin production build (/stock/import 23,4 kB) · **dev canlı E2E:** onay modali açıldı (ürün+3 anahtarın LİSTESİ+partisiz uyarısı), odak modal içinde "Vazgeç"te, 6 kardeş öğe aria-hidden, onayla → 3 kayıt girdi + toast "3 kayıt envantere girdi" · rozet senkronu ÖLÇÜLDÜ: /stock "Teslim edildi" `oklch(0.696 0.17 162.48)`+ikon = /orders ile BİREBİR aynı; /purchase-orders "Teslim alındı" yeşil+ikon (eskiden ikonsuz küçük harf) · dropdown→modal akışı (menü öğesinden onay) çalıştı |
| 2026-08-13 | 9f73221 | admin | **GÜN-BAZLI PARTİ ETİKETİ + CANLI SATIR/LİSANS SAYACI + SINIR GÖRÜNÜRLÜĞÜ** (kullanıcı: "parti etiketi gün de içerebilir ayırt etme konusunda; anahtar yapıştırma ekranında satır sınırı var mı, maks belirtilebilir; yapıştırılınca kaç ürün/lisans olduğunu hesaplayıp kenarda gösterebilir yanlış giriş olmaması açısından"). Parti etiketi `YYYY-MM-<HARF>` → **`YYYY-MM-DD-<HARF>`** (2026-08-13-A): etiket kendi başına ayırt edici, harf yalnız AYNI GÜN içindeki 2./3. girişi ayırır; eski ay-bazlı etiketler farklı desen (çakışmaz, diziyi kaydırmaz). Yeni **EntryMeter**: girdi alanının HEMEN ALTINDA canlı şerit — kayıt sayısı, atlanan boş satır, mükerrer görünen, `N / 10.000 satır`, `X KB / 700 KB` (%90'da uyarı rengi); sınır ayrıca bölüm açıklaması + alan yardımında (eskiden yalnız AŞILDIĞINDA görünüyordu). **3 gerçek kusur:** (1) MAK/çok kullanımlıkta anahtar sayısı BİRİM talebiyle kıyaslanıyordu → "N bekleyen birimi tamamlar" olduğundan AZ görünüyordu (1 anahtar × 500 kullanım = 500 birim); sayaç + bekleyen etkisi artık kapasite üzerinden ("3 anahtar = 1.500 kullanım hakkı") + özet rayına "Kullanım hakkı" satırı; (2) görünmez karakter denetimi yalnız hesap tablosundaydı — düz anahtar yapıştırmasında YOKTU (`trim()` yalnız uçları alır; anahtarın ORTASINDAKİ sıfır-genişlik karakter sessizce teslim edilir + hash'i değiştirip mükerrer kontrolünü kaçırır) → sayılır + tek tık temizlenir; (3) hesap TABLO modunda yapıştırma satır tavanı yoktu (hücre başına bir `<input>`; binlerce satırlık Excel bloğu sekmeyi kilitler, gövde sınırı render'dan SONRA görünür) → tavan 500 satır, sessiz kırpma yok. 1 KB altı boyut ham bayt. /guide: sınırlar + etiket biçimi + MAK örneği. Migration YOK | **prod /health 200 v1.0.0** (db+redis ok) · admin ERROR 0 · `/stock/import` 307 (auth) · typecheck 4/4 + check-use-server (21/68) · admin production build (21,2 kB) · autoBatchLabel 10/10 davranış testi (derlenmiş parse.js) · **dev canlı E2E:** 5 satırlık yapıştırmada sayaç "5 anahtar · 1 boş satır atlandı · 1 satır birbirinin aynısı · 5/10.000", sıfır-genişlik karakterli satır yakalandı → temizle düğmesi 0'a indirdi (boş satır korundu), etiket `2026-08-13-A` ön-dolu → tarih 04.11 → `2026-11-04-A`, gerçek giriş 2 anahtar + parti `2026-08-13-A` (13.08.2026) oluştu → aynı gün ikinci giriş `-B` önerdi (test verisi sonra silindi) |
| 2026-08-13 | 76deee7 | api + admin | **STOK GİRİŞİ UX CİLASI + TÜRKÇE ARAMA DÜZELTMESİ** (kullanıcı ekran görüntüleriyle: "düzen sıralama kolaylaştırılabilir, parti etiketi tarihe göre otomatik oluşabilir, search inputlardaki tasarım sorunlarını düzelt, başka yerlerde sorun/bağlantı varsa hallet"). 3 işçi + çekişmeli doğrulayıcı. **[GERÇEK KUSUR] Combobox süzgeci düz `toLowerCase()` kullanıyordu; projede tam bu iş için yazılmış `includesTr` YOKSAYILMIŞTI** → "ANAHTARI" yazınca "Anahtarı" kaydı BULUNAMIYORDU (hata yok, sessiz boş liste); /mappings · /customers · satın alma emri · stok girişi · stok düzeltme dahil TÜM comboboxlar etkileniyordu. **İki farklı arama görünümü hizalandı** (SearchInput kenarlıklı+×+Escape ↔ CommandInput kenarlıksız/temizlemesiz): aynı ikon, aynı (×) düğmesi, `bg-muted/40` ayrım, min/max genişlik, "N sonuç" şeridi + aria-live, boşta "X için sonuç yok". **Stok girişi 2. bölüm:** üç dev radyo kartı → kompakt segment (role=radiogroup, ok tuşları/Home/End, roving tabindex) ~200px→34px; **parti etiketi ARTIK OTOMATİK** (alım tarihinden `YYYY-MM-<HARF>`, o ürünün aynı ayındaki kullanılmayan ilk harf; dokununca donar + "Otomatik" geri-dönüş düğmesi) → alan artık boş/kırmızı-zorunlu başlamıyor; "Tedarikçi listede yok" onay kutusu alanın İÇİNE taşındı (değer kaybolmuyor); alan sırası kim/ne zaman → ne kadar → kimlik/not; maliyette para birimi öneki + canlı toplam alanın altında. **Panel temizliği:** /guide yeni akışa göre (hızlı giriş vs tedarikli giriş), /batches + /purchase-orders + /suppliers açıklamaları, otomatik parti/emirlerde ham `[oto-giris]` yerine "Otomatik" rozeti, create-supplier-form Field primitifine geçti. **Çekişmeli doğrulama 5 bulgu (hepsi kapatıldı):** Escape guard'ı odak (×) düğmesindeyken popover'ı KİLİTLİYORDU → guard tamamen kaldırıldı (popover içinde Escape her zaman kapatır; sayfa içi SearchInput'ta temizlemeye devam) · "N sonuç" allowClear satırını saymıyordu · ürün değişince `labelTouched` sıfırlanmıyordu (etiket diğer ürüne taşınıyordu) · tarih boşalınca "Otomatik'e basın" diyordu ama düğme yoktu (bugüne düşülür) · `aria-autocomplete` düz butondaydı. Migration YOK | **prod /health 200 v1.0.0** · admin hata 0 · typecheck 4/4 + check-use-server · admin production build · ölü iç link taraması TEMİZ · ham `[oto-giris]` yalnız kod yorumunda · **dev canlı kanıt:** "ANAHTARI" araması 2 sonuç (eskiden 0), (×) düğmesi + `bg-muted/40` + "2 sonuç" sayacı, segment 34px, etiket `2026-08-A` ön-dolu → tarih 04.11 → `2026-11-A`, elle yazınca DONDU |
| 2026-08-13 | c202636 | api + admin | **STOK GİRİŞİ YENİDEN TASARIMI — tek ekranda tedarikçi/tarih/maliyet** (kullanıcı: "Key/Stok hesap İçe Aktar karışık; eklerken hangi tarihin partisi olduğunu, tedarikçiyi vs seçmek mantıklı değil mi"). **ÖLÇÜLDÜ:** bu bilgiyi girmek 4 ekran/6 adım sürüyordu (Tedarikçi → Satın Alma Emri → Teslim Al → Partiler → ürün detayı → yapıştır) ve **alım tarihi hiçbir formda girilemiyordu** (`batches.received_at = now()`); parti yalnız SEÇİLEBİLİYOR, oluşturulamıyordu. Yeni **/stock/import** tek sayfa (Ürün · Tedarik bilgisi katlanır · Anahtarlar + canlı önizleme rayı): tedarikçi (listede yoksa adıyla oluşturulur) + alım tarihi + parti etiketi + birim maliyet AYNI istekte gider, API **tek transaction**'da 'received' satın alma emri + partiyi açar. Hesap ürünlerinde **sütunlu tablo** (Excel yapıştırma, ayraç otomatik) varsayılan, ham JSON "Gelişmiş" sekmesinde; **.txt/.csv dosya** (tarayıcıda okunur). **Kararlar:** parti adedi = GERÇEKTEN girilen kayıt (beyan değil — mükerrerler önceki partide sayıldı, tekrar saymak harcamayı ÇİFT gösterirdi) · auto-PO `ordered_at=NULL` (avgLeadDays KPI'ı sıfıra çekilmesin) · tedarikçisiz+maliyetli → 400 · hepsi mükerrer → 409 + tam rollback (hayalet emir yok). **GÜVENLİK:** Excel yapıştırma `serializeAccountPayload'ı` trim etmiyor + şema-dışı sütunu sessizce atıyordu → NBSP/akıllı tırnaklı **parola müşteriye YANLIŞ giderdi** (panelde maskeli, görülemez) ve hash farklılaşıp dedupe düşerdi → `normalizeFieldValue` + bilinmeyen-anahtar reddi (mesaj DEĞERİ taşımaz). **Yol boyunca:** pino redact `items[*].payload'ı` kapsamıyordu (anahtar loga sızabilirdi) · import sonrası /batches+/purchase-orders revalidate edilmiyordu · preview actor geçirmiyordu · PO birim maliyeti int4 taşmasıyla 500 veriyordu · `cleanupByTag` batches/PO/suppliers'a dokunmuyordu · sidebar aktif öğe düz prefix (iki öğe birden aktif) + breadcrumb ham "import". Migration YOK | **prod /health 200 v1.0.0** · 136 rota, boot hatası 0 · typecheck 4/4 + check-use-server (21/68) · shared 34/34 (mutasyonla kanıtlı) · api birim 72/72 · admin build (/stock/import 19 kB) · **VPS izole test DB entegrasyon 178/178 (+18 yeni) + yarış 3/3** · **dev E2E gerçek veriyle:** ürün seç → yeni tedarikçi "Acme" + tarih 12.08.2026 + etiket + 12,50 ₺ + 4 satır (1 mükerrer) → kuru çalıştırma "3 kabul/1 mükerrer, parti OLUŞTURULMADI" (DB: 0 yazım) → gerçek giriş: PO received qty 3 **ordered_at NULL** received_at 12.08, parti 3 adet, 3 lisansta 1250 kuruş snapshot, **byMonth 2026-08 = 37,50 ₺**, valuation kapsanamayan **0**, /purchase-orders "Otomatik" rozeti · disk %16 |
| 2026-08-12 | 1332861 | api + admin | **UI/UX BAĞLAM DENETİMİ — 36 bulgu** (kullanıcı: "/pending'de hangi site olduğu belirtilmiyor; başka yerlerde de UI/UX eksikleri varsa tespit edip düzelt"). 5 gruplu tarama + çekişmeli doğrulama (45 ajan): 56 ham → **36 CONFIRMED** (3 kozmetik, 1 çürütüldü) → 4 ayrık-dosya işçi düzeltti. **Şikâyet:** `pending()`/`list()` yalnız orders kolonlarını dönüyordu (site = okunamaz UUID), tabloda kolon yoktu → API+UI: sites leftJoin (siteDomain/siteType, sır kolonu YOK) + /pending & /orders'a **Site** kolonu + veriden türeyen site faceti + aramaya site dahil. **En kritik ek bulgu:** İNCELEMEDEKİ (held) sipariş kuyrukta sıradan "Bekliyor" görünüyordu → operatör stok girer, autoComplete held'i ATLAR, sipariş çürürdü; held bayrağı artık ham durumu EZER (rozet+sebep+/review linki+uyarı bandı, /orders facet'i). **[YÜKSEK]** /sites/new sihirbaz çıkmazı (kod üretilemeyince kurtarma yolu yok, var olmayan butona yönlendiriyordu). Ayrıca: /pending "Ürün / eksik adet" + stok bekleyen talep şeridi (tek korelasyonlu alt sorgu, N+1 yok) · /orders 200-kayıt penceresi dürüstleşti · /review'de siparişe/müşteriye link + "Onayla" sonucu gerçek durumu raporluyor · stok düzeltme formu stoku değiştirmeden yeşil "Eklendi" demiyor · /stock "Satıldığı siteler" + eşleme faceti · /support'a site+ürün · /customers/[email] site+tıklanabilir talepler · /security admin-auth Türkçe+facet+kırpılma · /notifications+/mappings ham enum temizliği · /deployments log görünürlüğü+otomatik yenileme · terim birliği (Ctrl+K "Tamamlandı" → "Teslim edildi"; kendi ürettiğim "Mağaza" → panelin yerleşik "Site" terimine hizalandı). Migration YOK | **prod /health 200 v1.0.0** · api+admin healthy · typecheck 4/4 + check-use-server (21/67) · api birim 72/72 · admin production build · **VPS izole test DB entegrasyon 160/160 + yarış 3/3** · **dev E2E:** 21 rota 200 (hata sınırı YOK) · /pending+/orders Site kolonu gerçek 3 mağazayla doğrulandı · **held rozeti canlı kanıtlandı** (gerçek sipariş incelemeye alındı → /pending "İncelemede"+sebep+uyarı bandı, /orders rozet, /review'de linkli satır → geri alındı) · disk temizliği %15 |
| 2026-08-12 | 6d24ea9 | api + admin + eklenti v1.0.3 | **WP SENKRON KESİNTİSİ (kök neden: kendi W1 düzeltmem) + yazılımsal denetim düzeltmeleri.** Kullanıcı raporu: dev'de atanan lisanslar WP'de görünmüyor, "Lisans bilgileriniz şu an görüntülenemiyor". ÖLÇÜLDÜ: WP→`http://api:3001/v1/health` 200 (ağ sağlam) ama dev API'de 24 saatte SIFIR site-facing istek → `is_secure_panel_url()` http'yi yalnız localhost'a izin verdiği için Docker İÇ servis adı (`api`) reddediliyor, eklenti isteği HİÇ yapmıyordu (code=0); sipariş push/iade/katalog senkronu da sessizce durmuştu. **DÜZELTME:** guard artık https + kanıtlanabilir ÖZEL adres (loopback · tek-etiketli Docker servis adı · 10/172.16/192.168/169.254 · IPv6 ULA · .local/.internal/.test) kabul eder; GERÇEK alan adı + http HÂLÂ RED (10-vaka matrisi) + blok artık GÖRÜNÜR (admin uyarısı). **Yazılımsal denetim maddeleri:** sessiz-mail guard (`MailConfigGuardService` — üretimde dev-sink SMTP'de kritik alarm) · docker-compose'un GEÇİRMEDİĞİ env'ler (admin REQUIRE_AUTH+TZ+APP_VERSION; api HMAC_IP_FAIL_LIMIT/AUTOCOMPLETE_INLINE_CAP/7×RETENTION_*/RECONCILE_*/SWEEP_ALARM_*) · log rotasyonu (10m×5, tüm servisler) · api+admin healthcheck · deploy.sh disk temizliği · ADMIN-ONLY `/v1/admin/system/status` (yalnız boolean) → /settings'te Telegram "hep kapalı" ve sürüm "0.0.0" yanlışları düzeldi · CI'a WP PHP-lint + migration drift denetimi. **Ayrıca:** Next standalone `HOSTNAME` Docker'da konteyner ID'sine ayarlandığı için admin loopback'e bind OLMUYORDU (healthcheck ortaya çıkardı) → `HOSTNAME=0.0.0.0`. Migration YOK | **prod /v1/health 200 v1.0.0** · api+admin **healthy** (yeni healthcheck) · **mail guard prod'da ATEŞLEDİ** → gerçek yapılandırma hatası bulundu (prod SMTP_HOST tanımsız→mailpit; kritik bildirim DB'de) · REQUIRE_AUTH=true artık konteynerde etkin, panel erişilebilir (307 login) · TZ=Europe/Istanbul · **disk %57→%13 dolu (64 GB geri kazanıldı)** · typecheck 4/4 · api birim **72/72** · VPS izole test DB **entegrasyon 160/160 + yarış 3/3** · PHP-lint 12/12 · guard matrisi 10/10 · dev E2E: WP→panel HMAC 200, metabox admin-view 200 (1 atama), My Account deliveries 200 (gerçek anahtar) |
| 2026-08-02 | 86dcc22 | api + admin | **DERİN-DENETİM DÜZELTMELERİ** (kullanıcı: "tüm eksikleri ve sorunları gider; stabil/güvenli/performanslı"). Derin-denetim bulgularının gerçek+güvenli olanları düzeltildi + 9-ajanlı adversaryel doğrulama workflow'u (her düzeltmeyi çürütmeye çalış + kapsam taraması → H1×2/H2/M1/bundleQty SOUND; M2/M3 CONCERN + sweep 1 HIGH). **[H1] iade yolları ÜÇÜ de** yalnız status='active' geri alıyordu → ASKIDAKİ (suspended) atama iadede/adet-düşürde CANLI kalıp "Geri aç" ile bedava lisans üretiyordu: `revokeOrderForSite` (tam iade) + `syncRefunds` (kısmi iade) + `revokeExcess` (adet-düşür, sweep bulgusu) aday kümesi active+suspended; `revokePartialUnits` guard suspended kabul; syncRefunds else-branch gerçek dönüşü sayar (over-count savunması). §2 korunur (MAK iadede kapasite dönmez, satır canceled). **[H2]** `apiRaw` oturum rolünü DÜŞÜRÜYORDU → envanter reveal-gate'i owner-olmayan admine düz-metin gösterebilirdi (canRevealPlaintext('')=true) → getSessionRole() ile iletir (latent: ikincil admin yok). **[M1]** karantina listesi rol'e bakmadan TAM düz anahtar döndürüyordu → A1 kararıyla rol-farkında maske (owner düz, owner-olmayan maskeli; reveal audit yalnız düz-metinde). **[M2]** readonly-sql (§15, AI KAPALI) superuser rolünde pg_read_file/dblink/lo_*/adminpack dosya-ağ + pg_authid/pg_shadow parola-hash okuyabiliyordu → tehlikeli-fonksiyon + sistem-katalog + parola-kolon denylist (savunma-derinliği). **[M3]** KVKK anonymize serbest-metin (reason/resolution_note/message body + subject/detail) atlıyordu VE farklı-kasada PII bırakıyordu → case-insensitive regexp_replace('gi') + replacement_messages JOIN (drizzle `ANY(${arr})` bozuk SQL'i düzeltildi). **[LOW]** bundleQty .positive()→.max(1000). Migration YOK | **prod /v1/health 200 v1.0.0** (db+redis) · typecheck 4/4 + check-use-server 66 export temiz · api birim 65/65 · admin production build · **VPS izole test DB: entegrasyon 157/157 + yarış 3/3** (yeni: h1 suspended-refund, revokeExcess suspended, quarantine-mask, readonly-sql M2 vektörleri, anonymize M3 case-insensitive) · adversaryel doğrulama workflow (9 ajan) SOUND + 3 follow-up bulgu deploy-ÖNCESİ kapatıldı · deploy.sh rollback'li (prod + dev, ikisi de 200) |
| 2026-08-02 | 5efffec | api + admin + eklenti v1.0.1 | ODAKLI DENETİM (admin yönetimi + WP eklentisi + WooCommerce; 5-lens çekişmeli workflow) → 12 bulgu (1 çürütüldü) + 2 KULLANICI KARARI. **[A1]** owner-only düz-metin (kullanıcı kararı): sipariş detayı + lisans envanteri + ürün detayı düz lisans/parola YALNIZ owner'a; owner-olmayan 'admin' MASKELİ (getDeliveries disiplini) — AdminRole/canRevealPlaintext + detail()/listLicenseItems reveal param; reveal audit yalnız gerçek düz-metinde. **[A3]** reveal + KVKK anonymize uçlarına API OwnerGuard. **[A2]** görüntüleme audit'i gerçek admin'e (apiGet actor+rol iletir). **[A4]** admin auth/CRUD (login/başarısız-login/create/disable/reset/remove) → security_events (brute-force görünür). **[C2]** §2 invaryantı (kullanıcı kararı): MAK/multi İADE'de kapasite havuza DÖNMEZ (returnMultiCapacity; revokeOrderForSite + syncRefunds false; değişim/adet-düşür true) — sessiz aşırı-satış kapandı. **[C1]** WP silinen ürün satırı push+iade'de atlanmaz (order-item ID). **[C3]** reconcile yeni-kalem görünür uyarı. **[C5]** resolveBundleQty tipi. **[B1]** order_key misafir-only. **[B3]** plugin_info URL doğrulama. Migration YOK | /health 200 v1.0.0 (db+redis) · typecheck 4/4 · api birim 65/65 · VPS izole test DB **entegrasyon 151/151 + yarış 3/3** (+2 C2 MAK-iade + güncellenen sync-refunds) · PHP-lint 12/12 · deploy.sh rollback'li (prod+dev) |
| 2026-08-02 | ac4e10c | api + admin + eklenti v1.0.1 | GÜVENLİK DENETİMİ (7-lens harici güvenlik skill workflow: VoltAgent/awesome-agent-skills + alirezarezvani/claude-skills → 14 bulgu düzeltildi + sıfırdan re-audit): **[HIGH]** releases publishRelease owner-guard eksikti → owner-olmayan admin tüm müşteri sitelerine keyfi eklenti .zip yayınlayabiliyordu (tedarik-zinciri RCE) + API OwnerGuard (savunma-derinliği); **[MED]** reconcileOrder advisory-lock+tek-tx (eşzamanlı qty-azalt re-push'ta over-revoke/canlı-lisans yanması); readonly-sql composite→text cast bypass (CAST_TO_SCALAR_RE — re-audit SELF-REGRESYONU yakaladı: tanımlıydı ama runSelect'e bağlanmamıştı); CreateOrder DTO üst sınırları (DoS); onboarding webhook SSRF host daraltma; müşteri e-postası PII log maskeleme (KVKK); SESSION_SECRET/ADMIN_TOKEN min-24 fail-closed + REQUIRE_AUTH opt-in; logout tokenVersion iptal; Docker non-root (USER node); .dockerignore özyinelemeli; next→15.5.22 nodemailer→8.0.4; WP updater HTTPS+paket-URL doğrulama (MITM RCE) + panel-client https; bulkReplaceBatch atomikliği | /health 200 v1.0.0 (db+redis) · typecheck 4/4 · api birim 61/61 · VPS izole test DB **entegrasyon 149/149 + yarış 3/3** · PHP-lint 12/12 · deploy.sh rollback'li (prod+dev) · eklenti v1.0.1 |
| 2026-07-30 | ed3850c | api + Redis config | **Geleceğe-hazırlık: dayanıklılık + retention + performans** (kullanıcı: "tüm eksikleri düzelt, performanslı+güvenli, sistem asla sorun yaratmamalı, stres testi + tablo"). 41-öngörülü arıza-modu taraması + fault-injection ile bulunan sistemik açıklar kapatıldı. **Kök tema: hiçbir katmanda zaman aşımı yoktu** → backing servis degrade olunca istekler askıda kalıyordu (ÖLÇÜLDÜ: Redis donunca 12sn, PG kilidinde 50sn askı, /health bile yanıtsız). **A (dayanıklılık):** Redis fail-fast (commandTimeout 2s + enableOfflineQueue false); HMAC nonce Redis-DOWN'da fail-CLOSED-FAST (503, askı yok); rate-limit fail-OPEN; health Promise.race 2s → hızlı degraded; Postgres statement_timeout 30s + lock_timeout 10s + idle_in_transaction 60s + connect_timeout 10s; Fastify requestTimeout 30s; Redis maxmemory 768mb+noeviction. HMAC uçlarına IP başarısızlık-tavanı (**yalnız auth-FAIL sayılır** — meşru mağaza asla kısıtlanmaz; stres testi eski "tüm istekleri say" tasarımının yoğun mağazayı 429'ladığını gösterdi → düzeltildi). **B (retention, migration 0029):** RetentionService günlük (fulfillment_events 180g, outbox-delivered 30g, security 365g, email_log 365g PII maskele+730g sil KVKK, audit auto-reveal gürültüsü 90g); reconcile 30g pencere; sweep hata alarmı. **C (perf):** stok import autoComplete cap 200 inline + kalanı arka plana (BullMQ). **Fault-injection KANITI (dev):** Redis-down artık 12sn askı yerine **503/4.5sn** + health hızlı degraded; PG-kilit 50sn askı yerine **500/10.5sn + /health 18ms** (cascade bitti) | **prod /v1/health 200 v1.0.0** (db+redis) · migration 0029 (tracking 30) · boot ERROR 0 · 0029 indeksleri canlı · retention ucu smoke OK · typecheck+build temiz · **api birim 56/56 · entegrasyon 145/145 · yarış 3/3** (çifte-satış=0 korundu) · **stres (dev, güncel veri): okuma 652 istek/sn @300VU 0 hata / yazma no-wedge, meşru trafik 429 yemiyor** · deploy.sh rollback'li · Redis maxmemory 768mb canlı |
| 2026-07-29 | 5a50809 | api + admin + eklenti v1.0.0 | **Panelden kaynaktan sürüm yayınlama + sitelerdeki kurulu sürüm görünürlüğü** (kullanıcı: "sürüm ve dağıtımı sen gerçekleştir, panelde güncellemeler çalışmıyor gibi"). Teşhis: `/releases` + `/deployments` kırık DEĞİLDİ ama panelden uçtan uca kullanılamıyordu — `/releases` elde hazır `.zip` istiyordu (panelde paket üretilemez), `/deployments` yalnız push edilmiş kodu canlıya alıyordu; ayrıca hangi mağazanın hangi eklenti sürümünü çalıştırdığı panelde HİÇ görünmüyordu. **Yeni birincil akış:** `/releases` → owner-only "Kaynaktan yayınla" → panel yalnız istek kaydeder (`deployments` kuyruğu, `target='plugin'`) → host runner `scripts/publish-plugin.sh` ile repo HEAD'inden paketleyip yayınlar (panel konteynerine Docker/git yazma yetkisi YOK). `publish-plugin.sh` VPS'te **commit atmaz/push gerektirmez** — prod checkout'unda git kimliği ve kimlik bilgisi YOK (ölçüldü: `user.email` boş, push "could not read Username") ve yerel commit origin'den ayırıp sonraki `deploy.sh`'ın `git pull --ff-only` adımını KALICI kırardı. Sürüm kodda tanımlıdır (formda girilmez) → "yayınlanan zip = HEAD". Elle .zip "gelişmiş" altına indi. **Görünürlük:** eklenti v1.0.0+ her imzalı istekte `X-Wpteslimat-Version` gönderir; panel değiştiğinde site kaydına yazar (başlık İMZA KAPSAMI DIŞINDA — yalnız gösterim, yetki kararı verilmez). **migration 0028** (additive): `sites.plugin_version`/`plugin_version_at`, `deployments.note`. Ayrıca eklenti güncelleyicisi artık "yayın yok" (200 `{}`) ile "panele erişilemedi"yi ayırır (ikisi de 15dk negatif önbellek yazıyordu) | **prod /v1/health 200 v1.0.0** (db+redis) · migration 0028 uygulandı (tracking 29) · api+admin boot ERROR **0** · typecheck 4/4 + `check-use-server` 20 dosya/66 export temiz · api birim 56/56 · admin production build ✓ · **VPS izole test DB: entegrasyon 135/135** (4 yeni plugin-target testi) · PHP-lint 12/12 (VPS php:8.2) · **panel yolundan v1.0.0 yayını UÇTAN UCA kanıtlandı**: `POST /v1/admin/deployments {target:'plugin'}` → cron runner claim → `publish-plugin.sh` → success (42 sn, sha 5a50809) → public update ucu **v1.0.0** + zip indirilebilir (94.717 bayt) · **dev E2E:** sentetik daha yeni sürüm → WP "GUNCELLEME VAR → 1.0.1" (güncelleme zinciri gerçekten çalışıyor, sonra temizlendi) + katalog senkronu sonrası dev panelde `plugin_version=1.0.0` göründü |
| 2026-07-28 | ee59e14 | api + admin + eklenti v0.9.1 | **Sistem geneli tarama** (kullanıcı: "agent/işçi/workflow ile genel tara, bul-analiz-düzelt"). 8 lensli keşif (mail/kuyruk · auth/RBAC · stok/tedarik · müşteri/destek/güvenlik · rapor/AI/SQL · admin UI · WP eklentisi · altyapı/şema) + 4 çekişmeli doğrulayıcı → **54 ham, 35 CONFIRMED** (19 çürütüldü). 8 işçi düzeltti → 2 lensli re-doğrulama **6 KENDİ-regresyon** buldu → 4 işçi kapattı → 3 işçi glue bağladı. **migration 0027** (IDEMPOTENT, mevcut kurulumlarda tam no-op): drizzle snapshot 0020'de kalmıştı, `db:generate` 0021-0026'yı yeniden yaratan migration üretiyordu — prod'a gitseydi **API BOOT ETMEZDİ**; snapshot hizalandı + 0025'in elle yazılan 5 index'i şemaya taşındı. **[YÜKSEK]** /purchase-orders + /suppliers server action'ları CANLIDA KIRIKTI ('use server' obje re-export'u; 9b81c9b'nin tekrarı, `next build` YAKALAMAZ) → düzeltildi + tip-tabanlı `scripts/check-use-server.js` `pnpm typecheck`'e bağlandı. **[ORTA]** QueueModule REDIS_URL parola/TLS düşürüyordu (parolalı Redis'te tüm mail/webhook/sweep sessizce ölür, /health yine "ok") · /api/login hız-sınırsız + senkron scrypt event loop'u bloklar + kilit farklı identifier ile atlanır · usageMode multi→single MAK kapasitesini yok ediyordu · değişim onayı atomik değildi (added=0'da eski anahtar karantinada = müşteri lisans kaybı) · KVKK anonimleştirme security_events'i atlıyordu · maliyet raporu MAK'ta kapasite×anahtar-maliyeti çarpıyordu · deploy-runner kendi kilidini alamıyordu (panelden dağıtım hiç koşmuyordu) · WP klon koruma tabanı güncellemede kurulmuyordu · DataTable araması Türkçe İ/I'da sonuç bulamıyordu · "available" tanımı 11 sayım noktasında atama sorgusundan farklıydı (var olmayan stok). **Kendi-regresyonlar:** bildirim mailleri kuyruğa taşınıp tüketicisi eklenmemişti (HİÇ gitmiyordu) · `allocatableCountForLine` süre koşulunu atlıyordu (sonsuz "tekrar deneyin") · 0027 journal zaman damgası geriye gidiyordu (hiç uygulanmazdı) | **prod /v1/health 200 v1.0.0** (db+redis) · migration 0027 uygulandı (tracking 28) · boot ERROR 0 · admin async-export hatası 0 · typecheck api+admin+shared temiz · admin production build ✓ · **api birim 56/56** (3 yeni mail-processor testi) · `check-use-server` 20 dosya/65 export TEMİZ (negatif kontrolle doğrulandı) · **VPS izole test DB: entegrasyon 131/131 + yarış 3/3** · PHP-lint temiz · `bash -n` betikler temiz · dev smoke: /purchase-orders + /suppliers **200** (önceden kırık), dev admin+api ERROR 0 · **`db:generate` artık "No schema changes"** · eklenti v0.9.1 publish 201 · deploy.sh rollback'li |
| 2026-07-28 | d18e442 | api + admin + eklenti v0.9.0 | **Kullanıcının 4 şikâyeti** (eşlenmemiş sipariş görünmüyor · mağaza-panel gecikmesi · karantina süzme/indirme · "mağazada aç" URL'i yanlış). Kök nedenler dev'de GERÇEK veriyle ÖLÇÜLDÜ. 3 dalga paralel işçi + 5 lensli çekişmeli denetim (23 ajan → 32 doğrulanmış bulgu, hepsi kapatıldı). **migration 0026** (additive) `sites.admin_order_url_template_manual` = şablon kaynağı. **(1)** Kayıp sipariş `wc-on-hold` (havale) idi → hiç push edilmiyordu; ödenmemiş sipariş yine teslim EDİLMEZ (§2) ama mağaza listesinde "Panele iletilmedi — ödeme bekleniyor" etiketi+filtresi. `/v1/admin/pending` artık `unmapped` da döndürür (ayrı limitler) + satır başına `hasUnmappedLine`. `unmappedOrders` SATIR-tabanlı oldu (çok kalemli siparişte tek eşlemesiz kalem artık kaçmıyor); pano alarmı gerçek talepten türetiliyor, katalog sayacı BİLGİ (varyasyon ebeveyni hariç). **(2)** Gecikme ölçüldü **41 sn → 0,6 sn**: iş artık yanıt gönderildikten sonra aynı istekte koşuyor (fastcgi/litespeed; ikisi de yoksa sınırlı satır-içi), AS güvenlik ağı korunuyor; REST dışlanmıyor (Store API blok checkout); atomik iş kilidi + kuyruktaki ikizin iptali. **(3)** Karantina: tarih süzgeci SQL'e taşındı, `truncated` ham satır sayısından, indirme kapsam+biçim seçimli, "İç denetim" CSV'sine de KVKK uyarısı. **(4)** `storeAdminUrl` webhook iç hostname'inden türetiliyordu (`http://wordpress/...`) + HPOS tahmini → artık YALNIZ eklentinin bildirdiği şablon; yoksa link YOK. Ayrıca `syncRefunds` advisory-lock + tek tx (kilitsiz RMW → fazla revoke = canlı anahtar kaybı) | **prod /v1/health 200 v1.0.0** (db+redis) · migration 0026 uygulandı (tracking 27, kolon canlı) · boot ERROR 0 · admin 307 (auth) · typecheck api+admin+shared temiz · admin build · api birim **45/45** (14'ü yeni store-admin-url — eski dosya hiçbir config'e girmiyordu, hiç koşmuyordu) · **VPS izole test DB: entegrasyon 124/124 + yarış 3/3** · PHP-lint **12/12** · **dev E2E ölçümlü**: mod_php'de 41 sn → **0,6 sn** (sipariş fulfilled) · unmapped sipariş `/pending`'de `hasUnmappedLine:true`, üç sayaç tutarlı (1/1/1) · storeAdminUrl önce `null` (yanlış link YOK) → katalog senkronundan sonra `https://dev-wp…/wp-admin/admin.php?page=wc-orders&action=edit&id=24` (status `accepted`) · eklenti v0.9.0 publish 201 · deploy.sh rollback'li |
| 2026-07-28 | 73062fe | api + admin + eklenti v0.8.0 | **İş istasyonu partisi** (kullanıcının 13 maddelik listesi) + **5-lensli çekişmeli denetim** (32 ajan → 23 doğrulanmış bulgu, hepsi düzeltildi). **migration 0024** (notifications.read_at + kısmi index, sites.admin_order_url_template, replacement_messages) + **0025** (order_lines.bundle_qty ölçek anlık görüntüsü + 5 sıcak-yol index). ANA ŞİKÂYET ÇÖZÜLDÜ: eşleme sonradan kurulunca eski siparişler bekliyordu → yeni pending-lines servisi eşlemeyi GERİYE DÖNÜK uygular (advisory-lock + FOR UPDATE + canceled/held guard; otomatik eşleştirme YOK) + satır başına "neden bekliyor" tanısı. Yeni: lisans envanteri (arama/filtre/sayfalama/void), canlı iş istasyonu (tek ETag'li poll, gizli sekmede durur), bildirim çanı (ses varsayılan KAPALI), destek yazışması, karantina CSV (tedarikçi/iç-denetim ayrımı), mağaza sipariş linki (SALT YÖNLENDİRME). Denetim: [YÜKSEK] destek yazışması hiç açılmıyordu (yanıt şekli) · [ORTA] bundleQty çift-ölçekleme (bedava lisans) + ölçek kaybı (canlı anahtar geri alınıyordu) · karantina reveal audit · lowStockCount tam tarama (15sn poll) · consumeMultiUseCapacity assigned_at | **/health 200 v1.0.0** (db+redis) · 0024+0025 prod uygulandı (bundle_qty/replacement_messages/read_at/index = 1) · 12 yeni rota mapped · boot hatası 0 · typecheck api+admin temiz · admin build · **VPS izole test DB: entegrasyon 124/124 + yarış 3/3** (6 yeni pending-lines + 3 yeni ölçek regresyon testi) · PHP-lint 13/13 · dev E2E: gerçek eşlenmemiş sipariş çözüldü+teslim edildi, tekrar çalıştırma no-op (çifte teslimat yok) · eklenti v0.8.0 publish 201 · deploy.sh rollback'li |
| 2026-07-28 | 9b81c9b | admin | **BUG FIX** (kullanıcı: ürün detayı eşleme kutusunda "Katalog: Katalog alınamadı"): kök neden `products/[id]/actions.ts` ('use server') `export const initialStockAdjustState = {ok:false}` → Next 15 "A 'use server' file can only export async functions, found object" ile TÜM action chunk'ını patlatıyordu → o sayfadaki HER server action (yeni fetchSiteCatalogAction + mevcut stok-düzeltme) bozuktu. Başlangıç durumu objesi tüketici istemci bileşenine (stock-adjust-form.tsx) taşındı. Tarama: admin'deki 18 'use server' dosyasının hiçbirinde artık const/let/var/default export yok. Migration YOK | dev+prod admin logları "found object" hatasından TEMİZ (0) · typecheck+build temiz · dev product page client-console hatasız · /health 200 · deploy.sh rollback'li |
| 2026-07-28 | 471c955 | api + admin | Ürün detayı eşleme kutusu KATALOG-TABANLI (kullanıcı: "products'taki eşleştirme de tutarlı olsun, farklılık olmasın"): ham ID Input'ları ("ör. 555") KALDIRILDI → site seç → mağaza ürününü ADIYLA seç (yeni `fetchSiteCatalogAction` sunucu-taraflı, ADMIN_TOKEN gizli; Combobox ID/SKU aramaya dahil, eşli olanlar ipuçlu). Mevcut eşlemeler mağaza ADIYLA (detailMappings `site_remote_products` leftJoin → remoteName). Aksiyon: Pasifleştir/Etkinleştir + **Kaldır** (onaylı, /mappings ile tutarlı). removeMappingAction productId→/products/:id revalidate. **Otomatik eşleştirme YOK.** Migration YOK (mevcut audited uçları kullanır) | /health 200 v1.0.0 · typecheck api+admin temiz · admin build · dev doğrulandı (detail.remoteName 12→"Test Lisans"/16→"Windows 11"; sayfa HTML: ham-ID YOK, "Mağaza ürünü"+"ID yazmanıza gerek yok"+Kaldır+mağaza adı VAR) · deploy.sh rollback'li |
| 2026-07-28 | 8c2d61b | api + admin + eklenti v0.7.0 | **Proaktif katalog senkronu** (kullanıcı: reaktif-eşleme saçma, sipariş beklemeden adıyla eşlemek istiyorum): **migration 0023** `site_remote_products` (mağaza ürün SNAPSHOT'ı, SIR YOK; eşlemeler ayrı tabloda). Site-facing HMAC `POST /v1/site-mappings/catalog` (WP tam snapshot iter, advisory-lock'lu, boş=no-op). Panel `/mappings` "Site Kataloğu": site seç→tüm ürünleri adıyla gör→sipariş beklemeden eşle. **OTOMATİK EŞLEŞTİRME YOK** (güvenlik, kullanıcı isteği) — katalog yalnız liste getirir. **Eşlemeyi Değiştir/Kaldır** (PATCH remap {productId} + yeni DELETE /mappings/:id; katalog eşli satırda Değiştir + onaylı Kaldır). **Tazeleme:** polling YOK, olay-güdümlü (~3dk) + WP butonu + **hash-skip** (stok/fiyat edit'inde gereksiz push atlanır). Adversaryel denetim: 9-ajan/4-lens → 4 CONFIRMED low (advisory-lock, boş-wipe no-op, LIMIT 2000→5000, geçersiz ?site= sayfa-boşalma) + odaklı denetim 1 MED (reconnect hash reset) — hepsi deploy-ÖNCESİ düzeltildi | /health 200 **v1.0.0** (db+redis) · migration 0023 prod boot auto (site_remote_products) · 5 yeni rota mapped (catalog/summary, catalog, mappings/:id PATCH+DELETE, site-mappings/catalog) · typecheck api+admin temiz · admin build · PHP-lint 3/3 · **dev E2E tam** (gerçek WP collector→HMAC→snapshot; proaktif eşle 201→dedup 409; varyasyon çözümü; remap 200; delete 200→404; boş-push wipe-YOK 3→3) · eklenti v0.7.0 publish 201 (id 0952e346) · deploy.sh rollback'li |
| 2026-07-28 | ba19811 | api + admin + eklenti v0.6.0 | Ürün eşleştirme iyileştirmesi (kullanıcı: doğruluğu belirsiz, elle ham ID): mağaza ürün ADI öğrenme + "Eşlenmemiş Gelen Ürünler" ekranı. **migration 0022** (order_lines += remote_product_id/variation/name, additive nullable). WP v0.6.0 push'a remoteName ekler. `listUnmapped()` + `GET /v1/admin/mappings/unmapped` (gerçek siparişlerden eşlenmemiş ürünler). YENİ **/mappings** ekranı: adıyla gör → tek-tıkla eşle (elle ID yok). createMapping NULL-varyasyon çift-eşleme açığı kapatıldı (advisory-lock upsertSiteMapping ile aynı anahtar). Adversaryel denetim: 2 bulgu (çift-yazar kilit anahtarı + remoteName sipariş-bloklama) deploy-ÖNCESİ düzeltildi | /health 200 v1.0.0 · typecheck api+admin+shared temiz · admin build · dev E2E: eşlenmemiş→tek-tıkla-eşle 201→düştü→dedup 409 · PHP-lint temiz · migration 0022 prod (api boot auto) · unmapped route 200 · eklenti v0.6.0 publish 201 |
| 2026-07-28 | d5c958c | api + admin | Platform-bağımsız UI etiketleri: sunum katmanındaki WooCommerce-sabit metinler → "mağaza / satış kanalı" (çekirdek zaten agnostik: jenerik HMAC remote* kontratı + tipli site kanalı woocommerce\|marketplace\|reseller). Sipariş detayı çipi PLATFORM-FARKINDA (siteType'a göre); eşleme formu "Mağaza ürün ID"; create-site/site-config/sites/wizard/stock/products/rehber nötrleşti; rehber+wizard'da "bugün hazır entegrasyon: WooCommerce eklentisi" bilinçli tutuldu. detail() +siteType (migration YOK) | /health 200 v1.0.0 · typecheck api+admin temiz · admin build temiz · deploy.sh rollback'li |
| 2026-07-27 | 46741ee | api + admin | UI/UX dalgası 2 (kullanıcı geri bildirimi + ultracode workflow): sipariş detayı 2-kolon + etiketli aksiyon butonları + lisans/hesap MASKESİZ (admin detail() düz payload/fields + görüntülemede tek 'reveal' audit; WP/müşteri maskeli KALDI) + iptal sebebi + çok-ürün kart aksanı; panel geneli StatStrip (tedarikçi/ürün/site/müşteri/PO detayları); güvenlik jargon (§N/UUID→domain); YENİ /quarantine (Karantina/Değiştirilen Anahtarlar — quarantined/voided ölü key'ler, sebep+kaynak sipariş, migration YOK); 8 UX düzeltmesi (PO Sheet, Dead-letter→Başarısız İşler, COGS→Türkçe…) + 8 adversaryel bulgu düzeltmesi (voided sebebi stock_adjustments, MAK dedup, NULLS LAST, audit try/catch) | /health 200 v1.0.0 (db+redis) · typecheck 2/2 + admin build · dev canlı doğrulandı (maskesiz key + /quarantine API 6 satır gerçek veri) · quarantine route mapped · deploy.sh rollback'li |
| 2026-07-27 | bfa4e58 | admin | Sipariş detayı yerleşim yeniden tasarımı (1. tur): 4 büyük stat kartı → tek ince özet şeridi; "Satırlar" + "Aktif Lisanslar" tek ÜRÜN-MERKEZLİ "Ürünler ve Lisanslar" bölümünde birleşti; öncelik sırası (uyarı/destek → ürünler → geçmiş/referans katlanır); zaman çizelgesi yükseklik-sınırlı | typecheck temiz · dev canlı doğrulandı · prod deploy (admin) /health 200 |
| 2026-07-27 | 1a0d219 | api + admin + eklenti v0.5.1 | Sipariş detayı + destek akışı UX/correctness dalgası (kullanıcı geri bildirimi + 4-lens denetim/23 bulgu): sipariş detayında ürün adı + site + held uyarısı + aktif/iptal atama ayrımı + değişim geçmişinde tam eski key; "Kalanları Ata" added=0 dürüst raporlama; support↔sipariş çift yönlü bağ + siparişte inline değişim onay/red; replacements.approve TOCTOU advisory-lock + onay bildirimi; WP notları Türkçe (ham event/enum sızmaz) + başarısızlık notu. Migration YOK | /health 200 v1.0.0 · entegrasyon 115/115 + yarış 3/3 · PHP-lint temiz · dev canlı deneyim (approve→409 zaten-çözülmüş, eski key tam) · plugin v0.5.1 publish |
| 2026-07-27 | 48513d7 | api + admin + eklenti v0.5.0 | §7 WP-parite: lisanslar sipariş-kalemi altında (per-line, uzun yan metabox yerine) + **yenilenmiş kart UI** (özet sayaç, renkli durum rozetleri, ikonlu/hiyerarşik butonlar, ürün-bazlı Bonus Ekle, 5+ anahtarda kaydırma, katlanır değişim geçmişi) + kısmi-iade net-adet re-sync (bundleQty ölçekli, aşırı-revoke kapandı) + 16 WP denetim düzeltmesi. Backend değişmedi (kart UI saf sunum, migration yok) | /health 200 v1.0.0 · entegrasyon 115/115 + yarış 3/3 · PHP-lint temiz · plugin publish 201 (id 411e750b) |
| 2026-07-27 | 74a53e6 | api + admin + eklenti v0.4.0 | §7 D2 (meta box operasyon katmanı: reveal/replace/suspend/bonus/resend + admin-view, site-scoped HMAC + wp:kullanıcı@site audit) + D3 (ürün eşleme kutusu + liste filtresi + bundle) + P1 admin UX + P2 perf + P3 güvenlik; 5-lens denetim → 3 HIGH + 2 MED düzeltildi | /health 200 v1.0.0 · entegrasyon 114/114 + yarış 3/3 · PHP-lint 11/11 · tüm yeni rota map'lendi · plugin publish 201 |
| 2026-07-27 | 2c6be72 | api + eklenti v0.3.0 | §7 WP-parite D1: kısmi-iade satır-revoke (bedava-lisans kapandı) + tanılama sekmesi + admin bar + müşteri cila (parola göster/gizle, ilerleme, toplu .txt, canlı yoklama) | /health 200 · PHP-lint 3/3 · entegrasyon 94/94 · dev kısmi-iade e2e (3→2, karantina) |
| 2026-07-27 | ada9e12 | api + admin | Teslimat-hazırlık denetimi (5-lens, 16 bulgu): deploy detached-HEAD + runner log cap + WP async/read-timeout + connect→webhookUrl + sürüm 1.0.0 | /health 200 **v1.0.0** · PHP-lint 9/9 · entegrasyon 94/94 + yarış 2/2 · yeni deploy.sh (admin probe) sahada |
| 2026-07-27 | 9d050a9 | api + admin | Tam rename (jetlisans→@lisans/wpteslimat) + panelden dağıtım yönetimi (/deployments, migration 0021) | /health 200 · entegrasyon 92/92 + yarış 2/2 |
| 2026-07-27 | ecbc51b | admin | Panelden-dağıtım E2E kanıtı (runner claim→deploy→success) | /health 200 |
| 2026-07-27 | 5c8dda8 | admin | Sürüm yönetim sistemi + /releases sayfası (deploy.sh ile) | /health 200 (auto-rollback'li) |
| 2026-07-27 | 6dffa26 | admin | (docs) marka notu | — |
| 2026-07-27 | 49cf534 | api + admin | Marka "WP Teslimat Eklentisi" (görünen ad) | /health 200 |
| 2026-07-27 | 712e328 | api | 5-lens denetim 9 bulgu düzeltmesi | /health 200 · entegrasyon 89/89 + yarış 2/2 |
| 2026-07-27 | 04f8f5f | api + admin | Site→müşteri hiyerarşisi | /health 200 |

<!-- YENİ KAYITLAR YUKARIYA, tablo başlığının hemen altına eklenir (deploy.sh böyle yapar). -->
