# Dağıtım Günlüğü (Deploy Log)

Prod'a (VPS) yapılan her dağıtımın **append-only** kaydı. En yeni en üstte.
Her satır: tarih · sürüm/commit · ne dağıtıldı · sonuç. `scripts/deploy.sh` bu dosyaya
otomatik satır ekler; elle dağıtımda da buraya yaz. Bu, "güncelleme geçmişi"nin kalıcı
kaydıdır (sohbet hafızasından bağımsız).

Hedef: `root@167.233.108.12:/opt/lisans-yonetim-paneli` · URL'ler:
`api.167-233-108-12.sslip.io` / `admin.167-233-108-12.sslip.io`

| Tarih | Commit | Servis(ler) | Not | Sonuç |
|---|---|---|---|---|
| 2026-07-28 | d18e442 | api + admin + eklenti v0.9.0 | **Kullanıcının 4 şikâyeti** (eşlenmemiş sipariş görünmüyor · mağaza-panel gecikmesi · karantina süzme/indirme · "mağazada aç" URL'i yanlış). Kök nedenler dev'de GERÇEK veriyle ÖLÇÜLDÜ. 3 dalga paralel işçi + 5 lensli çekişmeli denetim (23 ajan → 32 doğrulanmış bulgu, hepsi kapatıldı). **migration 0026** (additive) `sites.admin_order_url_template_manual` = şablon kaynağı. **(1)** Kayıp sipariş `wc-on-hold` (havale) idi → hiç push edilmiyordu; ödenmemiş sipariş yine teslim EDİLMEZ (§2) ama mağaza listesinde "Panele iletilmedi — ödeme bekleniyor" etiketi+filtresi. `/v1/admin/pending` artık `unmapped` da döndürür (ayrı limitler) + satır başına `hasUnmappedLine`. `unmappedOrders` SATIR-tabanlı oldu (çok kalemli siparişte tek eşlemesiz kalem artık kaçmıyor); pano alarmı gerçek talepten türetiliyor, katalog sayacı BİLGİ (varyasyon ebeveyni hariç). **(2)** Gecikme ölçüldü **41 sn → 0,6 sn**: iş artık yanıt gönderildikten sonra aynı istekte koşuyor (fastcgi/litespeed; ikisi de yoksa sınırlı satır-içi), AS güvenlik ağı korunuyor; REST dışlanmıyor (Store API blok checkout); atomik iş kilidi + kuyruktaki ikizin iptali. **(3)** Karantina: tarih süzgeci SQL'e taşındı, `truncated` ham satır sayısından, indirme kapsam+biçim seçimli, "İç denetim" CSV'sine de KVKK uyarısı. **(4)** `storeAdminUrl` webhook iç hostname'inden türetiliyordu (`http://wordpress/...`) + HPOS tahmini → artık YALNIZ eklentinin bildirdiği şablon; yoksa link YOK. Ayrıca `syncRefunds` advisory-lock + tek tx (kilitsiz RMW → fazla revoke = canlı anahtar kaybı) | **prod /v1/health 200 v1.0.0** (db+redis) · migration 0026 uygulandı (tracking 27, kolon canlı) · boot ERROR 0 · admin 307 (auth) · typecheck api+admin+shared temiz · admin build · api birim **45/45** (14'ü yeni store-admin-url — eski dosya hiçbir config'e girmiyordu, hiç koşmuyordu) · **VPS izole test DB: entegrasyon 124/124 + yarış 3/3** · PHP-lint **12/12** · **dev E2E ölçümlü**: mod_php'de 41 sn → **0,6 sn** (sipariş fulfilled) · unmapped sipariş `/pending`'de `hasUnmappedLine:true`, üç sayaç tutarlı (1/1/1) · storeAdminUrl önce `null` (yanlış link YOK) → katalog senkronundan sonra `https://dev-wp…/wp-admin/admin.php?page=wc-orders&action=edit&id=24` (status `accepted`) · eklenti v0.9.0 publish 201 · deploy.sh rollback'li |
| 2026-07-28 | 73062fe | api + admin + eklenti v0.8.0 | **İş istasyonu partisi** (kullanıcının 13 maddelik listesi) + **5-lensli çekişmeli denetim** (32 ajan → 23 doğrulanmış bulgu, hepsi düzeltildi). **migration 0024** (notifications.read_at + kısmi index, sites.admin_order_url_template, replacement_messages) + **0025** (order_lines.bundle_qty ölçek anlık görüntüsü + 5 sıcak-yol index). ANA ŞİKÂYET ÇÖZÜLDÜ: eşleme sonradan kurulunca eski siparişler bekliyordu → yeni pending-lines servisi eşlemeyi GERİYE DÖNÜK uygular (advisory-lock + FOR UPDATE + canceled/held guard; otomatik eşleştirme YOK) + satır başına "neden bekliyor" tanısı. Yeni: lisans envanteri (arama/filtre/sayfalama/void), canlı iş istasyonu (tek ETag'li poll, gizli sekmede durur), bildirim çanı (ses varsayılan KAPALI), destek yazışması, karantina CSV (tedarikçi/iç-denetim ayrımı), mağaza sipariş linki (SALT YÖNLENDİRME). Denetim: [YÜKSEK] destek yazışması hiç açılmıyordu (yanıt şekli) · [ORTA] bundleQty çift-ölçekleme (bedava lisans) + ölçek kaybı (canlı anahtar geri alınıyordu) · karantina reveal audit · lowStockCount tam tarama (15sn poll) · consumeMultiUseCapacity assigned_at | **/health 200 v1.0.0** (db+redis) · 0024+0025 prod uygulandı (bundle_qty/replacement_messages/read_at/index = 1) · 12 yeni rota mapped · boot hatası 0 · typecheck api+admin temiz · admin build · **VPS izole test DB: entegrasyon 124/124 + yarış 3/3** (6 yeni pending-lines + 3 yeni ölçek regresyon testi) · PHP-lint 13/13 · dev E2E: gerçek eşlenmemiş sipariş çözüldü+teslim edildi, tekrar çalıştırma no-op (çifte teslimat yok) · eklenti v0.8.0 publish 201 · deploy.sh rollback'li |
| 2026-07-28 | 9b81c9b | admin | **BUG FIX** (kullanıcı: ürün detayı eşleme kutusunda "Katalog: Katalog alınamadı"): kök neden `products/[id]/actions.ts` ('use server') `export const initialStockAdjustState = {ok:false}` → Next 15 "A 'use server' file can only export async functions, found object" ile TÜM action chunk'ını patlatıyordu → o sayfadaki HER server action (yeni fetchSiteCatalogAction + mevcut stok-düzeltme) bozuktu. Başlangıç durumu objesi tüketici istemci bileşenine (stock-adjust-form.tsx) taşındı. Tarama: admin'deki 18 'use server' dosyasının hiçbirinde artık const/let/var/default export yok. Migration YOK | dev+prod admin logları "found object" hatasından TEMİZ (0) · typecheck+build temiz · dev product page client-console hatasız · /health 200 · deploy.sh rollback'li |
| 2026-07-28 | 471c955 | api + admin | Ürün detayı eşleme kutusu KATALOG-TABANLI (kullanıcı: "products'taki eşleştirme de tutarlı olsun, farklılık olmasın"): ham ID Input'ları ("ör. 555") KALDIRILDI → site seç → mağaza ürününü ADIYLA seç (yeni `fetchSiteCatalogAction` sunucu-taraflı, ADMIN_TOKEN gizli; Combobox ID/SKU aramaya dahil, eşli olanlar ipuçlu). Mevcut eşlemeler mağaza ADIYLA (detailMappings `site_remote_products` leftJoin → remoteName). Aksiyon: Pasifleştir/Etkinleştir + **Kaldır** (onaylı, /mappings ile tutarlı). removeMappingAction productId→/products/:id revalidate. **Otomatik eşleştirme YOK.** Migration YOK (mevcut audited uçları kullanır) | /health 200 v1.0.0 · typecheck api+admin temiz · admin build · dev doğrulandı (detail.remoteName 12→"Test Lisans"/16→"Windows 11"; sayfa HTML: ham-ID YOK, "Mağaza ürünü"+"ID yazmanıza gerek yok"+Kaldır+mağaza adı VAR) · deploy.sh rollback'li |
| 2026-07-28 | 8c2d61b | api + admin + eklenti v0.7.0 | **Proaktif katalog senkronu** (kullanıcı: reaktif-eşleme saçma, sipariş beklemeden adıyla eşlemek istiyorum): **migration 0023** `site_remote_products` (mağaza ürün SNAPSHOT'ı, SIR YOK; eşlemeler ayrı tabloda). Site-facing HMAC `POST /v1/site-mappings/catalog` (WP tam snapshot iter, advisory-lock'lu, boş=no-op). Panel `/mappings` "Site Kataloğu": site seç→tüm ürünleri adıyla gör→sipariş beklemeden eşle. **OTOMATİK EŞLEŞTİRME YOK** (güvenlik, kullanıcı isteği) — katalog yalnız liste getirir. **Eşlemeyi Değiştir/Kaldır** (PATCH remap {productId} + yeni DELETE /mappings/:id; katalog eşli satırda Değiştir + onaylı Kaldır). **Tazeleme:** polling YOK, olay-güdümlü (~3dk) + WP butonu + **hash-skip** (stok/fiyat edit'inde gereksiz push atlanır). Adversaryel denetim: 9-ajan/4-lens → 4 CONFIRMED low (advisory-lock, boş-wipe no-op, LIMIT 2000→5000, geçersiz ?site= sayfa-boşalma) + odaklı denetim 1 MED (reconnect hash reset) — hepsi deploy-ÖNCESİ düzeltildi | /health 200 **v1.0.0** (db+redis) · migration 0023 prod boot auto (site_remote_products) · 5 yeni rota mapped (catalog/summary, catalog, mappings/:id PATCH+DELETE, site-mappings/catalog) · typecheck api+admin temiz · admin build · PHP-lint 3/3 · **dev E2E tam** (gerçek WP collector→HMAC→snapshot; proaktif eşle 201→dedup 409; varyasyon çözümü; remap 200; delete 200→404; boş-push wipe-YOK 3→3) · eklenti v0.7.0 publish 201 (id 0952e346) · deploy.sh rollback'li |
| 2026-07-28 | ba19811 | api + admin + eklenti v0.6.0 | Ürün eşleştirme iyileştirmesi (kullanıcı: doğruluğu belirsiz, elle ham ID): mağaza ürün ADI öğrenme + "Eşlenmemiş Gelen Ürünler" ekranı. **migration 0022** (order_lines += remote_product_id/variation/name, additive nullable). WP v0.6.0 push'a remoteName ekler. `listUnmapped()` + `GET /v1/admin/mappings/unmapped` (gerçek siparişlerden eşlenmemiş ürünler). YENİ **/mappings** ekranı: adıyla gör → tek-tıkla eşle (elle ID yok). createMapping NULL-varyasyon çift-eşleme açığı kapatıldı (advisory-lock upsertSiteMapping ile aynı anahtar). Adversaryel denetim: 2 bulgu (çift-yazar kilit anahtarı + remoteName sipariş-bloklama) deploy-ÖNCESİ düzeltildi | /health 200 v1.0.0 · typecheck api+admin+shared temiz · admin build · dev E2E: eşlenmemiş→tek-tıkla-eşle 201→düştü→dedup 409 · PHP-lint temiz · migration 0022 prod (api boot auto) · unmapped route 200 · eklenti v0.6.0 publish 201 |
| 2026-07-28 | d5c958c | api + admin | Platform-bağımsız UI etiketleri: sunum katmanındaki WooCommerce-sabit metinler → "mağaza / satış kanalı" (çekirdek zaten agnostik: jenerik HMAC remote* kontratı + tipli site kanalı woocommerce\|marketplace\|reseller). Sipariş detayı çipi PLATFORM-FARKINDA (siteType'a göre); eşleme formu "Mağaza ürün ID"; create-site/site-config/sites/wizard/stock/products/rehber nötrleşti; rehber+wizard'da "bugün hazır entegrasyon: WooCommerce eklentisi" bilinçli tutuldu. detail() +siteType (migration YOK) | /health 200 v1.0.0 · typecheck api+admin temiz · admin build temiz · deploy.sh rollback'li |
| 2026-07-27 | 46741ee | api + admin | UI/UX dalgası 2 (kullanıcı geri bildirimi + ultracode workflow): sipariş detayı 2-kolon + etiketli aksiyon butonları + lisans/hesap MASKESİZ (admin detail() düz payload/fields + görüntülemede tek 'reveal' audit; WP/müşteri maskeli KALDI) + iptal sebebi + çok-ürün kart aksanı; panel geneli StatStrip (tedarikçi/ürün/site/müşteri/PO detayları); güvenlik jargon (§N/UUID→domain); YENİ /quarantine (Karantina/Değiştirilen Anahtarlar — quarantined/voided ölü key'ler, sebep+kaynak sipariş, migration YOK); 8 UX düzeltmesi (PO Sheet, Dead-letter→Başarısız İşler, COGS→Türkçe…) + 8 adversaryel bulgu düzeltmesi (voided sebebi stock_adjustments, MAK dedup, NULLS LAST, audit try/catch) | /health 200 v1.0.0 (db+redis) · typecheck 2/2 + admin build · dev canlı doğrulandı (maskesiz key + /quarantine API 6 satır gerçek veri) · quarantine route mapped · deploy.sh rollback'li |
| 2026-07-27 | bfa4e58 | admin | Sipariş detayı yerleşim yeniden tasarımı (1. tur): 4 büyük stat kartı → tek ince özet şeridi; "Satırlar" + "Aktif Lisanslar" tek ÜRÜN-MERKEZLİ "Ürünler ve Lisanslar" bölümünde birleşti; öncelik sırası (uyarı/destek → ürünler → geçmiş/referans katlanır); zaman çizelgesi yükseklik-sınırlı | typecheck temiz · dev canlı doğrulandı · prod deploy (admin) /health 200 |
| 2026-07-27 | 1a0d219 | api + admin + eklenti v0.5.1 | Sipariş detayı + destek akışı UX/correctness dalgası (kullanıcı geri bildirimi + 4-lens denetim/23 bulgu): sipariş detayında ürün adı + site + held uyarısı + aktif/iptal atama ayrımı + değişim geçmişinde tam eski key; "Kalanları Ata" added=0 dürüst raporlama; support↔sipariş çift yönlü bağ + siparişte inline değişim onay/red; replacements.approve TOCTOU advisory-lock + onay bildirimi; WP notları Türkçe (ham event/enum sızmaz) + başarısızlık notu. Migration YOK | /health 200 v1.0.0 · entegrasyon 115/115 + yarış 3/3 · PHP-lint temiz · dev canlı deneyim (approve→409 zaten-çözülmüş, eski key tam) · plugin v0.5.1 publish |
| 2026-07-27 | 48513d7 | api + admin + eklenti v0.5.0 | §7 WP-parite: lisanslar sipariş-kalemi altında (per-line, uzun yan metabox yerine) + **yenilenmiş kart UI** (özet sayaç, renkli durum rozetleri, ikonlu/hiyerarşik butonlar, ürün-bazlı Bonus Ekle, 5+ anahtarda kaydırma, katlanır değişim geçmişi) + kısmi-iade net-adet re-sync (bundleQty ölçekli, aşırı-revoke kapandı) + 16 WP denetim düzeltmesi. Backend değişmedi (kart UI saf sunum, migration yok) | /health 200 v1.0.0 · entegrasyon 115/115 + yarış 3/3 · PHP-lint temiz · plugin publish 201 (id 411e750b) |
| 2026-07-27 | 74a53e6 | api + admin + eklenti v0.4.0 | §7 D2 (meta box operasyon katmanı: reveal/replace/suspend/bonus/resend + admin-view, site-scoped HMAC + wp:kullanıcı@site audit) + D3 (ürün eşleme kutusu + liste filtresi + bundle) + P1 admin UX + P2 perf + P3 güvenlik; 5-lens denetim → 3 HIGH + 2 MED düzeltildi | /health 200 v1.0.0 · entegrasyon 114/114 + yarış 3/3 · PHP-lint 11/11 · tüm yeni rota map'lendi · plugin publish 201 |
| 2026-07-27 | 2c6be72 | api + eklenti v0.3.0 | §7 WP-parite D1: kısmi-iade satır-revoke (bedava-lisans kapandı) + tanılama sekmesi + admin bar + müşteri cila (parola göster/gizle, ilerleme, toplu .txt, canlı yoklama) | /health 200 · PHP-lint 3/3 · entegrasyon 94/94 · dev kısmi-iade e2e (3→2, karantina) |
| 2026-07-27 | ada9e12 | api + admin | Teslimat-hazırlık denetimi (5-lens, 16 bulgu): deploy detached-HEAD + runner log cap + WP async/read-timeout + connect→webhookUrl + sürüm 1.0.0 | /health 200 **v1.0.0** · PHP-lint 9/9 · entegrasyon 94/94 + yarış 2/2 · yeni deploy.sh (admin probe) sahada |
| 2026-07-27 | 9d050a9 | api + admin | Tam rename (jetlisans→@lisans/wpteslimat) + panelden dağıtım yönetimi (/deployments, migration 0021) | /health 200 · entegrasyon 92/92 + yarış 2/2 |
| 2026-07-27 | ecbc51b | admin | Panelden-dağıtım E2E kanıtı (runner claim→deploy→success) | /health 200 |
| 2026-07-27 | 5c8dda8 | admin | Sürüm yönetim sistemi + /releases sayfası (deploy.sh ile) | /health 200 (auto-rollback'li) |
| 2026-07-27 | 6dffa26 | admin | (docs) marka notu | — |
| 2026-07-27 | 49cf534 | api + admin | Marka "WP Teslimat Eklentisi" (görünen ad) | /health 200 |
| 2026-07-27 | 712e328 | api | 5-lens denetim 9 bulgu düzeltmesi | /health 200 · entegrasyon 89/89 + yarış 2/2 |
| 2026-07-27 | 04f8f5f | api + admin | Site→müşteri hiyerarşisi | /health 200 |

<!-- YENİ KAYITLAR YUKARIYA, tablo başlığının hemen altına eklenir (deploy.sh böyle yapar). -->
