{
  "name": "@jetlisans/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Jetlisans admin paneli — Playwright uçtan-uca duman testleri (CI/manuel artifact; deploy edilmez)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
